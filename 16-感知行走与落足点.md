# 第 16 章：感知行走与落足点——机器人怎样从“看见台阶”走到“真的踩稳”

> **所属部分**：第三部分 · 学习控制。
>
> **前置知识**：[第 05 章：状态估计](05-状态估计.md)、[第 14 章：腿足强化学习工程](14-腿足强化学习工程.md)、[第 15 章：特权学习与鲁棒行走](15-特权学习与鲁棒行走.md)。
>
> **本章只追踪一个场景**：机器人以 `0.6 m/s` 向前走，前方突然出现一个 `15 cm` 高的台阶，台阶后还有一块不能踩的缺口。
>
> **下一步**：[第 17 章：动作重映射与模仿学习](17-动作重映射与模仿学习.md)。

---

## 0. 先说结论：相机不会直接告诉机器人“下一脚放这里”

人看见台阶，似乎会自然地抬脚、跨上去。但把这个过程拆开，会发现中间至少有五层：

```text
相机收到光或深度测量
→ 判断前方可能有什么几何结构
→ 判断这些结构相对当前机器人在哪里
→ 选择一个腿够得到、脚放得下的位置
→ 生成并执行摆腿动作
→ 脚真正接触后，用力觉和身体反馈确认是否踩稳
```

因此，下面四件事不是同一个东西：

```text
看见一个表面
≠ 正确估计表面位置
≠ 选择了可行落脚点
≠ 脚最终真的踩在那里并站稳
```

本章最重要的世界观是：

> 视觉提供的是关于未来接触位置的证据；控制器利用这些证据提前准备动作；真实接触反馈最后检验预测是否正确。

---

## 第一遍：为什么只靠身体感觉不够

### 1. 一台“摸着走”的机器人遇到了台阶

第 15 章的机器人已经很鲁棒。它能感受：

- 身体角速度；
- 身体倾斜线索；
- 关节位置和速度；
- 上一次动作；
- 过去一小段身体历史。

它在平地上走得很好，也能在摩擦变化后逐渐适应。

现在，它以 `0.6 m/s` 向前走，右脚正准备向前摆。前方 `40 cm` 处有一个 `15 cm` 高台阶。

如果机器人只有本体感知——也就是主要感受自己的身体——它什么时候才知道有台阶？

很可能是脚尖已经撞上去之后：

```text
右脚按平地步态向前摆
→ 脚尖撞到台阶侧面
→ 关节运动和身体姿态出现异常
→ 传感器才收到变化
→ 策略开始补救
```

它并非完全不能恢复，但反应发生得太晚。台阶是一个“未来接触问题”：在脚到达之前，机器人就需要知道应该抬多高、向前放多远。

这就是感知行走要补上的能力：

```text
不仅根据已经发生的身体变化反应，
还根据前方环境证据，为尚未发生的接触提前行动。
```

---

### 2. 加上相机后，问题没有自动解决

我们在机器人头部装一台深度相机。它每秒拍摄 30 帧，也就是大约每 `33 ms` 得到一张深度图。

深度图不是彩色照片。它更像一张数字表格：

```text
每个像素 → 相机沿那个方向看到的表面距离
```

例如图像中央某个像素的值是 `1.2 m`，表示相机沿该像素对应的视线方向，大约 `1.2 m` 处碰到了一个表面。

但一个深度值并没有直接写着：

```text
“这是台阶顶面”
“右脚应该踩这里”
“这里摩擦足够”
```

策略或感知模块仍要结合：

- 相机内参：每个像素对应空间中的哪个方向；
- 相机外参：相机安装在机器人身体的哪里、朝向哪里；
- 机器人当前姿态：拍摄这一帧时身体怎样倾斜；
- 时间戳：这张图到底是什么时刻拍的；
- 任务命令：机器人想向前、侧移还是转弯。

所以，“装了相机”只意味着多了一类测量。它还没有变成可执行的落脚决定。

---

### 3. 深度图为什么只能算证据

真机深度图会遇到：

- 物体互相遮挡；
- 过近或过远区域测不到；
- 反光、透明或吸光表面产生空洞；
- 强光导致结构化误差；
- 相机震动和外参偏差；
- 视场有限，脚边或身体后方看不见；
- 图像传输和计算带来延迟。

因此，深度图不是“地形真值”。更准确的关系是：

```text
真实地形
→ 光学测量过程
→ 带噪声、空洞、遮挡和延迟的深度图
→ 对真实地形的当前估计
```

这与第 05 章的状态估计是同一种思想：

> 机器人永远在根据不完整证据判断一个看不见的真实世界。

仿真训练时必须主动破坏过于完美的深度图，例如加入：

- 无效像素；
- 结构化空洞；
- 深度偏差；
- 外参扰动；
- 随机丢帧；
- 图像与身体状态之间的时间错位。

只加一点独立高斯噪声，通常不能代表真实相机的主要问题。

---

## 第二遍：把“相机看到的过去”变成“机器人脚边的现在”

### 4. 同一个台阶，在三个坐标系里位置不同

相机先得到的是“台阶相对相机在哪里”。但落脚控制真正关心的是：

```text
台阶相对机器人身体或当前支撑脚在哪里？
```

假设相机装在头部，身体正在前倾。图像中的“正前方”并不等于地面的水平方向。必须利用已知的相机安装关系和当前身体姿态，把点从相机坐标系变换到机器人或世界坐标系。

概念上是：

```text
相机中的地形点
→ 根据相机安装位姿变到身体坐标系
→ 根据当前估计的身体位姿变到局部世界坐标系
```

如果相机外参偏了 2°，远处地面高度就可能被系统性估错。机器人也许以为台阶高 `12 cm`，实际却是 `15 cm`，摆脚高度因此不足。

这说明感知行走不是单独的“视觉问题”。它依赖第 01 章的坐标变换和第 05 章的身体状态估计。

#### 4.1 机器人中心高程图

一种常见中间表示，是 robot-centric elevation map，机器人中心高程图。

可以把机器人周围地面想成铺了一张方格纸：

```text
每一格保存这个水平位置的地面高度估计。
```

数学上常写：

```text
M ∈ R^(H×W)
```

这里：

- `M`：整张局部高度地图；
- `H`：网格有多少行；
- `W`：网格有多少列；
- 每个格子还要有实际分辨率，例如 `0.05 m/格`。

当机器人向前走时，地图也围绕机器人更新。这样，策略不必直接处理整张原始深度图，而能看到更接近落脚问题的局部地面形状。

但这种地图通常是 2.5D：同一个水平位置只保留一个高度。它很适合地面、坡和台阶，却难以同时表示桌面和桌下地面，也容易漏掉头顶障碍。

---

### 5. 最隐蔽的问题：相机看见的是过去

机器人策略以 `50 Hz` 运行，每 `20 ms` 决策一次。相机只有 `30 Hz`，图像处理还需要 `40 ms`。

当策略在当前时刻读取地图时，这份地图可能来自两三个控制周期之前。

这期间机器人仍在向前走：

```text
速度 0.6 m/s × 延迟 0.06 s = 前进约 3.6 cm
```

如果直接把旧图像当作当前地形，台阶就会被认为比实际更远。对只有几十厘米步长的机器人来说，`3.6 cm` 已经可能改变落脚安全性。

所以每份数据都需要时间戳：

```text
图像在什么时候曝光？
身体姿态对应哪个时刻？
策略现在是哪个时刻？
```

系统可以做几件事：

- 用拍摄时刻的身体位姿处理那张图；
- 根据机器人随后运动，把旧地图变换到当前坐标；
- 给策略保留历史，让它自己学习时间关系；
- 估计延迟，并预测当前地形相对位置；
- 感知过旧时主动减速。

“给图像加噪声”和“让图像晚到 `60 ms`”不是同一种困难。前者让位置不够准，后者让信息属于错误的时刻。

---

### 6. 为什么需要历史和时序记忆

单帧深度图只呈现一个瞬间。它可能无法判断：

- 地形在图像中移动，是机器人自己在动，还是障碍在动；
- 某块区域是空洞测量，还是确实没有表面；
- 刚被腿遮住的台阶边缘此前在哪里；
- 当前图像与上一动作之间是什么关系。

所以策略常接收多帧历史，或用 GRU（Gated Recurrent Unit，门控循环单元）等带记忆的网络。

递归形式常写成：

```text
h[t] = GRU(e[t], h[t-1])
```

先翻译成人话：

```text
当前图像和身体信息 e[t]
+ 上一时刻留下的记忆 h[t-1]
→ 更新后的当前记忆 h[t]
```

其中：

- `e[t]`：当前图像与本体感觉经过编码后的特征；这里不是控制误差；
- `h[t-1]`：上一时刻的内部记忆；
- `h[t]`：融合当前证据后的新记忆；
- `GRU`：一种学会保留或遗忘信息的循环神经网络结构。

历史能帮助整合证据，但不能凭空制造真值。如果相机从未看见缺口，所有传感器也没有相关线索，记忆网络不可能可靠地“猜中”缺口。

系统还必须规定：开机时 `h[0]` 怎样初始化、图像长时间丢失后是否清空、机器人被人工搬动后旧记忆是否仍有效。

---

## 第三遍：从地形证据走到一只脚的动作

### 7. 选择落脚点，要同时回答三个问题

现在机器人已经估计出：前方有一块 `15 cm` 高的平台，平台后有缺口。

右脚该放哪里？一个候选位置至少要通过三道检查。

#### 7.1 腿够得到吗

脚不能瞬间出现在任意地方。落脚点必须在当前支撑状态和关节活动范围允许的区域内。

这属于运动学可达性：

```text
那个位置看起来很好
≠ 机器人这条腿当前够得到
```

#### 7.2 脚放得下吗

一个地图点有足够高度还不够。脚底有面积：

- 平台是否比脚底宽；
- 局部坡度是否过大；
- 是否紧挨台阶边缘；
- 周围是否有空洞；
- 地图置信度是否足够。

如果只选“最高分的一个像素”，脚掌可能一半踩平台、一半悬空。

#### 7.3 踩下去后还能继续运动吗

落脚不是孤立动作。它要帮助身体继续向目标速度前进并保持平衡。

例如，台阶顶面有两个都能踩的位置：

- 太靠近身体的位置虽然安全，却让下一步很难向前；
- 太靠近边缘的位置推进更快，却对感知误差非常敏感。

所以选择落脚点是在可达性、几何安全、运动目标和后续平衡之间折中。

学习策略可以隐式完成这种折中；传统规划器也可以显式打分候选区域。无论采用哪一种，问题本身没有消失。

---

### 8. “预测落脚点”不是“真实落脚点”

假设模块选出了右脚目标：

```text
身体前方 32 cm
右侧 10 cm
高度 15 cm
```

这仍只是一份参考。接下来还要：

```text
生成摆脚轨迹
→ 把脚抬过台阶边缘
→ 按时间向目标移动
→ 关节控制器驱动电机
→ 真实脚与平台发生接触
```

中间可能出现：

- 电机响应不足，脚没有抬到计划高度；
- 身体在摆腿期间被推，目标相对位置变化；
- 台阶高度估计偏低；
- 脚提前碰到边缘；
- 脚落在目标附近，但地面摩擦太小而滑动。

因此，真正的闭环还要继续：

```text
视觉预测未来落点
→ 策略提前抬腿和移动身体
→ 足底力、关节和 IMU（Inertial Measurement Unit，惯性测量单元）检测真实接触
→ 状态估计判断是否踩稳
→ 下一次动作根据实际结果修正
```

视觉解决“还没碰到之前怎么办”；本体和接触反馈解决“真正碰到以后发生了什么”。两者不是互相替代，而是覆盖不同时间阶段。

---

### 9. 台阶后还有缺口：为什么显式地形有时有帮助

如果策略只把深度图压成一个抽象向量，我们很难知道它是否真的理解了台阶和缺口。复杂、稀疏地形上，训练信号也可能很弱：摔倒发生时，很难把责任准确归到几步前看见的某块区域。

训练时可以额外要求网络预测一张局部高度图或可通行区域：

```text
视觉和身体历史
→ 内部特征
→ 一路输出动作
→ 另一路预测局部地形
```

地形预测损失可写成：

```text
L_terrain = ‖m_hat[t] - m_gt[t]‖²
```

翻译一下：

- `m_hat[t]`：网络在时刻 `t` 预测的局部地图；
- `m_gt[t]`：仿真提供的同一时刻地形真值，只在训练时当答案；
- 两者逐格相减并平方，预测越错，损失越大；
- `L_terrain`：用于更新网络的地形预测损失。

这叫显式监督：我们不只告诉策略最终有没有走成功，还对“你认为前方地形怎样”提供更直接的训练信号。

但显式地图也不是真理。遇到训练中从未见过的地形时，网络可能自信地预测错误。它仍需要置信度、异常检测和失败出口。

---

## 第四遍：论文中的网络结构到底在解决哪一步

### 10. 隐式表示和显式估计不是二选一

网络内部可以保留一种难以直接命名的特征，常叫 latent，中文可理解为“内部压缩表示”。它也可以同时显式预测速度、地形高度或可通行性。

两者作用不同：

```text
隐式表示：保留那些难以人工完整命名、但对动作有用的信息
显式估计：用明确监督告诉网络某些关键物理量应该是什么
```

例如：

```text
本体历史 → 身体状态特征 ┐
                          ├→ 时序融合 → Actor → 动作
深度图   → 地形视觉特征 ┘
                    └────→ 速度/地形等显式预测
```

这里 Actor 是最终输出动作的策略网络。显式预测可以帮助训练和诊断，隐式特征则允许网络保留我们没有手工列出的信息。

PIE 论文的全名是 [Parkour with Implicit-Explicit Learning Framework for Legged Robots](https://arxiv.org/abs/2408.13740)。它可以作为这类设计的一项具体例子：把本体历史、视觉、隐式表示和显式估计结合起来，用于腿足跑酷控制。论文的真实硬件验证对象是四足机器人；本章借用的是感知—估计—控制的数据流，不把它直接当作人形机器人上已经验证完毕的同一方案。

现在再看到“视觉 + DreamWaQ”“隐式—显式学习”这些名称，应该先把它们放回本章故事：它们在尝试让机器人从带延迟的视觉和身体历史中，形成对当前身体与地形的有用判断，然后输出闭环动作。

论文名字不是新的物理规律。

---

### 11. 注意力：从许多地形点中挑当前更有用的信息

局部地图可能包含几百或几千个格子。机器人当前抬右脚时，不需要同等关注所有位置。

注意力机制可以先用一个很朴素的比喻理解：

```text
当前身体状态和运动命令提出一个问题：
“我这只右脚下一步应该关心哪些地形区域？”

每个地图区域提供自己的特征；
网络给更相关的区域更高权重；
再把这些区域的信息汇总给策略。
```

常见术语是 Query、Key、Value：

- Query：当前机器人状态与任务提出的“查询”；
- Key：每个地图位置用来判断自己是否相关的特征；
- Value：该位置真正要提供给策略的地形内容。

单个注意力头的权重常写成：

```text
α[i] =
exp(q^T k[i] / √d)
────────────────────
Σ_j exp(q^T k[j] / √d)
```

第一遍只需要理解：

```text
匹配程度越高
→ 权重 α[i] 越大
→ 第 i 个地图区域对汇总结果影响越大
```

符号含义：

- `q`：Query 特征，不是关节角；
- `k[i]`：第 `i` 个地图区域的 Key；
- `q^T k[i]`：两者匹配分数；
- `d`：特征维度，除以 `√d` 是为了控制数值尺度；
- 分母对所有区域求和，让所有 `α[i]` 非负且总和为 1。

地图汇总特征可写成：

```text
z_map = Σ_i α[i] · v[i]
```

`v[i]` 是第 `i` 个区域的 Value 特征，`z_map` 是加权汇总后的内部表示。

![注意力权重、地形特征与真实落脚之间的区别](picture/attention_foothold.svg)

必须再次区分：

```text
注意力最高的地图点
≠ 网络明确输出的落脚点
≠ 真实脚底最后接触的位置
```

注意力热图可以帮助诊断，但不自动构成因果证明。要验证策略是否真的依赖某片地形，可以遮挡、移动或替换该区域，再观察动作与成功率怎样改变。

---

## 第五遍：怎样训练，怎样允许它说“我看不清”

### 12. 先学会使用视觉，再把视觉弄脏

如果一开始就同时加入：

- 极难地形；
- 大量深度空洞；
- 随机延迟；
- 外参误差；
- 强扰动；
- 稀疏奖励；

策略可能发现最简单的办法是完全忽略视觉，退化成第 1 节的盲走策略。

可以分两阶段：

```text
阶段 1：较准确的地图 + 简单台阶
目标：先让策略学会“视觉信息确实有用”

阶段 2：逐渐加入噪声、延迟、位姿误差和复杂地形
目标：让它在不完美视觉下仍能合理使用证据
```

怎样确认策略没有偷偷忽略视觉？

- 把深度图置零，比较成功率；
- 打乱图像与时间的对应关系；
- 人为移动某个台阶，看落脚是否随之改变；
- 分别评估盲走能过和必须视觉才能过的地形。

如果图像被打乱后性能完全不变，视觉分支很可能没有发挥实际作用。

---

### 13. 感知系统必须允许“不知道”

现在把场景再变坏一点：机器人走近台阶时，相机被强光干扰，大半深度像素失效。

危险的系统会继续把旧地图当成确定事实，保持 `0.6 m/s` 冲上去。

更合理的系统除了给出地形估计，还给出：

- 当前数据是否有效；
- 地图置信度；
- 图像年龄；
- 哪些区域长期未观测；
- 估计是否超出训练分布。

上层或策略据此选择：

```text
证据可靠 → 正常跨越
证据变差 → 减速、提高摆脚余量
关键区域看不清 → 停下或重新观察
感知失效 → 退化到保守盲走或安全停机
```

机器人不需要对所有场景都自信。知道自己不知道，是安全系统的一部分。

#### 13.1 永远存在的表示边界

- 前视相机通常不擅长可靠后退；
- 高草会遮挡真实地面；
- 透明、镜面和强光会破坏深度；
- 2.5D 地图无法完整表达多层结构；
- 训练中没有墙，策略可能把墙误当成很高的台阶；
- 注意力和大视觉网络还会增加推理延迟。

这些不是再加一点训练时间就必然消失的问题。有些是传感器和表示本身的边界，必须通过多传感器、任务限制或安全降级处理。

---

### 14. 把 `15 cm` 台阶故事完整走一遍

最后把整条链合起来：

```text
1. 机器人收到 0.6 m/s 前进命令

2. 深度相机在过去某一时刻看见前方表面

3. 感知模块结合相机参数、时间戳和身体位姿
   把深度证据变到机器人当前局部坐标

4. 历史模型融合多帧与本体感觉
   估计前方有 15 cm 台阶，后方有缺口

5. 策略或规划器在可达区域内选择安全落脚区域
   同时考虑脚底大小、边缘、后续平衡和速度命令

6. 策略提前提高右脚摆动高度并调整身体

7. 关节控制器和电机执行动作

8. 右脚真正碰到平台

9. 足底力、关节和 IMU 告诉系统：
   接触是否提前、是否踩实、是否滑动

10. 下一控制周期根据真实结果继续修正
```

这条链里没有任何一步拥有上帝视角：

- 深度是测量；
- 地图是估计；
- 落脚点是计划或策略输出；
- 摆脚轨迹是参考；
- 电机命令是请求；
- 真实接触才是现实给出的结果。

感知行走不是让视觉取代反馈控制，而是把反馈能够利用的信息从“已经撞上了什么”扩展到“可能即将撞上或踩到什么”。

---

## 本章小结

机器人从看见台阶到踩稳台阶，依次需要：

1. 把带噪声和延迟的深度图当作证据；
2. 利用坐标变换、身体位姿和时间戳判断地形当前在哪里；
3. 用历史处理遮挡、自运动与旧信息；
4. 在可达性、脚底几何、运动目标和平衡之间选择落脚区域；
5. 把落脚参考变成摆脚和身体动作；
6. 用真实接触反馈验证并修正视觉预测；
7. 在证据不可靠时减速、重新观察或停机。

下一章会换一个问题：如果我们不只想让机器人“走过去”，还想让它像某段人类动作那样转身、下蹲或跳舞，怎样把人类动作翻译成机器人真正能够执行的参考？

---

## 练习

1. 为什么深度图中的一个像素不能直接当作落脚点？
2. 相机总延迟 `60 ms`、机器人速度 `0.6 m/s`，机器人在这段时间前进多少？
3. 为什么视觉落脚仍然需要足底接触和 IMU 反馈？
4. 怎样验证策略是否完全忽略了视觉？
5. 为什么 2.5D 高程图无法完整表示一张桌子？

参考答案：

1. 它只给相机视线上的距离，还没有完成坐标、时间、可达性、脚底大小和后续平衡判断；
2. `0.6×0.06=0.036 m`，也就是 `3.6 cm`；
3. 视觉只能预测接触，实际脚是否到达、踩实或滑动仍要由真实身体响应确认；
4. 遮挡、置零、打乱或移动视觉地形，再比较动作和成功率；
5. 同一个水平位置可能同时存在地面和桌面两个高度，而 2.5D 每格通常只保存一个高度。
