# 第 10 章：WBC 与 TSID——让全身任务在物理约束下同时成立

> **所属部分**：第二部分 · 传统模型控制。
>
> **前置知识**：[第 02 章](02-运动学与雅可比.md)、[第 03 章](03-动力学与接触.md)、[第 06 章](06-最优化控制.md)。
>
> **核心问题**：怎样把质心、躯干、手脚和接触任务统一变成可执行的关节力矩？
>
> **下一步**：[第 11 章](11-SRBD与凸MPC.md)和[第 12 章](12-质心动力学与NMPC.md)说明高层参考怎样预测出来。

WBC 是 Whole-Body Control，全身控制，用来协调质心、躯干、手脚等全身任务。TSID 是 Task-Space Inverse Dynamics，任务空间逆动力学，用逆动力学把这些任务落实成力矩。不同项目对术语边界略有差别，本章关注共同的优化结构。

![WBC 的输入、任务代价、物理约束和真实执行](picture/wbc_qp_flow.svg)

## 1. 为什么需要 WBC

人形一帧内可能同时要求：

- 支撑脚不滑；
- 质心跟踪参考；
- 躯干直立；
- 摆动脚跟踪落脚轨迹；
- 双手完成操作；
- 所有关节不超限；
- 力矩和接触力可实现。

逐个写独立 PD（Proportional-Derivative，比例—微分控制）会互相打架。WBC 在同一个决策空间里协调它们。

## 2. 决策变量

常见选择：

```text
z=
[
v_dot
τ
λ
]
```

- `v_dot∈ℝ^(6+n)`：广义加速度；
- `τ∈ℝ^n`：关节力矩；
- `λ`：所有接触力/wrench；
- `z`：QP（Quadratic Programming，二次规划）要求解的总向量；QP 在这里统一处理二次任务代价和线性约束。

有的实现会用动力学消元，不把所有变量都显式保留。结构不同，物理含义相同。

从闭环角度看，WBC 的输入是当前状态估计 `x_hat`、任务参考和接触估计；QP 输出的是一组当前时刻的候选广义加速度、接触力和关节力矩。真正下发给硬件的通常只有关节命令，真实机器人是否产生预测的加速度和接触力，要由下一周期的传感器反馈验证。

## 3. 最重要的等式约束

### 全身动力学

```text
Mv_dot+h
=
Sᵀτ
+J_cᵀλ
```

在当前控制周期，`M,h,J_c` 都由当前状态估计计算，可视作已知系数，因此对决策变量是线性的。

### 刚性接触

```text
J_c · v_dot
+ J_dot,c · v
=0
```

它限制支撑脚的加速度为零。

## 4. 任务如何写成加速度目标

例如末端位置误差：

```text
x_ddot,cmd
=
x_ddot,des
+ K_p · (x_des-x)
+ K_d · (x_dot,des-x_dot)
```

- `x_ddot,cmd`：带反馈修正的命令加速度；
- `des`：desired，期望；
- `K_p,K_d`：任务空间增益。

实际任务加速度：

```text
x_ddot
=
J · v_dot
+ J_dot · v
```

所以任务误差可写成线性残差：

```text
e_task
=
J · v_dot
+ J_dot · v
-x_ddot,cmd
```

加入代价：

```text
J_task
=
‖e_task‖_W²
=
e_taskᵀ · W · e_task
```

`W` 是任务权重矩阵。

## 5. 不等式约束

### 力矩限制

```text
τ_min
≤τ
≤τ_max
```

### 摩擦

```text
|λ_x|≤μλ_z,
|λ_y|≤μλ_z,
λ_z≥0
```

### 关节下一步限位

可用短时预测近似：

```text
q_next
≈
q
+q_dotΔt
+(1)/(2)q_ddotΔt²
```

再限制 `q_min ≤ q_next ≤ q_max`。

## 6. 加权任务与严格优先级

### 加权和

```text
J=
w_comJ_com
+w_torsoJ_torso
+w_swingJ_swing
+w_τ‖τ‖²
```

实现简单，但“权重 1000”不等于数学意义上的绝对优先。任务尺度和数值条件会影响结果。

### 层级 QP

HQP 是 Hierarchical Quadratic Programming，层级二次规划：

1. 先求最高优先级任务；
2. 在不破坏已得到最优值的空间内求下一任务；
3. 逐级向下。

它更符合“安全接触 > 平衡 > 手部精度 > 姿态好看”这类严格优先关系，但求解结构更复杂。

## 7. 一个简化 QP 结构

```go
// 根据当前状态估计计算动力学和接触运动学中的已知系数。
m, h := dynamics(q, v)
jc, dJcV := contactKinematics(q, v)

// QP 的未知量包括广义加速度、关节力矩和接触力。
decision := DecisionVariables{
	Acceleration: dv,
	Torque:       tau,
	ContactForce: contactForce,
}

constraints := []Constraint{
	wholeBodyDynamics(m, h, decision), // 必须满足全身动力学
	rigidContact(jc, dJcV, decision),  // 支撑脚不能穿地或滑动
	lowerTorqueLimit(torqueMin, tau),  // 关节力矩下限
	upperTorqueLimit(tau, torqueMax),  // 关节力矩上限
	frictionPyramid(contactForce),     // 接触力不能超过摩擦能力
}

// 各项代价分别衡量质心、躯干、摆动脚和力矩使用情况。
cost := weightedNorm(Sub(comTask(dv), comAccCmd), wCOM) +
	weightedNorm(Sub(torsoTask(dv), torsoAccCmd), wTorso) +
	weightedNorm(Sub(swingTask(dv), swingAccCmd), wSwing) +
	weightedNorm(tau, wTorque)
```

求解后只把 `τ` 发给电机，其余变量是帮助满足动力学的内部决策。

因此，QP 可行只说明在当前估计、模型和约束下存在数学解，不代表脚一定不滑或电机一定精确实现该力矩。应把预测接触力、期望加速度与随后测得的足底力、关节运动进行对照。

## 8. 为什么 WBC 能高频运行

它只优化当前时刻，不展开未来几十帧。当前状态估计确定后：

- 动力学对决策变量线性；
- 任务是线性残差的平方；
- 约束可线性化；
- 因而形成 QP。

高频不是自动保证的。自由度、接触数、任务数量、稀疏实现和求解器都会影响耗时。

## 9. 参考从哪里来

WBC 不是规划器。它需要上游提供：

- 质心参考；
- 摆动脚轨迹；
- 身体姿态；
- 接触计划；
- MPC（Model Predictive Control，模型预测控制）算出的期望接触力。

若参考轨迹运动学不可达或动力学太激进，WBC 只能妥协或报告不可行。

## 10. 常见调试指标

- 动力学等式残差；
- 支撑脚加速度/速度残差；
- 摩擦锥余量；
- 力矩饱和比例；
- 每个任务的归一化误差；
- QP 可行性与求解时间；
- 接触切换瞬间的命令跳变。

只看“机器人是否摔倒”会丢失大量定位信息。

## 11. 局限

- 单帧控制缺少长时预测；
- 对模型、接触状态和状态估计敏感；
- 多任务权重难调；
- 刚性接触假设在冲击时不准确；
- 参考生成仍需另一个复杂模块。

因此常用低频 MPC 规划未来接触力和状态，高频 WBC 落实为全身力矩。

## 本章小结

- WBC 把任务加速度、全身动力学、刚性接触、摩擦和执行器限制放进同一个当前时刻问题。
- 决策变量中的加速度和接触力是内部候选量，真正发给硬件的通常是关节命令，仍需下一周期反馈验证。
- WBC 擅长高频落实任务但缺少长时预测，因此常与高层 MPC、轨迹规划或策略组合。

## 12. 练习

1. 29 关节机器人只有双脚点接触、每脚 3 维力时，`λ` 是几维？
2. 为什么全身动力学在 WBC QP 中是线性约束？
3. 把“支撑脚不能动”写成加速度约束。
4. 为什么直接把任务权重从 10 改到 10000 可能让求解变差？

答案：1）6 维；2）当前状态确定后矩阵系数已知，决策变量线性出现；3）`J_c · v_dot + J_dot,c · v = 0`；4）可能造成数值尺度病态，也不能严格表达优先级。
