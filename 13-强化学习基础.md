# 第 13 章：强化学习基础——把控制律从在线求解搬到离线训练

> **所属部分**：第三部分 · 学习控制。
>
> **前置知识**：[第 03 章](03-动力学与接触.md)到[第 06 章](06-最优化控制.md)；概率和期望按需查[附录 A 第 6 节](附录A-数学急救包.md#6-概率估计和学习控制的共同语言)。
>
> **核心问题**：策略、价值和奖励怎样把大量搜索从部署时搬到训练时？
>
> **下一步**：[第 14 章：腿足强化学习工程](14-腿足强化学习工程.md)。

## 怎么读这一章

- **第一遍读第 1～5、12～13 节**：只理解训练与部署的区别、MDP 五要素、累计奖励、策略、价值，以及 RL 与控制闭环怎样对应。
- **第二遍读第 6～7 节**：再看 Bellman 递推和 Actor–Critic。
- **第三遍读第 8～11 节**：策略梯度、GAE、PPO 和 on-policy 是算法细节，第一次完全可以跳过。

第一遍不需要推导 PPO。只要能回答“策略看见什么、输出什么、奖励只在什么时候使用、真机反馈怎样回来”，就没有丢掉主线。

RL 是 Reinforcement Learning，强化学习。Deep RL（深度强化学习）通常只是指策略或价值函数由深度神经网络表示。

## 1. 为什么控制还需要强化学习

第 06 章介绍的 MPC（Model Predictive Control，模型预测控制）属于“运行时根据模型求答案”：每个控制周期拿到状态和目标，再求一次优化问题。传统控制路线中的 WBC（Whole-Body Control，全身控制）也会在线求解当前全身任务；如果你跳过了传统路线，只需先把它理解为“把任务和物理约束换算成关节命令的控制器”。这类方法的优点是约束和物理含义清楚，但复杂接触、模型误差和高维动作会让建模与在线求解变难。

强化学习换了一种计算分工：

```text
训练阶段：
反复试动作 → 观察后果 → 根据奖励调整策略

部署阶段：
当前观测 → 神经网络一次前向计算 → 当前动作
```

它仍然在解决“状态下该选什么动作”，只是把大量搜索放到离线训练中，再用网络近似结果。代价是约束不再天然严格满足，行为也更依赖奖励、训练分布和仿真质量。

“离线训练”不等于“部署时开环播放”。训练结束后固定的是网络参数；部署时策略仍处在在线反馈环中，每个周期接收由传感器或状态估计得到的新观测，再输出当前动作。RL（Reinforcement Learning，强化学习）改变的是控制律怎样得到，并没有绕过电机、接触、重力和真实反馈。

![强化学习的训练参数更新与部署在线反馈是两个不同过程](picture/rl_training_deployment.svg)

理解本章时，可以始终把 RL 和第 06 章对应起来：

- 环境相当于动力学；
- 奖励相当于负的代价；
- 策略相当于控制律；
- 训练相当于寻找一组好用的控制器参数。

## 2. MDP

为了让“试动作、看后果”可以被数学描述，首先需要规定：环境里有哪些状态、控制器能做哪些动作、动作会带来什么后果、怎样评价结果。MDP 就是这份问题定义，不是某一种神经网络。

MDP 是 Markov Decision Process，马尔可夫决策过程，由：

```text
(𝒮,𝒜,P,r,γ)
```

构成：

- `𝒮`：state space，状态空间；
- `𝒜`：action space，动作空间；
- `P(s_(t+1)|s_t,a_t)`：状态转移概率；
- `r(s_t,a_t)`：即时奖励；
- `γ∈[0,1)`：折扣因子；
- Markov 性：给定当前状态和动作后，未来不再依赖更早历史。

`P(s_(t+1)|s_t,a_t)` 可以用人话读成：“已经知道当前状态 `s_t` 和动作 `a_t` 时，下一状态成为 `s_(t+1)` 的可能性。”竖线 `|` 表示“在右侧条件已经给定的情况下”。这里的 `P` 是概率规律，不是第 05 章协方差矩阵 `P`。

腿足策略的观测往往不是真正完整状态，所以严格来说更接近 POMDP（Partially Observable Markov Decision Process，部分可观测马尔可夫决策过程）。堆叠历史、RNN（Recurrent Neural Network，循环神经网络）、GRU（Gated Recurrent Unit，门控循环单元）和状态估计器都在利用历史补偿看不见的状态。

## 3. 回报

```text
G_t=
Σ_(k=0)^(∞)γ^k r_(t+k)
```

- `G_t`：从时刻 `t` 开始的累计折扣回报；
- `r_(t+k)`：未来第 `k` 步奖励；
- `γ^k`：越远奖励权重越小。
- `Σ_(k=0)^(∞)`：从当前奖励开始，把所有未来奖励按折扣相加；有限回合在终止时停止，不是真的必须运行到无限远。

若 `γ=0.99`、控制频率 50 Hz，一秒后的奖励权重约为：

```text
0.99^(50)≈0.605
```

因此 `γ` 的时间意义和控制频率绑定。不同频率直接复制 `γ` 会改变有效规划长度。

## 4. 策略

确定性策略：

```text
a_t=μ_θ(o_t)
```

随机策略：

```text
a_t∼π_θ(·|o_t)
```

- `o_t`：observation，策略可见的观测；
- `a_t`：动作；
- `θ`：神经网络参数；
- `μ_θ(o_t)`：确定性策略在观测 `o_t` 下直接给出的动作；
- `π_θ(·|o_t)`：给定观测以后，对所有可能动作定义的概率分布；圆点 `·` 代表尚未指定的动作；
- `∼`：从右侧概率分布中随机采样。

连续控制常输出高斯分布均值 `μ` 和标准差 `σ`：

```text
a∼𝒩
(μ_θ(o),
diag(σ²))
```

- `𝒩`：高斯分布；
- `μ_θ(o)`：动作分布均值；
- `σ`：每个动作维度的标准差，决定探索有多分散；
- `diag(σ²)`：把各维方差 `σ²` 放到协方差矩阵对角线上，这个简化表示暂不建模动作维度间的相关性。

训练时采样提供探索；部署时通常使用均值动作。这里的 `μ` 是分布均值，不是第 03 章摩擦系数，必须看上下文。

## 5. Value、Q 和 Advantage

先用“机器人准备迈步”区分三个量。机器人当前身体前倾，这是状态 `s`：

- `V(s)`：从这个状态出发，按照当前策略继续走，平均能获得多少未来奖励；
- `Q(s,a)`：在这个状态先指定执行动作 `a`，之后再按当前策略走，平均能获得多少未来奖励；
- `A(s,a)`：指定动作 `a` 比当前策略在该状态下的平均选择好多少。

所以 Value 评价“这个处境”，Q 评价“这个处境下的某个动作”，Advantage 比较“这个动作相对平常选择是否更好”。它们都预测未来累计回报，不是传感器直接测得的物理量。

状态价值：

```text
V^π(s)
=
𝔼_π[G_t|s_t=s]
```

表示从状态 `s` 出发，之后按策略 `π` 行动的平均回报。

其中 `𝔼_π[...]` 表示把策略采样和环境随机性造成的许多可能结果取平均；竖线右侧仍表示计算这个平均时已经给定的条件。

动作价值：

```text
Q^π(s,a)
=
𝔼_π[G_t|
s_t=s,a_t=a]
```

优势：

```text
A^π(s,a)
=
Q^π(s,a)-V^π(s)
```

它回答：在这个状态下，所选动作比策略平均动作好多少。

## 6. Bellman 递推

```text
V^π(s_t)
=
𝔼[
r_t+γ V^π(s_(t+1))
]
```

这条 Bellman 期望关系表达当前策略下价值函数的自洽性：从现在开始的长期回报，等于当前奖励加上折扣后的下一状态价值。它在这里讨论的是固定策略 `π` 的平均结果，还没有声称每一步都是全局最优动作。

不用先理解“自洽性”这个词也能使用它。假设机器人迈一步立刻得到奖励 `1`，下一状态的预计未来价值是 `10`，折扣因子 `γ=0.9`，那么当前状态价值的一个估计是：

```text
1 + 0.9×10 = 10
```

Critic 不必等整段行走彻底结束才学习。它可以用“刚得到的奖励 + 自己对下一状态的估计”来修正对当前状态的判断。用一个估计去更新另一个估计叫 bootstrap（自举）；它提高学习效率，也会把 Critic 自身的错误传播回来。

## 7. Actor–Critic

- Actor：策略网络，决定动作；
- Critic：价值网络，估计状态有多好；
- Critic 提供低方差的学习信号；
- Actor 根据优势调整动作概率。

TD error（Temporal-Difference error，时序差分误差）：

```text
δ_t
=
r_t+γ V_φ(s_(t+1))
-V_φ(s_t)
```

- `φ`：Critic 参数；
- `δ_t>0`：这一步结果比 Critic 原先预期好；
- `δ_t<0`：比预期差。

## 8. 策略梯度

核心形式。为避免把训练目标和第 02 章雅可比矩阵都记成 `J`，这里把策略目标写成 `Objective(θ)`：

```text
∇_θ Objective(θ)
≈
𝔼
[
∇_θ logπ_θ(a_t|s_t)
A_hat_t
]
```

- `∇_θ`：对参数 `θ` 求梯度；
- `Objective(θ)`：希望策略参数最大化的期望回报目标；
- `logπ_θ(a_t|s_t)`：当前动作的对数概率；
- `A_hat_t`：优势估计；帽子表示它是从采样数据和 Critic 得到的估计，不是真值；
- 若 `A_hat_t>0`，提高该动作概率；若小于 0，降低概率。

这里为了写标准 MDP 公式使用完整状态 `s_t`。若 Actor 实际只能看观测 `o_t`，策略概率项应按它真正获得的输入写成 `π_θ(a_t|o_t)`，不能在部署代码中偷偷换成仿真真值状态。

RL 不需要显式动力学导数，是因为通过采样回报和概率恒等式估计梯度。它不是“没有模型就不优化”，而是用环境采样替代显式模型传播。

## 9. GAE

GAE 是 Generalized Advantage Estimation，广义优势估计：

```text
A_hat_t^GAE
=
Σ_(l=0)^(∞)
(γλ)^lδ_(t+l)
```

- `λ∈[0,1]`：GAE 参数；
- `λ` 小：更依赖短期 Critic，方差低、偏差可能大；
- `λ` 大：使用更长期回报，偏差低、方差更大。

这里的 `λ` 与接触力常用符号相同，含义完全不同。

## 10. PPO

PPO 是 Proximal Policy Optimization，近端策略优化。前文 `r_t` 已经表示即时奖励，因此这里不再沿用论文中常见的同名字母，而把新旧策略概率比明确写成 `ratio_t(θ)`：

```text
ratio_t(θ)
=
(π_θ(a_t|s_t))/(π_(θ_old)(a_t|s_t))
```

- `θ_old`：采集当前这批数据时使用的旧策略参数；
- `ratio_t(θ)`：新策略与旧策略对已采样动作给出的概率之比；
- 若 `ratio_t=1`，新旧策略对该动作概率相同。

裁剪目标：

```text
L_clip =
平均值 {
    min(
        ratio_t · A_hat_t,
        clip(ratio_t, 1-ε, 1+ε) · A_hat_t
    )
}
```

- `ε`：裁剪范围，例如 0.2；
- `clip`：把概率比限制到 `[1-ε,1+ε]`；
- `min`：对有利方向采用保守收益，抑制一步改得太猛。

PPO 并没有直接裁剪神经网络参数，而是限制新旧策略在已采样动作上的概率变化收益。

总损失常写：

```text
L
=
-L_clip
+ c_v · L_value
- c_e · H(π)
```

- `L_value`：Critic 价值回归损失；
- `H(π)`：策略熵，越大表示探索越分散；
- `c_v,c_e`：权重；
- 前面负号是因为优化器通常做最小化，而策略目标希望最大化。

## 11. On-policy

策略在训练中不断变化。昨天的策略可能经常小步慢走，今天的策略已经学会大步跑；两者访问的状态和动作分布不同。

PPO 是 on-policy（同策略）算法：一批数据由当前策略采集后，用它做有限轮更新，随后通常丢弃并用新策略重新采集。原因是 PPO 的概率比和优势估计都假设数据与当前策略足够接近。反复使用很久以前的数据，会让这种假设越来越不可靠。

因此 on-policy 不是“只能用一次数据”的绝对字面规则，而是要求采样策略与正在更新的策略足够接近。它的旧数据很快过期，样本效率不高，但大量并行仿真可以提高采样吞吐量。

Off-policy（异策略）算法允许当前策略学习其他策略过去采集的数据，因而可以重复使用 replay buffer（经验回放池）。代价是需要处理“数据来自谁”和“当前策略想学什么”之间的分布差异，训练稳定性更复杂。

## 12. RL 与 OCP 的对应

| OCP | RL |
|---|---|
| 模型状态 `x` | 环境状态 `s`；策略通常只见观测 `o` |
| 控制 `u` | action |
| 动力学 `f` | 环境 `step()` |
| 代价 `ℓ` | 负奖励 `-r` |
| 控制律 | policy |
| 价值函数 | value/Q |
| 在线求解轨迹 | 训练后前向推理 |

强化学习部署快，是因为昂贵优化发生在训练阶段，策略网络近似了对许多状态的解。

但这张对应表里还有一个关键差别：训练环境的 `step()` 是 `f_sim`，部署面对的是 `f_real`；策略输入通常是观测 `o`，而不是完整真实状态。奖励只在训练时表达设计者的偏好，它不是传感器，也不是物理真理。部署阶段即使没有在线奖励计算，真实闭环仍会继续运行。

## 13. 常见误解

- “奖励高”不等于行为正确：可能钻奖励漏洞；
- “仿真并行多”不等于数据独立：环境可能高度相关；
- “PPO 稳定”不等于超参数不重要；
- “model-free”不等于不依赖物理模型：仿真器就是隐式模型；
- Critic 训练时可用特权状态，不代表 Actor 部署时也能用。

## 本章小结

- 强化学习把大量动作搜索放到训练阶段，再用策略网络在部署时根据当前观测快速输出动作；部署仍是在线反馈。
- MDP 定义问题，策略选择动作，Value/Q/Advantage 评价未来回报，PPO 只是更新策略的一种算法。
- 奖励、仿真器和观测共同决定策略学到什么；model-free 不等于摆脱模型、状态估计或真实物理约束。

## 14. 练习

1. 50 Hz 下 `γ=0.99`，为什么不能简单理解成“只看未来 100 步”？
2. Advantage 为正时，策略应提高还是降低已采样动作概率？
3. PPO 裁剪的是什么？
4. POMDP 下为什么历史观测有帮助？

答案：1）折扣是连续衰减而非硬窗口；2）提高；3）新旧策略的动作概率比对目标的影响；4）历史可以推断单帧观测中看不见的速度、延迟、地形和扰动。
