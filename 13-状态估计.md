# 第 13 章：状态估计——传感器给的是证据，不是真相

控制器需要基座位置、速度、姿态、关节状态和接触状态，但真机无法直接、无噪声地得到全部信息。Estimator（估计器）要把不同传感器的证据融合成内部状态。

全书统一使用下面的关系：

```text
x_real
  → y = h_real(x_real) + noise
  → estimator(y, u, f_model)
  → x_hat
  → controller
```

- `x_real`：真实状态，真机上通常无法完整直接读取；
- `y`：传感器测量；
- `h_real`：现实中的测量过程；
- `f_model`：估计器内部用于预测的模型；
- `x_hat`：估计器对真实状态的当前判断；
- 控制器实际使用的是 `x_hat`，不是上帝视角的 `x_real`。

![模型预测与多种传感器证据如何融合成状态估计](picture/state_estimation_fusion.svg)

## 1. 有哪些传感器

- encoder：编码器，测关节位置/速度；
- IMU：Inertial Measurement Unit，惯性测量单元，通常含陀螺仪和加速度计；
- force/torque sensor：力/力矩传感器；
- motor current：电机电流，可间接估计力矩；
- camera/depth/LiDAR：相机、深度相机、激光雷达；
- mocap：motion capture，外部动作捕捉系统，实验室内可提供高精度位姿。

IMU 不直接给世界坐标速度。加速度计测到的是 specific force（比力），需要结合姿态去除重力，再积分；微小偏差会不断积累。

## 2. 预测与观测更新

卡尔曼滤波（Kalman Filter，KF）的心智模型：

1. 用模型预测现在应该在哪里；
2. 用传感器观测纠正预测；
3. 根据双方不确定性决定更信谁。

线性系统：

```text
x_k
=
Ax_(k-1)
+Bu_(k-1)
+w_(k-1)
```

观测：

```text
z_k
=
Hx_k
+v_k
```

- `x_k`：待估计状态；
- `u_(k-1)`：已知输入；
- `z_k`：传感器观测；
- `w`：过程噪声；
- `v`：观测噪声；
- `A`：状态转移；
- `H`：从状态映射到传感器量。

注意这里的 `v_k` 是观测噪声，不是速度；符号含义依上下文。

状态估计也有信息论上的边界：如果两个不同真实状态在已有传感器和一段历史中产生几乎相同的测量，估计器就无法可靠区分它们。这叫不可观或弱可观。缺失的信息不能只靠“把滤波参数调得更好”凭空恢复，需要增加传感器、主动产生可辨识运动，或接受更大的不确定性。

## 3. 协方差

```text
P
=
𝔼[
(x_real-x_hat)
(x_real-x_hat)ᵀ
]
```

- `x_hat`：状态估计；
- `x_real`：真实状态；
- `P`：估计误差协方差；
- 对角线：每个状态的不确定性；
- 非对角线：不同状态误差的相关性。

过程噪声协方差 `Q` 表示模型有多不可信；观测噪声协方差 `R` 表示传感器有多不可信。不要和 OCP（Optimal Control Problem，最优控制问题）的权重矩阵 `Q,R` 混淆。

## 4. KF 核心步骤

预测：

```text
x_hat^-_k
=
Ax_hat_(k-1)
+Bu_(k-1)
```

```text
P^-_k
=
AP_(k-1)Aᵀ
+Q
```

上标 `-` 表示观测更新前的先验估计。

创新：

```text
y_k
=
z_k-Hx_hat^-_k
```

卡尔曼增益：

```text
K_k
=
P^-_kHᵀ
(HP^-_kHᵀ+R)⁻¹
```

更新：

```text
x_hat_k
=
x_hat^-_k+K_ky_k
```

- `y_k`：innovation，创新/观测残差；
- `K_k`：卡尔曼增益；
- `R` 大表示传感器噪声大，更新时更信模型；
- `P^-` 大表示预测不确定，更愿意接受观测修正。

## 5. 足端里程计

若支撑脚相对世界静止：

```text
v_foot^W = 0
```

脚速度由基座速度和关节速度共同决定。利用接触雅可比，可以反推出基座速度。这是 leg odometry（腿式里程计/足端里程计）。

问题：

- 脚会滑；
- 接触判断会错；
- 关节速度噪声大；
- 冲击期间刚性约束不成立；
- 雅可比接近奇异。

因此它适合作为有条件的观测，不应当成绝对真值。

## 6. IMU 与足端互补

- IMU 短期变化响应快，但积分会漂移；
- 支撑脚提供“脚相对世界应静止”的速度约束，长期不漂，但接触时噪声和异常大。

滤波器结合两者：

- 短期主要跟随 IMU；
- 接触可靠时，用足端约束校正漂移；
- 检测到滑动或摆动时，增大足端观测噪声或禁用它。

## 7. EKF

EKF 是 Extended Kalman Filter，扩展卡尔曼滤波。非线性模型：

```text
x_k=f_model(x_(k-1),u_(k-1))+w
```

```text
z_k=h_model(x_k)+v
```

- `f_model`：用于预测状态演化的内部模型；
- `h_model`：用于预测某个状态应产生什么测量的内部模型。

在当前估计附近计算雅可比：

```text
F_k=(∂ f)/(∂x),
H_k=(∂ h)/(∂x)
```

再用线性 KF 类似方式更新。姿态估计还需谨慎处理 `SO(3)`（三维旋转群，可以先理解为所有合法三维姿态的集合）上的误差，常见 error-state EKF（误差状态扩展卡尔曼滤波）。

## 8. 接触估计

可融合：

- 足底法向力；
- 关节力矩反推接触力；
- 足端高度/速度；
- 计划接触相位；
- 概率状态机。

不要只用一个硬阈值。接触状态错误会同时污染估计器和控制器，是典型的正反馈故障：估计错 → 力矩错 → 接触更异常 → 估计更错。

## 9. 一致性与创新监控

滤波器“有输出”不代表正确。应监控：

- 创新 `y_k` 的大小；
- 归一化创新是否长期过大；
- 协方差是否异常缩小或爆炸；
- 估计速度与外部 mocap 的离线对比；
- 静止时速度均值；
- 接触切换时的跳变。

若滤波器过度自信，协方差很小却持续估错，控制器会比“承认不知道”更危险。

## 10. 练习

1. 为什么加速度计积分能得到速度，却不能长期可靠？
2. 足端已经打滑时，还把它当作零速度观测会怎样？
3. `R` 增大时，KF 更信模型还是观测？
4. EKF 与 KF 的核心关系是什么？

答案：1）偏置和噪声积分累积；2）会把错误约束注入基座速度/位置估计；3）更信模型；4）EKF 在当前估计附近把非线性模型一阶线性化后使用 KF 更新。
