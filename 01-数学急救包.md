# 第 01 章：数学急救包——只补机器人真正要用的部分

这章不试图重讲高等数学。目标是让你看到后文公式时，知道它在表达什么、维度是否合理、怎样翻译成代码。

先建立一个边界：数学符号是描述物理世界的语言，不是物理世界本身。把位置从世界坐标系改写到身体坐标系，数字会变化，但机器人没有因此移动；用更复杂的模型预测运动，也不会自动让真实机器人更准确。后文遇到公式时，要同时问“它描述了哪个物理对象”和“它只是怎样表示这个对象”。

## 1. 标量、向量和矩阵

### 1.1 标量

标量是一个数，例如质量：

```text
m=50 kg
```

`m` 表示质量，数值是 50，单位是千克（kg）。

### 1.2 向量

向量是一组有顺序的数，例如三维位置：

```text
p=
[
1.0
0.2
0.8
] m
```

符号解释：

- `p`：位置向量；
- 三个分量依次是 `x`、`y`、`z` 方向的位置；
- `p∈ℝ³`：表示它由 3 个实数组成；
- 在 Go 中可用 `[3]float64` 表示固定三维向量，或用 `[]float64` 表示运行时长度的向量。

向量不是“带括号的三个数”这么简单；它还必须属于某个坐标系。`[1, 0, 0]` 在世界坐标系和机器人身体坐标系中可能指向不同方向。

向量的价值也不只是把数排在一起，而是把“同一时刻需要共同处理的一组物理量”组成一个整体。例如状态向量把位置、姿态和速度放在一起，使控制器可以一次计算它们的联合变化。

### 1.3 矩阵

矩阵可以看成一个线性映射：

```text
y=Ax
```

若 `A∈ℝ^(m× n)`、`x∈ℝ^n`，则 `y∈ℝ^m`。

- `n`：输入维度；
- `m`：输出维度；
- `ℝ^(m× n)`：`m` 行 `n` 列的实数矩阵。

这和函数签名很像：

```go
func linearMap(A Matrix, x Vector) Vector {
	// 矩阵 A 把 n 维输入映射成 m 维输出。
	return MatVecMul(A, x)
}
```

维度检查是读公式时最有用的“类型系统”。若两个向量维度不同却直接相加，公式一定缺了映射或定义。

矩阵通常描述多个输入如何共同影响多个输出。例如雅可比矩阵的第 `i` 列表示“只让第 `i` 个关节产生单位速度时，末端速度会怎样变化”。因此矩阵不是一张随意排列的数字表，而是一个局部因果关系的编码。

## 2. 点积、范数和叉积

### 2.1 点积：两个方向有多一致

```text
aᵀb
=a_xb_x+a_yb_y+a_zb_z
```

符号解释：

- `a`、`b`：两个三维向量；
- `a_x`：`a` 在 `x` 方向的分量；
- 上标 `T`：转置；
- 结果是标量。

若两向量都是单位向量，点积等于夹角余弦。结果为 1 表示同向，0 表示垂直，-1 表示反向。

### 2.2 范数：向量有多长

```text
‖a‖_2=√(a_x²+a_y²+a_z²)
```

`‖·‖_2` 是二范数，也就是欧氏长度。后文常见的平方误差：

```text
‖x-x_ref‖_2²
```

其中 `x_ref` 是参考状态，`ref` 是 reference（参考）的缩写。这个式子表示当前状态与目标状态各分量误差的平方和。

### 2.3 叉积：产生一个垂直方向

```text
τ=r×f
```

- `τ`：力矩向量，单位牛顿米（N·m）；
- `r`：从旋转中心指向受力点的位置向量，单位米（m）；
- `f`：力向量，单位牛顿（N）；
- `×`：叉积。

例子：门把手距门轴 0.8 m，垂直推力 10 N，则力矩大小为 `0.8×10=8` N·m。相同的力作用在门轴附近几乎打不开门。

## 3. 导数：变化有多快

位置 `p(t)` 是时间 `t` 的函数。速度定义为位置对时间的导数：

```text
v(t) = p_dot(t) = dp(t)/dt
```

- `t`：时间，单位秒（s）；
- `p(t)`：位置；
- `v(t)`：速度；
- `p_dot`：`p` 对时间的一阶导数；论文通常写成 `p` 上方一个点；
- `d`：这里表示“无穷小变化”，不需要把它当变量。

加速度是速度的导数，也是位置的二阶导数：

```text
a(t) = v_dot(t) = p_ddot(t)
```

若位置每秒增加 2 m，速度是 2 m/s；若速度每秒增加 3 m/s，加速度是 3 m/s²。

### 偏导数

函数有多个输入时，偏导表示只改变一个输入：

```text
f(x,u)=x²+3u,
(∂ f)/(∂ x)=2x,
(∂ f)/(∂ u)=3
```

`∂` 表示偏导。控制中常在当前点附近求偏导，把非线性函数近似成线性函数。

## 4. 积分：把许多小变化累加

速度累积成位置：

```text
p(t) = p(0) + ∫[0→t] v(τ) dτ
```

- `p(0)`：初始位置；
- `∫`：积分符号；
- `τ`：积分内部使用的临时时间变量，与力矩常用的 `τ` 字形相同，含义由上下文决定；
- `dτ`：对时间累加。

代码里通常是离散近似：

```go
// 用当前速度在一个很短的时间步内近似更新位置。
pNext := pNow + vNow*dt
```

对应公式：

```text
p_(k+1)=p_k+v_kΔt
```

`Δt` 是采样周期。这个方法叫前向欧拉积分（Forward Euler）。如果加速度或接触力变化很快，`Δt` 太大就会产生明显误差，甚至数值发散。

## 5. 线性化：在当前位置用切线代替曲线

对非线性函数 `f(x)`，在 `x_0` 附近：

```text
f(x)≈ f(x_0)+f'(x_0)(x-x_0)
```

- `x_0`：展开点，通常是当前状态；
- `f'(x_0)`：函数在 `x_0` 处的斜率；
- `≈`：近似相等。

多维系统写成：

```text
δx_(k+1)
≈
A_kδx_k+
B_kδu_k
```

- `δx_k=x_k-x_bar_k`：状态相对参考轨迹的微小偏差；
- `δu_k=u_k-u_bar_k`：输入偏差；
- `A_k=(∂ f)/(∂x)`：动力学对状态的雅可比矩阵；
- `B_k=(∂ f)/(∂u)`：动力学对输入的雅可比矩阵；
- 横线 `x_bar`：名义值或参考值。

线性化不是说世界是线性的，而是说控制周期足够短时，当前点附近的曲线可以暂时用切线近似。

## 6. 概率：估计和学习控制的共同语言

高斯分布常写成：

```text
x∼𝒩(μ,σ²)
```

- `∼`：服从某种分布；
- `𝒩`：正态（高斯）分布；
- `μ`：均值，代表中心；
- `σ`：标准差，代表波动尺度；
- `σ²`：方差。

状态估计中，方差表示对传感器有多不信任；强化学习中，策略可输出动作分布的均值和标准差，再从分布采样动作。

### 期望

```text
𝔼[X]
```

表示随机变量 `X` 的平均值。它不是一次实验的结果，而是大量重复采样后的平均趋势。

## 7. 最优化符号

```text
min_u J(u)
s.t.
g(u)=0,
h(u)≤0
```

- `min_u`：在所有可选 `u` 中找最小值；
- `J`：代价函数（cost function），也常叫损失函数（loss）；
- `s.t.`：subject to，表示“满足以下约束”；
- `g=0`：等式约束；
- `h≤0`：不等式约束。

软件类比：

```go
var best Control
bestCost := math.Inf(1)

for _, u := range candidates {
	// 先过滤不满足等式和不等式约束的候选控制量。
	if !equalityOK(u) || !inequalityOK(u) {
		continue
	}

	// 在所有可行候选中保留代价最小的一个。
	if c := cost(u); c < bestCost {
		best = u
		bestCost = c
	}
}
```

## 8. 二次型

控制中最常见的代价：

```text
J=eᵀQe
```

- `e`：误差向量；
- `Q`：权重矩阵；
- `eᵀQe`：最终是一个标量。

若 `Q=diag(10,1)`，第一个误差的惩罚权重是第二个的 10 倍。`diag` 是 diagonal（对角矩阵）的缩写。

例子：

```text
e=
[0.2
0.5],
Q=
[10  0
0  1]
```

则 `J=10×0.2²+1×0.5²=0.65`。

## 9. 单位是隐藏的类型系统

读公式时要做单位检查：

```text
F=ma
```

- `m` 的单位是 kg；
- `a` 的单位是 m/s²；
- `F` 的单位应为 kg·m/s²，也就是 N。

位置不能直接与速度相加；若公式里出现，必须有带时间单位的系数把二者转换到一致量纲。权重矩阵常常也隐含单位转换，所以不同损失项的权重不能只凭数值大小比较。

## 10. 你现在不必掌握的内容

第一次阅读可以暂时跳过：

- 严格的极限定义；
- 手算矩阵求逆和特征值；
- 李群李代数的完整证明；
- 变分法和 Hamilton–Jacobi–Bellman 方程；
- 策略梯度的严格测度论推导。

需要掌握的是直觉、输入输出、维度、单位、假设和失败边界。

## 11. 小练习

1. `A` 是 `6×12` 矩阵，`x` 是 12 维向量，`Ax` 是几维？
2. 机器人以 0.5 m/s 匀速前进，控制周期 0.02 s，一步前进多少米？
3. 写出关节位置误差 `q-q_ref` 的加权平方代价。
4. 如果 IMU（Inertial Measurement Unit，惯性测量单元）给出的加速度存在恒定的 0.01 m/s² 偏差，积分 100 秒后，速度误差约是多少？IMU 是机器人测量加速度和角速度的传感器。

答案：1）6 维；2）0.01 m；3）`(q-q_ref)ᵀQ(q-q_ref)`；4）约 1 m/s。
