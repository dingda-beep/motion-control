# 关节 PD 增益从零推导：为什么简单公式能用于 RL 真机

> **适合谁**：已经知道 PD 大概是“位置误差拉回+速度阻尼刹车”，但不明白 `K_p` 和 `K_d` 为什么可以算出来的读者。
>
> **这篇只解决四个问题**：
>
> 1. `J q_ddot=τ` 怎样与 PD 组成二阶系统？
> 2. `K_p=Jω_n²`、`K_d=2ζJω_n` 究竟是怎样一步步推出的？
> 3. `J`、`ω_n`、`ζ` 各自是什么，数值可以从哪里来？
> 4. 真实人形明明多关节耦合、有接触和延迟，为什么用这组 PD 训练的 RL 仍能部署真机？
>
> **主线位置**：读到[04-反馈控制基础](04-反馈控制基础.md)的 PD 增益设计时，可以转入本文；关心学习控制的读者，读完第 14～18 节后回到[14-腿足强化学习工程](14-腿足强化学习工程.md)。

本文不要求你会微分方程。我们会把每一次换元、求导和系数对齐都写出来，并先解释它在问什么。

本文反复使用两个缩写：

- PD（Proportional-Derivative，比例—微分）：用位置误差和速度误差产生纠正作用的反馈控制；
- RL（Reinforcement Learning，强化学习）：策略通过反复行动、观察后果和奖励，逐渐学会选择动作。

---

## 1. 先把结论放在地图上

整条推导其实只有一条主线：

```text
力矩使关节产生加速度
→ PD 根据位置和速度误差产生力矩
→ 把 PD 力矩代回关节动力学
→ 得到“惯量+弹簧+减震器”的二阶运动
→ 用希望的快慢ω_n和阻尼程度ζ反算 K_p/K_d
```

最后得到：

```text
K_p = J ω_n²
K_d = 2 ζ J ω_n
```

这两条公式的正确定位是：

> 根据一个简化但有物理意义的关节模型，算出一组合理的底层增益设计值。

它不是在声称：

> 整台人形机器人已经被精确简化成了许多个彼此独立的单关节。

这个区别会在后半篇解开“简化公式为什么仍能用于 RL 真机”的疑问。

---

## 2. 只研究一个转动关节

想象一根连杆被一个电机带动：

```text
                    连杆和负载
关节轴 ●────────────●
       ↻ q
       ↻ τ
```

我们先定义符号：

- `q`：关节实际角度，单位 `rad`；
- `q_dot`：关节角速度，单位 `rad/s`；
- `q_ddot`：关节角加速度，单位 `rad/s²`；
- `τ`：读作 tau，电机在关节上施加的力矩，单位 `N·m`；
- `J`：这个关节当前感受到的等效转动惯量，单位 `kg·m²`。

> **注意字母复用**：本文的 `J` 是一个表示转动惯量的数，不是第 02 章中表示速度映射的雅可比矩阵。不同领域惯例使用了同一个字母，必须根据当前定义区分。

### 2.1 等效惯量 J 到底在说什么

对直线运动，我们有：

```text
力 = 质量 × 加速度
```

对转动，对应关系是：

```text
力矩 = 转动惯量 × 角加速度
```

因此写成：

```text
J q_ddot = τ
```

它表示：

```text
同样的力矩下：
J 大 → 难以加速
J 小 → 容易加速
```

`J` 不只是电机转子自己的惯量。电机经过减速器带动连杆、手臂或小腿时，这些被带动的质量都会影响电机“感觉起来有多难加速”。

### 2.2 这个模型暂时忽略了什么

`J q_ddot=τ` 为了让我们先看清增益与关节响应的骨架，暂时忽略了：

- 重力；
- 摩擦；
- 其他关节的牵连；
- 脚底和地面的接触；
- 电机的力矩上限；
- 传感器、通信和电机响应延迟；
- 减速器、连杆和鞋底的弹性。

暂时忽略它们不是说它们不重要，而是先问一个更小的问题：

> 如果关节的基本难加速程度可以用 `J` 表示，PD 怎样决定它追目标的快慢与振荡？

---

## 3. PD 先把关节变成虚拟弹簧和减震器

希望关节停在目标角度 `q_des`。本文统一定义误差：

```text
e = q_des-q
```

- `e>0`：实际角度还比目标小，需要向正方向纠正；
- `e<0`：实际角度已经超过目标，需要向负方向拉回；
- `e=0`：位置正好到达目标，但关节仍可能带着速度冲过去。

完整 PD 写成：

```text
τ = K_p(q_des-q) + K_d(q_dot,des-q_dot)
```

其中：

- `q_dot,des`：期望关节速度；
- `K_p`：位置增益，决定位置误差产生多少力矩；
- `K_d`：速度增益，决定相对目标的运动速度产生多少阻尼力矩。

如果目标暂时不动：

```text
q_dot,des = 0
```

则：

```text
τ = K_p(q_des-q)-K_d q_dot
```

它可以读成：

```text
K_p(q_des-q)  → 虚拟弹簧，把关节拉向目标
-K_d q_dot    → 虚拟减震器，抵抗当前运动
```

`K_p` 和 `K_d` 都取正数时，它们的方向都倾向于缩小误差或制动运动。如果关节、编码器或力矩的正负方向接反，这个前提会被破坏；那不是调大或调小增益能修好的问题。

---

## 4. 第一次关键代入：得到误差的运动方程

现在把两个已知关系放在一起。

关节动力学：

```text
J q_ddot = τ
```

PD 力矩：

```text
τ = K_p(q_des-q)-K_d q_dot
```

把第二行的 `τ` 放进第一行：

```text
J q_ddot = K_p(q_des-q)-K_d q_dot
```

这一步没有添加新规律，只是说：

> 关节实际拿到的力矩，正是 PD 根据当前误差算出的力矩。

### 4.1 为什么要换成误差 e

我们最关心的不是 `q` 本身，而是它与目标还差多少：

```text
e = q_des-q
```

暂时假设 `q_des` 是一个不动的常数。对它观察一次时间变化：

```text
e_dot
= q_dot,des-q_dot
= 0-q_dot
= -q_dot
```

再观察一次时间变化：

```text
e_ddot
= q_ddot,des-q_ddot
= 0-q_ddot
= -q_ddot
```

所以：

```text
q_dot  = -e_dot
q_ddot = -e_ddot
```

把它们代入：

```text
J q_ddot = K_p e-K_d q_dot
```

得到：

```text
-J e_ddot = K_p e+K_d e_dot
```

把右边全部移到左边，再整体乘以 `-1`：

```text
J e_ddot + K_d e_dot + K_p e = 0
```

这就是第一个最重要的结果。

### 4.2 这行式子在说什么

```text
J e_ddot + K_d e_dot + K_p e = 0
```

可以按物理读成：

```text
惯量造成的运动趋势
+ 减震器造成的阻尼作用
+ 弹簧造成的拉回作用
= 当前没有其他外部作用
```

它和一个质量块连着弹簧与减震器的运动完全同形：

```text
质量 m      ↔ 关节惯量 J
弹簧刚度 k ↔ 位置增益 K_p
阻尼系数 c ↔ 速度增益 K_d
```

因此 PD 不只是在算两个误差；它在闭环中给关节制造出了一种虚拟的力学反应。这就是关节 PD 常与**关节阻抗**联系在一起的原因。

---

## 5. 为什么它叫二阶系统

一个方程中，误差最高出现到了二次时间变化 `e_ddot`，所以它叫**二阶系统**。

```text
e        → 误差现在有多大
e_dot    → 误差正在怎样变化
e_ddot   → 误差的变化速度又在怎样变化
```

“二阶”不表示这个方法比“一阶”更高级，只是说方程最高需要描述到加速度。

两边除以 `J`：

```text
e_ddot + (K_d/J)e_dot + (K_p/J)e = 0
```

这时可以看到，真正决定误差响应的不是孤立的 `K_p`、`K_d`，而是：

```text
K_p/J
K_d/J
```

同样的 `K_p/K_d` 放到一个惯量更大的关节上，实际反应就会更慢。这已经在暗示：增益应该和 `J` 一起设计。

---

## 6. 先只看快慢：自然角频率 ω_n 怎样出现

先把减震器拿掉，也就是暂时令：

```text
K_d = 0
```

误差方程变成：

```text
J e_ddot + K_p e = 0
```

两边除以 `J`：

```text
e_ddot + (K_p/J)e = 0
```

这就是理想的无阻尼弹簧振子。它会在目标两边不断摆动。控制理论把这个摆动的速度尺度定义为自然角频率 `ω_n`：

```text
ω_n² = K_p/J
```

所以：

```text
ω_n = √(K_p/J)
```

反过来就是：

```text
K_p = J ω_n²
```

这就是第一条公式的来历。

### 6.1 ω_n 不是控制频率

`ω_n` 的单位是 `rad/s`，它表示希望的关节闭环运动快慢。

控制频率例如 `500 Hz`，表示控制器每秒重新计算 500 次。两者不是同一件事：

```text
ω_n       → 想让关节闭环反应多快
控制频率 → 控制程序每秒更新多少次
```

如果用普通频率 `f_n`，单位是 `Hz`，它与角频率的关系是：

```text
ω_n = 2π f_n
```

例如 `5 Hz` 对应的不是 `ω_n=5 rad/s`，而是：

```text
ω_n = 2×π×5 ≈ 31.4 rad/s
```

这个换算错误会让 `K_p` 因为平方关系而相差很大。

---

## 7. 再看振荡：阻尼比 ζ 和公式里的 2 从哪里来

现在回到：

```text
J e_ddot + K_d e_dot + K_p e = 0
```

我们想知道：`K_d` 多大时会振荡，多大时刚好不振荡？

### 7.1 先尝试一种会随时间放大或缩小的误差

> **第一遍如果害怕指数函数，可以直接跳到第 7.3 节看结果。**

假设误差可以有一种形式：

```text
e(t) = E exp(st)
```

- `t`：时间；
- `E`：这个误差模式的初始大小；
- `exp(st)`：指数形式，可以表示随时间衰减、放大，也可以和振荡组合；
- `s`：决定这种模式怎样变化的数。

`s` 可能是实数，也可能是包含虚数的复数。第一遍不需要学会计算复数；后面只会用“平方根内部是正、零还是负”来判断响应是否振荡。

这种形式有一个方便之处：

```text
e_dot  = s E exp(st)
e_ddot = s² E exp(st)
```

代入误差方程：

```text
J s² E exp(st)
+ K_d s E exp(st)
+ K_p E exp(st)
= 0
```

把共同部分 `E exp(st)` 提出来：

```text
(J s² + K_d s + K_p) E exp(st) = 0
```

对一个不是始终为零的误差模式，需要：

```text
J s² + K_d s + K_p = 0
```

它叫**特征方程**。能够使它成立的 `s` 就是闭环系统的**极点**。

### 7.2 极点不是新的物理零件

“极点”听起来很抽象，它实际在回答：

> 这个闭环允许误差按哪几种基本节奏衰减、放大或振荡？

解二次方程得到：

```text
s = [-K_d ± √(K_d²-4JK_p)]/(2J)
```

关键是平方根内部：

```text
K_d²-4JK_p
```

它决定两个极点的性质。

### 7.3 临界阻尼是“振荡”与“不振荡”的边界

可以把结果分成三种：

```text
K_d² < 4JK_p
→ 欠阻尼，通常会越过目标并衰减振荡

K_d² = 4JK_p
→ 临界阻尼，正好位于是否振荡的边界

K_d² > 4JK_p
→ 过阻尼，通常不振荡，但可能显得更迟钝
```

在边界上：

```text
K_d² = 4JK_p
```

所以临界阻尼增益是：

```text
K_d,critical = 2√(JK_p)
```

公式里的 `2` 就来自这个二次方程的振荡边界，不是为了好看人为放进去的。

阻尼比 `ζ` 定义为：

```text
ζ
= 实际阻尼/临界阻尼
= K_d/[2√(JK_p)]
```

因此：

```text
K_d = 2ζ√(JK_p)
```

再使用：

```text
K_p = Jω_n²
```

就有：

```text
√(JK_p)
= √(J×Jω_n²)
= Jω_n
```

最终得到：

```text
K_d = 2ζJω_n
```

这就是第二条公式的来历。

---

## 8. 用标准二阶式再检查一遍

把关节误差方程除以 `J`：

```text
e_ddot + (K_d/J)e_dot + (K_p/J)e = 0
```

理想二阶系统通常用下面的标准形式记录：

```text
e_ddot + 2ζω_n e_dot + ω_n² e = 0
```

把两行放在一起：

```text
关节：e_ddot + (K_d/J)e_dot + (K_p/J)e = 0
期望：e_ddot + 2ζω_n e_dot + ω_n² e = 0
```

希望它们描述同样的误差节奏，速度项前的系数就应该相等，位置项前的系数也应该相等：

```text
K_d/J = 2ζω_n
K_p/J = ω_n²
```

两边乘回 `J`：

```text
K_p = Jω_n²
K_d = 2ζJω_n
```

这就叫**系数匹配**。它之所以可以算，原因很朴素：

```text
我们有两个可调参数：K_p、K_d
我们提出两个期望特性：快慢 ω_n、阻尼程度 ζ
两个系数一一对齐
→ 就能反算两个增益
```

“极点配置”不是另一种 PD，而是这套设计视角的名字：

> 先决定希望误差按怎样的节奏衰减，再让 `K_p/K_d` 把闭环极点放到对应位置。

---

## 9. 完整算一个数字例子

假设某关节在代表性姿态下的等效惯量是：

```text
J = 0.2 kg·m²
```

希望关节的快慢尺度和阻尼比是：

```text
ω_n = 10 rad/s
ζ   = 1
```

### 9.1 计算 K_p

```text
K_p
= Jω_n²
= 0.2×10²
= 20 N·m/rad
```

### 9.2 计算 K_d

```text
K_d
= 2ζJω_n
= 2×1×0.2×10
= 4 N·m·s/rad
```

### 9.3 代回原方程检查

误差方程是：

```text
0.2 e_ddot + 4 e_dot + 20 e = 0
```

两边除以 `0.2`：

```text
e_ddot + 20 e_dot + 100 e = 0
```

期望的标准二阶式是：

```text
e_ddot
+ 2×1×10 e_dot
+ 10² e
= 0
```

也就是：

```text
e_ddot + 20 e_dot + 100 e = 0
```

两边一致，所以计算没有丢掉系数。

### 9.4 用单位再检查

```text
K_p(q_des-q)
= (N·m/rad)×rad
= N·m
```

```text
K_d(q_dot,des-q_dot)
= (N·m·s/rad)×(rad/s)
= N·m
```

P 项和 D 项最后都是力矩，才能相加后交给电机。

---

## 10. ω_n 和 ζ 应该怎样选

公式并没有自动告诉我们“机器人必须多快”。它只是把设计意图换成增益：

```text
你选择希望的 ω_n 和 ζ
→ 公式计算对应的 K_p 和 K_d
```

### 10.1 ω_n 越大会发生什么

```text
ω_n 增大
→ K_p 按 ω_n² 增大
→ K_d 按 ω_n 增大
→ 理想关节追赶目标更快
→ 但所需力矩更大，对延迟、噪声和结构弹性更敏感
```

如果希望速度翻一倍：

```text
ω_n → 2ω_n
```

那么：

```text
K_p → 4K_p
K_d → 2K_d
```

这解释了为什么“只想再快一点”可能很快就撞上电机力矩上限。

### 10.2 ζ 越大会发生什么

```text
0 < ζ < 1 → 欠阻尼，较快，但会有过冲或振荡
ζ = 1     → 临界阻尼，理想模型中处在振荡边界
ζ > 1     → 过阻尼，通常不振荡，但可能更慢
```

`ζ=0.7～1` 常被当作候选起点，不是机器人行业的强制答案。

### 10.3 从希望稳定时间反推ω_n

> **第一遍可跳过。**

对理想二阶系统，一个常用的 `2%` 稳定时间近似是：

```text
T_s ≈ 4/(ζω_n)
```

- `T_s`：误差进入并大致保持在最终值 `2%` 范围内所需的时间；
- `≈`：近似相等，不是精确恒等；
- 这个估算针对理想线性二阶响应，真机饱和、延迟或接触后不能照搬。

例如希望 `ζ=0.8`，理想上约 `0.5 s` 进入 `2%` 范围：

```text
ω_n ≈ 4/(0.8×0.5) = 10 rad/s
```

它可以帮助产生第一个候选值，但不能替代执行器能力和实际闭环测试。

---

## 11. 实际工程中 J 从哪里来

### 11.1 来自 URDF 和动力学模型

URDF（Unified Robot Description Format，统一机器人描述格式）通常描述：

- 每段身体的质量；
- 质心位置；
- 转动惯量；
- 关节怎样连接。

动力学库可以根据它们计算质量矩阵：

```text
M(q)
```

- `q`：整台机器人当前的关节姿态；
- `M(q)`：在当前姿态下，整机各部分共同加速有多难的矩阵描述。

在“其他关节的加速度暂时取零”的简化条件下，对角元素：

```text
M_ii(q)
```

可以作为第 `i` 个关节在这个姿态下的一个候选 `J`。

对浮动基人形，脚底是否接触、其他自由度是被固定还是允许运动，都会改变关节看到的有效惯性。因此 `M_ii(q)` 在这里只是名义设计的候选值，不是所有接触模式下的精确等效惯量。

但同一个关节在不同姿态下的 `M_ii(q)` 可能不同。手臂下垂、水平伸直、拿着重物，对肩关节的等效负载都不一样。

### 11.2 URDF 可能没有完整描述执行器

URDF 里的连杆惯性参数可能并不完整包含：

- 电机转子惯量；
- 减速器反射到关节端的惯量；
- 电机电流环和力矩环响应；
- 减速器弹性、摩擦和间隙；
- 线缆、外壳和真实装配差异。

所以“已有 URDF”不等于“真实 `J` 在所有姿态下已经被精确知道”。

### 11.3 来自安全的系统辨识

在可控的测试架条件下，可以给出已知的小力矩，测量关节加速度，再结合重力、摩擦和电机模型辨识一个有效的近似 `J`。

最朴素的直觉来自：

```text
J ≈ 力矩/角加速度
```

但真实试验不能直接忽略重力、摩擦和测量噪声，也不应在无支撑的整机上冒险施加测试力矩。这里只说明 `J` 也可以由数据估计，不展开真机辨识流程。

### 11.4 应该选哪个姿态的 J

没有一个对所有任务永远正确的固定答案。实际可以：

1. 选择任务中最有代表性的几个姿态；
2. 计算每个姿态下的候选 `J`；
3. 观察它的变化范围；
4. 选择一个代表性设计值，或按关节组别设计增益；
5. 把整个范围放回仿真和真实执行器响应中验证。

所以公式里的 `J` 更像一个**设计工作点**，而不是刻在关节上永远不变的常数。

---

## 12. 目标 q_des 明明在变，前面为什么假设它不动

这是理解 RL 位置动作的关键问题。

前面为了研究“给定一个目标后，关节自己怎样追过去”，才暂时令：

```text
q_dot,des  = 0
q_ddot,des = 0
```

如果目标也在运动，误差导数应该保留为：

```text
e       = q_des-q
e_dot   = q_dot,des-q_dot
e_ddot  = q_ddot,des-q_ddot
```

使用完整 PD：

```text
τ = K_p e+K_d e_dot
```

代入后，误差会受到目标加速度的驱动：

```text
J e_ddot + K_d e_dot + K_p e = J q_ddot,des
```

右边不再是零，表示目标自己在加速时，关节还需要追赶它。

如果可以提前加入理想加速度所需的前馈力矩：

```text
τ
= J q_ddot,des
  + K_p e
  + K_d e_dot
```

那么在这个理想单关节模型中，误差又回到：

```text
J e_ddot + K_d e_dot + K_p e = 0
```

这说明：

```text
前馈 → 提前承担目标运动预计需要的作用
PD   → 继续纠正真实跟踪误差
```

真实 RL 部署未必显式使用 `J q_ddot,des` 前馈。但策略在训练中可以学会提前改变关节目标，利用位置偏差让 PD 产生所需力矩。这是学习出来的闭环行为，不表示重力和动力学在现实中消失了。

还要区分两种实现：跟踪一条光滑轨迹的传统 PD 可以明确接收 `q_dot,des`；常见腿足 RL 位置动作则在每个策略时刻更新 `q_des`，同时仍把 `q_dot,des` 取为零，并不会必然把相邻两个 `q_des` 相减当成期望速度。这时可以把它理解为：Actor 在不断移动一个短暂保持的虚拟平衡点。

---

## 13. 人形机器人不是 29 个互不相干的单关节

固定在地面上的多关节机械臂，动力学更接近：

```text
M(q) q_ddot + h(q,q_dot)
= τ
```

人形的躯干基座不固定在大地上，还要加入浮动基和接触：

```text
M(q) v_dot + h(q,v)
= Sᵀτ + J_cᵀλ
```

这里：

- `q`：浮动基位姿和所有关节角组成的整机配置；
- `v`：浮动基速度和所有关节速度组成的广义速度；
- `v_dot`：对应的广义加速度；
- `M(q)`：姿态相关的整机质量矩阵，包含关节之间的惯性耦合；
- `h(q,v)`：重力、科里奥利和离心等不用单独展开的模型作用；
- `τ`：各关节的电机力矩；
- `Sᵀ`：把电机力矩放进整机广义力向量中对应关节位置的选择映射；
- `λ`：地面对脚等接触点的力；
- `J_c`：接触点雅可比，下标 `c` 表示 contact（接触），它不是前面的标量惯量 `J`；`J_cᵀ` 把接触力换算成对整机各自由度的广义作用。

关节 PD 则为：

```text
τ
= K_p(q_des-q)
  + K_d(q_dot,des-q_dot)
```

把 PD 产生的 `τ` 交给整机动力学后，真正的加速度由下列内容共同决定：

```text
所有关节 PD 力矩
+ 整机惯性耦合
+ 重力和速度相关作用
+ 脚底接触力
+ 摩擦、延迟和执行器特性
```

因此，就算每个关节都按单关节公式选了增益，整机的真实闭环极点也不会简单等于每个关节设计出的极点。

这并不否定底层 PD 设计。它只是重新划清任务边界：

> 单关节公式负责把底层局部反应带到合理区域；整机控制器或 RL 策略负责使这些关节在全身耦合与接触中协同工作。

---

## 14. RL 使用这组 PD 时，仿真里真正发生了什么

腿足 RL 常让 Actor（输出动作的策略网络）输出无量纲动作 `a[t]`，再换成关节目标：

```text
q_des[t] = q_default + α a[t]
```

- `q_default`：默认站姿；
- `α`：动作缩放，决定网络输出 `1` 代表多少弧度的目标偏移；
- `a[t]`：第 `t` 个策略时刻的网络动作；
- `q_des[t]`：这一时刻交给底层 PD 的关节目标。

之后：

```text
τ[t]
= K_p(q_des[t]-q[t])
  + K_d(q_dot,des[t]-q_dot[t])
```

本章采用的常见位置动作实现中，`q_dot,des[t]` 取零，所以 D 项就是 `-K_d q_dot[t]`。

整条训练闭环是：

```text
整机观测 o[t]
→ Actor 输出动作 a[t]
→ 动作缩放成 q_des[t]
→ 关节 PD 计算力矩 τ[t]
→ 仿真器用完整多体动力学和接触计算身体运动
→ 得到新的整机观测 o[t+1]
→ Actor 继续决定下一个动作
```

这里最关键的事实是：

> RL 训练没有把整台人形当成 `J q_ddot=τ`。

`J q_ddot=τ` 只在设计底层 `K_p/K_d` 时提供了一个物理起点。训练中的动作结果，仍由仿真器中的整机质量、惯量、重力、关节耦合、脚底接触和摩擦共同产生。

### 14.1 策略为什么能适应这组 PD

策略不是训练完以后才临时接上 PD。每一次试错都经过同一类动作链：

```text
策略提出 q_des
→ PD 按训练增益产生力矩
→ 身体真正运动
→ 奖励评价这样做好不好
→ 策略参数继续更新
```

经过大量经历，策略学到的其中一件事就是：

> 在这组 `K_p/K_d`、这个动作缩放和这种执行器响应下，应该输出怎样的 `q_des`？

因此训练结果属于“策略+PD+动作缩放+执行器+整机物理”组成的整个闭环，不只属于一个孤立网络。

### 14.2 RL 只在较慢频率更新 q_des，二阶设计还有意义吗

有。例如：

```text
Actor 频率：50 Hz  → 每 20 ms 给一次新 q_des
PD 频率：  500 Hz → 在这 20 ms 内继续多次纠正
```

在相邻两次 Actor 输出之间，`q_des` 通常被暂时保持。对底层 PD 来说，这就是一段短暂的“追赶固定目标”过程。

二阶公式设计的正是这种局部追踪反应的快慢和阻尼。Actor 则每隔一段时间重新移动这个目标，组成站立、迈步和恢复平衡。

---

## 15. 为什么从仿真到真机仍可能成功

现在可以把原因分层看清。

### 15.1 公式先把底层增益带到合理区域

它避免了完全没有物理依据地随意猜数：

```text
负载惯量更大
→ 想保持类似快慢
→ K_p/K_d 应相应增大
```

在 `ω_n`、`ζ` 选择合理并通过执行器检查的前提下，这给了 RL 一副大致不会软得支撑不住、也不会在理想模型中毫无阻尼地乱摆的“关节肌肉”。

### 15.2 PD 是根据真实误差每轮重算的

就算 `J` 估计并不完全准确，PD 仍会持续读取新的关节位置和速度，重新计算力矩。

如果真实 `J` 比设计值稍大，关节通常会比理想预期更慢，实际阻尼比也会改变。在误差不大、延迟和饱和仍受控的范围内，PD 不会因为 `J` 有一点误差就立刻失去全部纠偏能力；超出这个范围则仍可能振荡或失效。

这叫一定程度的鲁棒性：模型不完全准确时，闭环仍可在一定范围内工作。它不表示任何大小的误差都可以被忽略。

### 15.3 RL 在完整物理中学会了如何使用这组 PD

单关节公式忽略的重力、耦合和接触，并没有被 RL 训练忽略。它们仍在仿真器的每一步里产生结果。

策略经过奖励和反复试错，学会利用关节目标去间接产生：

- 支撑身体所需的力矩；
- 重心转移与迈步；
- 对地面接触变化的修正；
- 被推后的恢复动作。

它不一定在网络里显式写出“现在正在补偿重力”，但可以学会使 `q_des-q` 保留合适偏差，让 PD 产生支撑所需的力矩。

### 15.4 域随机化让策略不只适应一个精确参数点

如果训练中适度随机改变：

- 质量和惯量；
- 摩擦；
- 电机强度；
- `K_p/K_d`；
- 控制延迟；
- 观测噪声；
- 外部扰动；

策略学到的就不只是一个精确仿真模型上的单一动作。它可以在一定参数范围内学会继续闭环纠偏。

但域随机化不是“真机必成功”的保证。真机超出训练覆盖的参数、延迟或接触范围时，策略仍可能失败。

### 15.5 训练与部署保持了同一套动作语言

真机上必须尽量保持：

```text
关节顺序
+ 默认姿态
+ 动作缩放
+ K_p/K_d
+ 策略和 PD 频率
+ 力矩限幅
+ 延迟和执行器响应
```

如果这些大致对齐，策略在仿真中学会的“输出某个 `q_des` 会换来怎样身体反应”才可能继续成立。

---

## 16. 真机成功究竟证明了什么

假设你使用二阶公式设计 PD，用它训练 RL，然后成功部署真机。这个结果证明：

- 这组 PD 处于可工作区域；
- 策略学会了如何使用这组 PD；
- 仿真与真机的执行器和接口差异没有超过整个闭环的承受范围；
- 策略、PD、传感器、执行器和身体合在一起的真实闭环能工作。

它不能单独证明：

- 每个真机关节在所有姿态下都精确具有设计的 `ω_n`；
- 每个关节的真实阻尼比始终精确等于 `ζ`；
- 单关节模型已精确描述了整机动力学；
- 这组增益对所有任务、负载和硬件都是最优的。

最准确的结论是：

> 简化模型给出了有效的底层设计起点，而仿真中的完整动力学、RL 训练、鲁棒性和接口对齐，共同把这个起点变成了可部署的真机闭环。

---

## 17. 它有没有致命缺陷

这套方法本身没有一个导致“绝对不能用”的致命缺陷。它是经典而实用的增益设计起点。

真正会把它变成危险方法的，是忘记它的使用边界。

### 17.1 把估计的 J 当成永远不变的真值

姿态、负载和接触改变后，等效惯量会变。固定增益下，真实闭环快慢和阻尼程度也会变。

### 17.2 忽略力矩、速度和功率上限

公式可能要求某个电机输出 `100 N·m`，真实电机却只能给 `40 N·m`。发生饱和后，实际闭环已经不再是前面的理想线性二阶系统。

### 17.3 把ω_n设得太高，却忽略延迟和电机带宽

公式假设 PD 力矩立刻实现。真机根据过时误差用大力时，可能在命令生效前已经冲过目标，最终高频抖动或失稳。

### 17.4 不知道驱动器里已经有哪些环路

如果驱动器内部已经有位置环、速度环或力矩环，外部 PD 面对的就不是理想的“力矩数字直接变成关节力矩”。重复的高增益环路可能互相影响。

### 17.5 训练和部署使用不同的 K_p/K_d

对已经训练完的策略，大幅改变增益等于换了一副关节肌肉：

```text
同样的 q_des-q
× 不同的 K_p
→ 不同的力矩
→ 不同的身体反应
```

所以 `K_p/K_d` 是训练与部署契约的一部分，不是模型导出后可随意替换的附件。

### 17.6 没检查正负方向就上高增益

如果位置误差为正时，电机却让误差继续增大，那就从纠偏的负反馈变成了放大偏差的正反馈。增益越大，失控越快。

---

## 18. 一套可执行的增益设计与验证顺序

### 第一步：确认接口和正负方向

确认关节角、角速度、力矩的单位和正方向，以及驱动器内部已经运行什么环路。

### 第二步：估计代表性 J 和变化范围

使用 URDF/动力学模型或安全辨识，至少知道任务中 `J` 大概处在什么量级。

### 第三步：选择候选 ω_n 和 ζ

根据希望的响应快慢、电机能力、控制频率、总延迟和任务冲击程度选择，不只看“越快越好”。

### 第四步：计算候选 K_p/K_d

```text
K_p = Jω_n²
K_d = 2ζJω_n
```

如果已经先选定 `K_p`，也可以使用同一套关系：

```text
K_d = 2ζ√(JK_p)
```

### 第五步：先做静态能力与限幅检查

对代表性位置误差和速度，计算 PD 可能请求的力矩，检查电流、力矩、速度、功率和温度上限。

### 第六步：在仿真或固定测试架上看真实响应

不只看最终到没到目标，还要记录：

- 实际过冲；
- 稳定时间；
- 力矩饱和比例；
- 速度噪声产生的 D 项抖动；
- 总延迟和控制周期抖动；
- 不同姿态与负载下的变化。

### 第七步：把实际执行器特性放进 RL 训练

保持动作缩放、PD 增益、力矩限幅、策略频率和执行器响应与部署尽量一致，并对有依据的不确定性做随机化。

### 第八步：真机从低能量条件逐级验收

先固定基或吊架，再站立，再小速度命令，最后才扩大运动范围。公式计算完成不代表真机安全验收完成。

---

## 19. 把整篇压缩成一条因果链

### 单关节物理

```text
J q_ddot = τ
```

### PD 产生力矩

```text
τ = K_p(q_des-q)-K_d q_dot
```

### 换成误差运动

```text
J e_ddot + K_d e_dot + K_p e = 0
```

### 用期望的快慢和阻尼表示

```text
e_ddot + 2ζω_n e_dot + ω_n² e = 0
```

### 匹配系数

```text
K_p = Jω_n²
K_d = 2ζJω_n
```

### 放回 RL 整机闭环

```text
二阶公式设计底层关节大致怎样反应
→ RL 在完整仿真动力学中学习怎样协调这些关节
→ 域随机化与反馈吸收一定的参数误差
→ 训练与部署接口对齐
→ 整个闭环才可能在真机工作
```

最应该记住的不是某个默认增益，而是：

> 公式负责把物理直觉变成一组有依据的增益；反馈、完整仿真、RL 训练和真机验证负责处理简化模型没有包住的现实。

---

## 20. 不背公式时，应该记住什么

1. `J q_ddot=τ` 说的是力矩怎样通过惯量变成角加速度。
2. PD 给关节制造了虚拟弹簧 `K_p` 和虚拟减震器 `K_d`。
3. 自然角频率 `ω_n` 描述理想响应的快慢，阻尼比 `ζ` 描述相对临界阻尼的程度。
4. 两个可调增益与两个期望特性逐项对齐，所以可以反算 `K_p/K_d`。
5. 计算值是局部、名义工作点上的设计起点，不是整机精确极点和真机安全保证。
6. RL 没有使用单关节公式取代整机物理；它在完整仿真中学会如何使用这组底层 PD。
7. 训练和部署的增益、动作缩放、频率、限幅和执行器特性必须对齐。

---

## 21. 小练习

### 练习 1：从期望响应计算增益

某关节的代表性等效惯量为：

```text
J = 0.5 kg·m²
```

希望：

```text
ω_n = 4 rad/s
ζ   = 1
```

计算 `K_p` 和 `K_d`。

### 练习 2：响应速度翻倍

在 `J`、`ζ` 不变时，如果 `ω_n` 变为原来的 2 倍，`K_p` 和 `K_d` 分别变为原来的多少倍？

### 练习 3：为什么不能训练后随便改 K_p

策略训练时的 `K_p=40`，部署时改为 `K_p=80`。对同样的位置误差，P 力矩发生了什么变化？为什么这会改变策略面对的闭环？

### 练习 4：简化模型与完整仿真

使用单关节公式设计 PD，是不是表示 RL 仿真也只需要运行 `J q_ddot=τ`？请用自己的话解释。

### 练习 5：判断公式的边界

二阶公式计算出的 PD 请求 `80 N·m`，但真实电机只能输出 `35 N·m`。此时为什么不能继续宣称关节精确符合前面的标准二阶系统？

---

## 练习答案

### 答案 1

```text
K_p
= Jω_n²
= 0.5×4²
= 8 N·m/rad
```

```text
K_d
= 2ζJω_n
= 2×1×0.5×4
= 4 N·m·s/rad
```

### 答案 2

```text
K_p = Jω_n²
```

所以 `ω_n` 变为 2 倍时，`K_p` 变为 4 倍。

```text
K_d = 2ζJω_n
```

所以 `K_d` 变为 2 倍。

### 答案 3

对同样位置误差，`K_p` 从 `40` 变成 `80`，P 力矩会变成原来的 2 倍。策略训练时学习的是“输出某个目标偏移会引起怎样的身体反应”。部署时突然把 P 力矩翻倍，相当于改变了策略的动作含义。

### 答案 4

不是。单关节公式只用来设计底层关节大致的硬度、阻尼和响应速度。RL 训练仍要在完整多体动力学、重力、接触和摩擦中观察整个身体对这些关节力矩的真实响应。

### 答案 5

电机饱和后，实际力矩不再等于 PD 公式要求的力矩。从误差到力矩的线性关系被限幅打断，所以真实闭环不再是前面推导的那个理想线性二阶系统。
