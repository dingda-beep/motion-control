# 第 19 章：Sim2Real 与控制工程——仿真能走，真机为什么不走

> **所属部分**：第四部分 · 集成与实践；传统控制和学习控制在这里汇合。
>
> **前置知识**：[第 05 章](05-状态估计.md)、[第 07 章](07-人形控制系统架构.md)，以及至少一条具体控制路线。
>
> **核心问题**：模型、接口、传感器、执行器和时延的差异怎样沿闭环传播？
>
> **下一步**：[第 20 章：传统控制与学习控制如何选](20-传统控制与学习控制如何选.md)。

## 怎么读这一章

- **第一遍读第 1、5、6、9 节**：先检查观测、命令和真实响应三条接口是否一致。
- **第二遍读第 2～4 节**：再理解接触模型、域随机化和系统辨识分别在补什么差距。
- 第 7～8 节是真机部署安全清单，需要实验前逐项执行，而不是只读一遍。
- 第 10.1 节把前面所有检查放进一个从零 PPO 到 G1 吊架真机的真实案例，建议在第一遍最后完整读一次。

Sim2Real 是 Simulation to Reality，从仿真迁移到现实。它不是只属于强化学习的问题：传统模型控制也在做“数学模型到现实”的迁移。

本章始终跟着一次失败部署：同一台机器人在仿真中能以 `0.5 m/s` 平稳直行；第一次上吊架真机时，右膝动作总是慢一点，身体逐渐左右振荡，第三步右脚又发生滑移。我们不会用“仿真不真实”一句话结束，而要沿着观测、命令、执行器、接触和时间逐层追查，找到差异从哪里进入闭环。

## 1. 差距（Gap）在哪里

为什么仿真中能走，换到真机却可能立刻发抖甚至摔倒？控制器学到或算出的其实是一个闭环规律：

```text
看到某种观测 → 发出某个动作 → 预期机器人这样响应
```

只要现实中的“观测”“动作”或“响应”有一环和仿真不同，这条规律就可能失效。例如训练中 `0.1 rad` 的关节目标立即生效，真机却有 30 ms 延迟；策略会在旧动作尚未完成时继续纠错，最终形成振荡。

所以 Sim2Real 不是一个神秘的新算法，而是系统地寻找并缩小两套闭环之间的差异。下面的分类是在回答：差异究竟可能从哪里进入系统？

它的本质不是把仿真画面做得越来越像照片，而是让下面这条因果关系在仿真和现实中足够接近：

```text
控制器看到的数字
→ 控制器发出的数字
→ 执行器真正做到什么
→ 身体和接触怎样响应
→ 下一周期又测到什么
```

哪怕机器人外观完全一样，只要关节方向、延迟或动作缩放不同，闭环就已经不是同一道题。反过来，画面并不逼真的仿真，只要抓住任务真正敏感的动力学和接口，也可能很有训练价值。

也可以把它写成两套状态转移：

```text
仿真：x_sim[k+1] = f_sim(x_sim[k], u_sim[k], Δt_sim)
现实：x_real[k+1] = f_real(x_real[k], u_real[k], disturbance[k], Δt_real)
```

- `k`：离散控制周期编号；`k+1` 表示推进一个周期以后；
- `x_sim[k]`：仿真在第 `k` 个周期的机器人状态；
- `x_real[k]`：现实机器人在第 `k` 个周期的真实状态，真机通常不能完整直接读取；
- `f_sim`：仿真器采用的状态变化规律；
- `f_real`：现实世界真正发生的状态变化规律，它不是一条能够被程序完整写出的已知函数；
- `u[k]`：控制器或策略原本发出的命令；
- `u_sim[k]`：仿真执行器模型实际施加的输入；若仿真没有执行器动态，它可能被简单设成等于 `u[k]`；
- `u_real[k]`：真机驱动器、电机和传动系统最终真正实现的输入；
- `Δt_sim、Δt_real`：仿真和真实闭环实际经过的时间步长；
- `disturbance[k]`：真机这一周期受到的未建模外界扰动。

这两行不能都直接写 `u[k]`，否则等于提前假设“仿真和真机完全照着命令执行”，把最常见的执行器差异从公式中删掉了。

在 `f_sim` 中成功，只证明控制器适合所测试的仿真模型和参数分布。真机测试检验的是观测、估计、动作接口、执行器、接触和反馈时序组成的整条因果链，而不只是检查策略网络是否输出了一个看似合理的动作。

在开头的真机里，三个现象可能来自完全不同的环节：

```text
右膝总是慢一点
→ 可能是执行器带宽、增益、传动摩擦或关节接口不同

身体逐渐振荡
→ 可能是动作延迟、观测延迟或控制频率不一致

右脚第三步滑移
→ 可能是地面摩擦、鞋底材料、接触估计或落脚动作造成
```

它们也可能串联：右膝延迟先改变落脚姿态，脚底随后更容易滑，状态估计再因错误接触假设产生偏差，下一步动作于是更糟。Sim2Real 要研究的是这种差异怎样沿闭环传播。

![Sim2Real 差异可能出现在观测、执行和真实响应的整条闭环中](picture/sim2real_gap.svg)

### 模型参数

- 质量、质心、惯量；
- 摩擦、阻尼、齿隙；
- 电机力矩常数；
- 鞋底刚度；
- 电池电压和温度。

### 接触

- 仿真地面过平；
- 摩擦模型过理想；
- 穿透深度和接触刚度不真实；
- 时间步长改变冲击；
- 自碰撞几何过简。

### 执行器

- 命令延迟；
- 力矩带宽；
- 饱和和变化率；
- 执行器机械结构差异，例如 SEA（Series Elastic Actuator，串联弹性执行器）和 QDD（Quasi-Direct Drive，准直驱）；
- 低层驱动器内部控制未知。

QDD 通常具有较低减速比、较高力矩密度和较好反驱性。SEA 则在电机和负载之间引入弹性元件。二者的结构和内部控制不同，相同外部命令不一定产生相同响应。

### 传感器和计算

- IMU（Inertial Measurement Unit，惯性测量单元）偏置、编码器噪声；
- 状态估计延迟；
- 丢包和时钟偏差；
- 神经网络/求解器实际耗时抖动；
- CPU（Central Processing Unit，中央处理器）与 GPU（Graphics Processing Unit，图形处理器，擅长大规模并行计算）上的数值差异。

## 2. 仿真器中的接触不是现实复刻

常见接触处理大致包括：

- penalty/spring-damper（惩罚法/弹簧—阻尼）：允许物体在数值上发生很小的重叠，重叠越深、相对运动越快，计算出的反力通常越大；
- constraint/time-stepping（约束/时间步进）：每个物理小步求一组接触作用，使物体尽量不互相穿透；其中冲量可以先理解成碰撞在极短时间内累计产生的“改变速度的作用”；
- soft contact（软接触）：故意把绝对坚硬的接触变得柔和一些，换取更稳定、更快速的数值计算。

没有“对所有任务绝对最真实”的设置。并行训练常牺牲精细接触换吞吐量。

这三类名称描述的是仿真器怎样近似“不能穿透、会碰撞、会摩擦”的现实接触，不是三种机器人控制器。控制策略最后学到的反应会受到这套数值近似影响，所以换仿真时间步长或接触参数，也可能改变训练结果。

## 3. Domain Randomization

Domain Randomization（域随机化，DR）在训练中随机改变参数：

```text
m∼ U(m_min,m_max)
```

- `U(a,b)`：在区间 `[a,b]` 均匀采样；
- `m`：质量。
- `∼`：从右侧给出的随机分布中采样；
- `m_min、m_max`：训练随机化允许的质量下界和上界。

可随机：

- 质量和惯量；
- 摩擦；
- 电机强度；
- PD（Proportional-Derivative，比例—微分控制）增益；
- 观测噪声；
- 动作延迟；
- 外部推力；
- 地形。

目标不是让策略“见过一切”，而是让它不依赖仿真器某个过于精确的偶然细节。

鲁棒性的含义也不是模型复杂到包含世界全部细节，而是闭环在一组合理的模型误差、噪声和扰动下仍能恢复。扩大随机化范围只有在策略还能从可用观测中辨认并应对这些变化时才有意义。

随机范围太窄无效；太宽则训练问题可能变得不可学。范围应由测量、系统辨识和风险分析驱动。

## 4. System Identification

System Identification（系统辨识，SysID）用真实输入输出数据估计模型参数。

例子：给单关节安全的扫频或阶跃命令，记录位置、速度、电流。

- 阶跃命令：目标从一个固定值突然变到另一个固定值，用来观察响应有多快、是否超调；
- 扫频命令：让输入振动频率从低到高逐渐变化，用来观察关节对快慢不同命令的跟随能力；
- 拟合：调整模型参数，让模型输出尽量接近真实记录。

然后可以估计：

- 等效惯量；
- 阻尼；
- 静摩擦；
- 延迟；
- 电流到力矩比例。

不要一开始就在全身行走中同时猜所有参数。先做组件级实验，再做整机残差分析。

因此，对“右膝慢”不应直接把全身质量、摩擦、网络和奖励一起改掉。可以先让机器人悬空，在安全范围内给左右膝相同的小幅阶跃或扫频命令，比较命令、编码器位置、速度和电流响应。若右膝单关节就明显滞后，问题已经在行走策略以下被复现，定位范围会小很多。

## 5. 动作接口必须一致

训练输出可能是关节位置偏移：

```text
q_des
=
q_default
+αa
```

- `a`：策略动作，常裁剪在 `[-1,1]`；
- `α`：动作缩放，单位 rad；
- `q_default`：默认站立角。

然后低层 PD：

```text
τ_cmd
=
K_p · (q_des-q)
- K_d · q_dot
```

- `q、q_dot`：由真机传感器或状态估计得到的当前关节位置和速度；
- `K_p、K_d`：与训练一致的逐关节位置和速度增益；
- `τ_cmd`：PD 请求的关节力矩命令，不是真实电机已经实现的 `τ_real`。

驱动器限幅、延迟和电机动态仍会造成 `τ_real≠τ_cmd`。日志最好同时记录策略动作、`q_des`、`τ_cmd` 和电机反馈，才能判断差异在哪一层产生。

真机必须和训练时保持：

- 关节顺序；
- 正负方向；
- 零位；
- 动作缩放；
- 增益；
- 控制频率；
- 动作保持方式；
- 延迟模型。

这类接口错误比“算法不够先进”常见得多。

例如训练部署代码若把右膝动作缩放从 `0.25 rad` 写成了 `0.20 rad`，它也会表现为“右腿迈不开”。这不是需要域随机化的物理不确定性，而是一份离散的软件契约写错了。随机化不会把错误关节顺序和错误单位训练成正确接口。

### 5.1 安全后处理也是动作接口的一部分

常见部署链不是 `策略→电机`，而是：

```text
策略原始动作
→ 动作缩放得到原始 q_des
→ 关节硬限位、相对当前位置限幅、单步变化率限制
→ 保护后的 q_des
→ PD 与驱动器
```

每一次后处理都会改变真实下发的命令，也会改变下一帧观测。它必须纳入训练—部署接口审计。

一个真实 G1 吊架实验提供了很直观的反例。策略输出关节位置目标，某版保护把“目标与实测关节的最大误差”限制为 `0.25 rad`，把每 `20 ms` 的最大目标变化限制为 `0.08 rad`。机器人能上电，也有动作，却无法在移动中正常承重。

以踝关节位置增益 `K_p=28.5 N·m/rad` 粗略计算，`0.25 rad` 的误差上限让比例项最多只提供：

```text
28.5 × 0.25 ≈ 7.1 N·m
```

而同一策略正常回放需要约 `25～29 N·m` 的峰值支撑力矩。问题不是网络“没学会”，而是部署保护移除了它学会使用的控制权限。

项目随后在相同模型、随机种子（seed）和场景上做 A/B。这里 A/B 的意思是只改变待检查的保护配置，让其他条件尽量相同，再比较结果：

```text
无这两项保护：移动通过
只加任一项旧保护：移动失败
只保留关节硬限位：移动仍通过
```

再扩大种子和场景，才选择新的保护范围。这个例子说明：

> 安全层不是越保守越安全。它既要挡住异常命令，也必须保留正常闭环所需的控制能力。

因此日志应同时记录：策略原始动作、缩放后的原始目标、保护后目标、实际关节反馈、每种保护的触发次数与最大修正量。否则只看到“机器人没站住”，很容易误判成奖励、动力学或网络问题。

## 6. 观测接口必须一致

检查：

- 观测顺序；
- 坐标系；
- 归一化均值和尺度；
- 历史帧顺序；
- 重力投影定义；
- 速度是世界系还是身体系；
- 四元数顺序；
- 是否使用仿真真值。

一阶段策略若偷偷使用了真机不可测的质心真速度，就不是可部署策略。

对第 13 章的 MLP Actor 来说，这一节检查的其实是网络输入契约。权重 `W、b` 即使导出完全正确，只要第 37 个输入在训练中代表“左膝速度”，部署代码却放入“右膝速度”，前向计算仍会忠实地产生错误动作。因此部署物不只是一个网络权重文件，还应包含或明确绑定：

```text
观测字段及顺序
+ 单位和坐标系
+ 归一化统计
+ 历史窗口构造
+ 网络结构与权重
+ 动作顺序、缩放和后处理
```

只说“1014 维输入、29 维输出”远远不够；尺寸相同不能证明每一维的含义相同。

### 6.1 可部署对象不是孤零零的权重文件

同一份 ONNX（Open Neural Network Exchange，开放神经网络交换格式）模型，如果配上不同的观测顺序、历史帧、动作缩放、PD 增益或事件持续时间，就是不同的闭环控制器。一个可复现的部署包至少应冻结：

```text
模型文件与哈希
+ 观测字段、顺序、历史、单位和归一化
+ 动作顺序、默认姿态、缩放与 PD 增益
+ policy dt、物理/发送频率与动作保持方式
+ 安全阈值和硬件型号
+ 状态机、事件触发、持续、复位与权限
+ 配置清单、代码版本和验证报告
```

哈希是文件内容的指纹，用来确认测试过的文件与准备部署的文件是同一份；manifest（清单）则记录这一版究竟绑定了哪些文件和证据。

有状态逻辑尤其不能遗漏。例如策略多了一维“自由落体事件记忆”，还必须说明：连续多少帧低加速度才触发、触发后保持多久、重新承重多久才允许下一次触发。只复制网络、没有复制这套时间规则，1015 维输入中的最后一维已经换了含义。

真机前应运行静态契约检查，自动比对训练配置、部署配置和模型：输入输出 shape（张量每个方向有多少个数字）、观测顺序、关节顺序、默认姿态、动作尺度、PD 增益和时间步。机器可检查的契约不要只靠人眼读 YAML 配置文件。

## 7. 渐进式部署

推荐门控：

1. 离线回放真实日志，检查策略输出范围；
2. 软件在环（SIL，Software-in-the-Loop）：真实控制软件连接模拟机器人；
3. 硬件在环（HIL，Hardware-in-the-Loop）：把真实控制器或驱动硬件接入测试，但暂不让机器人承重；
4. 吊架/保护绳下低增益站立；
5. 原地踏步；
6. 低速行走；
7. 逐步扩大命令和扰动范围。

每一级都定义进入条件和回退条件，不凭“看起来差不多”升级。

### 7.1 每一级扩大的是“已经被证据允许的包络”

渐进部署不是简单按日期把速度调大。每一级都应冻结：

- 模型和配置哈希；
- 允许运行的最长时间；
- 速度、横移、转弯和扰动范围；
- 自动停止与人工急停条件；
- 本级必须出现的日志证据；
- 通过后下一次只扩大哪一个维度。

例如先验证零命令站立，再验证短时小前进，再验证移动中回零，然后才扩大转弯。若部署端加入“快速转弯时优先减小前进速度”的命令整形，它虽然没有重训 ONNX，也已经改变了完整控制器，仍要生成新配置版本并重跑相关场景。

控制权也必须作为门槛：请求厂商控制服务释放、等待硬件 ACK、确认命令通道没有第二个活跃发布者，然后才允许策略发非零命令。退出时要恢复安全原生模式并再次等待 ACK。仅在本进程里把状态改成 `ACTIVE`，不构成硬件证据。

## 8. 安全预算

真机实验前至少明确：

- 最大关节力矩、速度、位置；
- 最大动作变化率；
- 可接受机身 roll/pitch；
- 急停链路；
- 通信超时时间；
- 电机温度阈值；
- 吊架承载和人员站位；
- 摔倒后的动作策略。

机器人摔倒不是普通异常，它会改变传感器、接触和机械结构状态。恢复控制应是单独设计的行为。

### 8.1 实时性要量“哪一段、最坏多慢”

只报告平均推理耗时不够。控制周期更关心：

- P99：99% 的周期不超过多长；
- max：观察到的最坏周期；
- deadline miss：超过周期预算多少次，是否连续发生；
- 被计时区间：只计 ONNX，还是包含传感器更新、观测拼接、日志和命令发布。

最后一项特别重要。若计时器包住 `传感器→观测→日志→ONNX→动作`，出现 `109 ms` 就不能直接写成“ONNX 推理耗时 109 ms”。同步写盘和每条日志立即刷新，也可能把实时线程卡住。

一个可靠做法是分别记录各阶段耗时，并让控制循环中的日志限频、缓冲或交给非实时线程。外部系统服务再负责总运行时上限和进程异常后的恢复，因为主控制线程一旦卡死，不能指望它自己执行退出逻辑。

## 9. 诊断顺序

真机一启动就异常：

1. 检查关节索引、方向、零位和单位；
2. 检查控制频率与延迟；
3. 检查观测坐标系与归一化；
4. 检查 PD 增益和动作缩放；
5. 检查力矩饱和；
6. 再检查模型参数和训练分布；
7. 最后才考虑更换算法。

这种顺序先排除离散的软件契约错误，再处理连续的模型误差。

## 10. 把第一次真机失败完整查一遍

```text
1. 先保持吊架和低速，不扩大测试风险

2. 对齐日志时间戳
   同时记录观测、策略动作、q_des、τ_cmd 和电机反馈

3. 发现右膝从收到命令到明显响应比仿真晚约 25 ms
   用悬空单关节实验复现，并更新执行器延迟模型

4. 发现真机策略频率偶尔从 50 Hz 掉到 42 Hz
   修复推理与通信调度，记录最坏周期而不只看平均值

5. 检查右脚滑移时的足底接触和地面
   测量合理摩擦范围，把鞋底和接触差异纳入模型与随机化

6. 用修正后的仿真重新训练或验证
   再做离线回放、悬空、站立、踏步、低速直行

7. 真机仍用新测量继续检验
   仿真中的改进只产生了新假设，不是现实成功证明
```

这就是 Sim2Real 的工程方法：把一句“真机不对”拆成可测量、可复现、可逐层证伪的差异。

### 10.1 一个从零 PPO 到 G1 真机的完整证据链

[G1-Command-RL](https://github.com/dingda-beep/G1-Command-RL) 把本章的抽象检查变成了一条真实时间线。项目从随机权重训练统一速度命令策略，最终在 Unitree G1 吊架条件下完成站立、前后与横向移动、快速转弯、停止和恢复原生模式。值得学习的不是“用了 PPO 所以成功”，而是每次失败怎样缩小原因范围。

#### 训练阶段：能力不是按训练步数单调增加

站立、移动、停止和重新起步在不同阶段逐渐出现；后续 checkpoint 又可能遗忘旧能力。项目没有把“最后 checkpoint”当答案，而是用固定场景筛选版本，分别检查方向、速度、停止、扰动和长时间站立。

#### 恢复阶段：先用 Oracle 判断问题在哪一层

仿真真值和专项专家先被当作诊断上界：Oracle 成功而普通观测失败，说明恢复动作并非不可能，瓶颈更靠近可观测性与事件识别。最终没有把仿真接触真值搬到真机，而是使用 IMU 线加速度和一段有触发、持续、复位条件的因果事件记忆。

#### 仿真阶段：旧策略突然成功，可能是仿真器修对了

某次复杂地形失败最后定位到过薄的 heightfield（高度场）碰撞几何。修改地形基体厚度后，旧策略无需重训就成功。这说明“换奖励、加网络、继续训练”不是默认动作；若旧控制器在只修复环境以后通过，根因就在测试世界，而不在策略参数。

#### 真机阶段：安全保护也可能制造失败

前述 `0.08 rad/周期` 和 `0.25 rad` 两道限制让策略失去承重能力。相同模型的保护开关 A/B 把问题从“整个 Sim2Real 都可能有错”收缩到两行后处理。随后再用多种随机种子、命令和推扰验证新阈值，而不是直接删掉全部保护。

#### 实时阶段：“推理超时”最后是同步日志抖动

一次长时运行在约一分钟后报告百毫秒级超时，但计时区间其实包含传感器、观测、同步刷盘、ONNX 和动作。移除实时循环内的同步日志写盘、改用系统服务统一收集的 journal 日志并限频后，长时复测通过。修复对象不是网络结构，而是实时路径中的 I/O（Input/Output，输入输出；这里主要是日志写盘）。

#### 控制权阶段：同一策略可以走两条硬件命令链

项目既测试厂商 UserCtrl（用户控制接口）路径，也测试直接写入 `rt/lowcmd`（更底层关节命令通道）的路径。原始低层路径使用 `50 Hz` 策略和 `500 Hz` 最新命令保持线程，同时检查是否出现外部发布者；退出后恢复原生被动模式。它不能宣称“完全没有厂商控制”，因为电机内部电流和位置环仍然存在。准确写清“绕过了哪一层、保留了哪一层”，比给架构贴“端到端”标签更重要。

#### 发布阶段：部署文件也会损坏，恢复不能依赖主进程

一次重启暴露了 `0 byte` 二进制问题，随后发布改为：先复制到临时文件、检查架构与非空、同步完成，再原子重命名，也就是让最终文件名一次从完整旧文件切换到完整新文件，不暴露复制到一半的状态。外部系统服务设置硬超时，并在主进程异常退出后执行恢复命令。

把这些事实压缩起来，真正通过真机验收的是：

```text
策略能力
+ 训练/部署接口契约
+ 安全后处理
+ 控制权与多频率线程
+ 可恢复状态机和外部监督
+ 可追溯模型、配置、哈希与日志
```

这个案例没有削弱强化学习的价值，恰恰说明强策略只有进入一条被验证的闭环因果链，才从“一个成功模型”变成“一个可运行控制系统”。

## 本章小结

- Sim2Real 是模型、传感器、动作接口、执行器、接触和时延组成的整条闭环差异，不只属于强化学习。
- 随机化用于覆盖不确定性，系统辨识用于缩小已知误差；二者都不能修复训练与部署接口不一致。
- 网络、部署契约、事件状态、安全参数和控制权恢复共同组成可复现的控制器；孤立权重文件不够。
- 真机部署应按观测、动作、后处理、模型、求解/策略、驱动和反馈顺序诊断，并设置明确安全预算与降级出口。

## 11. 练习

1. 域随机化和系统辨识分别解决什么？
2. 为什么训练与部署的 PD 增益不同会造成大 gap？
3. 一个策略使用了仿真真值线速度，真机有什么替代方案？
4. 为什么真机部署应该从站立而不是直接跑步开始？
5. 为什么更严格的动作限幅可能让机器人反而更容易倒？
6. 一次计时得到 `100 ms`，为什么不能立即断言“ONNX 推理用了 100 ms”？

答案：1）DR 提高对参数分布的鲁棒性，SysID 缩小模型中心值误差；2）相同动作对应完全不同力矩和闭环动态；3）状态估计、历史编码器或 Teacher–Student 蒸馏；4）减少接触模式、速度和能量，便于隔离接口与估计问题；5）限幅会进入闭环，过严时会移除承重和恢复所需的控制能力；6）必须先说明计时器包住了哪些步骤，传感器、观测、日志、发布和调度抖动都可能包含在内。
