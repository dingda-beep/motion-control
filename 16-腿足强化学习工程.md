# 第 16 章：腿足强化学习工程——观测、动作和奖励就是你的“隐式控制设计”

RL（Reinforcement Learning，强化学习）论文常突出网络结构，但实际成败往往由环境接口、奖励、重置、并行度和部署一致性决定。

先把一轮交互看成一个普通控制循环：

```text
环境给出观测 o[t]
→ 策略输出动作 a[t]
→ 动作经缩放和底层控制器作用于机器人
→ 仿真推进到下一时刻
→ 环境返回新观测、奖励和是否结束
```

网络并不知道数组中每个数字的物理含义。观测漏掉关键信息、动作单位写错或奖励衡量错目标时，再大的网络也只会把错误接口学得更熟。因此本章不是零散的“训练技巧”，而是在定义策略实际面对的控制问题。

![强化学习环境中观测、策略、动作和奖励的循环](picture/rl_interaction_loop.svg)

## 1. 观测设计

典型 Actor 观测：

```text
o_t=
[
v_cmd,
ω_body,
g_proj,
q-q_default,
q_dot,
a_(t-1)
]
```

- `v_cmd`：速度命令；
- `ω_body`：身体角速度；
- `g_proj`：重力方向在身体坐标系的投影，表达姿态；
- `q-q_default`：关节相对默认姿态的偏移；
- `q_dot`：关节速度；
- `a_(t-1)`：上一动作，帮助策略理解动作保持和执行器动态。

观测应满足真机可获得、坐标系明确、数值尺度合理。

这里的 `o_t` 是传感器测量、状态估计和历史的组合，不应默认等于真实状态 `x_real`。同样，策略动作 `a_t` 是关节目标、力矩或残差等命令，不是“下一帧机器人一定达到的状态”。两者之间仍隔着动作缩放、底层控制器、执行器和真实动力学。

## 2. 特权观测

Critic 训练时可使用：

- 真值基座速度；
- 精确地形高度；
- 外部扰动力；
- 摩擦系数；
- 负载参数。

这叫 asymmetric actor-critic（非对称 Actor-Critic）：Actor 只看可部署观测，Critic 看更多仿真信息以更准确估值。部署只保留 Actor。

## 3. 动作空间

常见选择：

### 位置目标

```text
q_des
=
q_default
+αa_t
```

易训练，底层 PD（Proportional-Derivative，比例—微分控制）像虚拟弹簧和阻尼一样提供稳定性。

### 直接力矩

```text
τ=τ_scalea_t
```

物理表达直接，但探索更危险、对频率和执行器模型更敏感。

### 残差控制

```text
u
=
u_nominal
+αa_t
```

策略只修正传统控制器或参考轨迹。能降低探索难度，但上限受名义控制器结构影响。

## 4. 奖励设计

速度跟踪常用指数：

```text
r_v =
exp(
    -‖v_xy-v_cmd‖² / σ_v
)
```

- `v_xy`：实际水平速度；
- `σ_v`：误差尺度参数；
- 误差为 0 时奖励为 1，误差变大时平滑下降。

惩罚项：

```text
r_τ=-w_τ‖τ‖²
```

```text
r_action_change
=
-w_action_change · ‖a[t]-a[t-1]‖²
```

分别限制能量/力矩和动作抖动。

总奖励：

```text
r =
w_v · r_v
+ w_ω · r_ω
+ r_τ
+ r_action_change
+ r_alive
```

权重反映任务偏好，但奖励项之间可能冲突。先定义可测验的行为，再设计奖励，不应看到坏动作后无限追加惩罚。

## 5. 奖励漏洞

例子：

- 只奖励前进位移：机器人可能扑倒后滑行；
- 只惩罚能耗：机器人选择不动；
- 接触奖励写错：机器人拖脚；
- 姿态奖励过强：机器人不敢加速；
- 动作平滑过强：来不及对扰动反应。

每个奖励项都应单独记录时间序列和 episode 总和，确认它真的在度量想要的行为。

## 6. Reset 与 termination

Episode（回合）结束条件：

- 身体高度过低；
- roll/pitch 超阈值；
- 非允许部位碰地；
- 关节严重超限；
- 达到最大时长。

Early termination（提前终止）减少无意义数据，但也会制造“终止边界恐惧”：策略在接近边界时可能采取剧烈动作避免终止。模仿学习中尤其明显。

Reset distribution（重置分布）决定策略从哪些状态学起。只从完美站姿开始，会缺少恢复能力。

## 7. Curriculum

Curriculum learning（课程学习）逐渐增加难度：

- 先小速度，再大速度；
- 先平地，再斜坡、台阶、随机地形；
- 先小扰动，再大扰动；
- 先低随机化，再扩大参数范围。

升级条件最好基于成功率或跟踪指标，而不是固定训练步数。

## 8. 并行环境

GPU（Graphics Processing Unit，图形处理器，擅长并行计算）仿真可同时运行数千个机器人。每次 rollout 收集：

```text
N_samples
=
N_env× T
```

- `N_env`：并行环境数；
- `T`：每个环境本轮步数。

例如 4096 个环境，每个收集 24 步，就是 98,304 个样本。batch 太大可能减少更新频率，太小则梯度噪声大。

## 9. 训练指标

不要只看 episode reward：

- 命令速度跟踪误差；
- 摔倒率和 episode 长度；
- 力矩、功率、关节速度；
- 足端滑移；
- 动作变化率；
- 不同地形/参数桶的成功率；
- Actor 标准差和熵；
- Critic explained variance；
- KL divergence（Kullback–Leibler divergence，KL 散度）：衡量新旧策略分布差异。

若平均奖励上升但最坏 5% 地形成功率下降，部署风险可能反而增加。

## 10. 控制频率与 decimation

物理仿真可能 500 Hz，策略 50 Hz。每个策略动作保持 10 个物理步：

```text
decimation
=
(500)/(50)=10
```

改变 decimation 会改变闭环动态、动作平滑和奖励时间尺度。部署频率必须一致，或重新训练/验证。

## 11. 归一化和裁剪

```text
o_normalized =
clip(
    (o-μ)/σ,
    -c,
    c
)
```

- `μ,σ`：训练统计或手工尺度；
- `c`：裁剪阈值；
- 真机必须使用相同统计量和顺序。

动作先由策略输出，再裁剪、缩放为物理单位。日志应同时记录裁剪前后动作。

## 12. 最小实验顺序

1. 单环境、可视化、固定参数，验证接口；
2. 多环境训练站立；
3. 加速度命令跟踪；
4. 加随机推力；
5. 加参数随机化；
6. 加地形课程；
7. 独立评估环境，不参与训练；
8. 导出策略做部署一致性测试。

部署后要把训练图中的“仿真推进”替换成真实机器人与环境，把“新观测”替换成真实传感器链。奖励和 episode 终止主要服务于训练；在线稳定依靠的是策略、低层控制和物理系统共同形成的反馈，而不是部署时继续给机器人算奖励分数。

## 13. 练习

1. 为什么 Actor 不能使用仿真真值摩擦系数？
2. 只奖励速度跟踪会出现哪些副作用？
3. 物理频率 1 kHz、策略 100 Hz，decimation 是多少？
4. 为什么改变控制频率后要重新审视 `γ` 和奖励缩放？

答案：1）真机通常无法实时直接测得；2）可能高能耗、抖动、拖脚、姿态难看；3）10；4）每个时间步代表的真实时长变了，折扣和累计奖励的时间含义随之改变。
