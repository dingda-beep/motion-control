# 全书初学者可读性审查：从“符号解释”升级到“叙事能否走通”

> 本文记录当前正文的第三轮审查结果。
>
> 第一轮主要检查初学者理解断点，第二轮检查公式、符号、单位和因果链；第三轮进一步追问：**读者能否从一个具体困难出发，看着新概念一步步长出来，并最终回到真实机器人闭环。**

第 04 章仍是写作标尺：固定一个对象，先让问题真实发生，再讲直觉、数字、公式、边界和反馈结果。其他章节不要求复制它的篇幅，但要保留同样的展开逻辑。

---

## 1. 为什么“每个符号都解释了”仍可能看不懂

下面这段话即使逐项解释 `z、H、c、A_eq`，初学者仍可能不知道为什么突然要学 QP：

```text
在 z 中最小化二次目标
满足线性等式和不等式约束
```

真正缺少的不是字典，而是问题长出来的过程：

```text
机器人右手想前伸
→ 脚又不能离地
→ 电机力矩还有上限
→ 几个要求不能各自独立控制
→ 必须从许多候选动作中选择一个折中
→ “候选量、偏好、不可违反条件”自然形成优化问题
→ 某一类目标和约束恰好能写成 QP
```

所以第三轮不再只问“名词是否定义”，而是问：

1. 为什么出现这个问题；
2. 读者已经会的办法为什么不够；
3. 新概念解决链条中的哪一处困难；
4. 公式左右两边各自在故事中表示什么；
5. 计算结果是谁的参考或命令；
6. 真实世界最后怎样反馈并纠正它。

---

## 2. 当前统一审查标尺

每一章至少要通过下面九项检查。

### 2.1 开头先出现机器人困难

在第一个重要缩写或矩阵之前，读者应该知道机器人正在做什么、哪里失败了。

### 2.2 有固定的贯穿对象

可以是一根关节、二维二连杆、一台被推的人形、一次跑步落地或一次真机失败。后文引入新概念时，应回到同一个对象，而不是每节重新开场。

### 2.3 前一种办法确实“不够”

新方法不能凭论文目录突然出现。例如：

```text
只靠本体感知要等脚撞到台阶才知道
→ 因此才需要视觉提前提供未来接触证据
```

### 2.4 先讲选择，再讲公式

先让读者知道系统要在什么候选量中作什么决定，才出现 `min`、矩阵和损失函数。

### 2.5 每个符号都在原地回答四件事

1. 它是什么；
2. 它从哪里来；
3. 谁使用它；
4. 它是事实、测量、估计、参考、候选量还是命令。

只写“`u`：控制输入”不算通过。

### 2.6 明确时间顺序

读者要能回答：这是当前测量、上一周期动作、未来预测，还是训练时才有的标签。

### 2.7 第一遍和第二遍分层

世界观、最小闭环和失败边界属于第一遍；精确离散、求解器变体、论文结构和较密公式可明确放到第二遍。不能要求读者在同一页同时完成启蒙与研究综述。

### 2.8 计算结果必须回到物理链

`q_des`、接触力参考、落脚点、策略动作和优化解都不是现实结果。后文要继续说明它怎样经过低层控制、电机、接触和新测量。

### 2.9 章末能够用人话复述

不看公式时，读者应能讲出：机器人遇到什么困难，新方法看见什么、选择什么、输出什么、哪里还可能错。

---

## 3. 全书真正的总叙事

全书不是 22 个机器人名词。它在回答同一个问题：

> 一个“我希望机器人这样动”的意图，怎样在信息不完整、物理受限、现实会出错的条件下，变成下一小步动作，并根据结果继续修正？

主线按因果展开：

```text
00：先看完整闭环，区分现实、测量、估计、模型和命令

01：要控制，先说清位置和方向是相对谁表达
02：知道关节角以后，怎样判断手脚在哪里、怎样瞬时运动
03：仅有几何还不够，力、质量和接触怎样改变运动
04：模型永远不完美，因此必须反复测量并纠偏
05：控制器读不到完整现实，只能融合模型和测量形成当前判断
06：多个目标和限制冲突时，怎样从候选动作中作选择
07：把目标、规划、估计、控制、驱动、安全和真实反馈接成系统

08～12：传统路线怎样选择不同程度的物理简化并向前预测
13～18：学习路线怎样从试错扩展到工程接口、隐藏信息、视觉、模仿和多技能

19：无论哪条路线，仿真或模型进入真机后都会遇到整条闭环差异
20：按任务结构与风险组合方法，不按算法阵营站队
21：把知识变成逐级增加难度、每级都可验收的项目
```

前一章提出的能力，正是后一章默认的起点。例如第 04 章说“必须反馈”，第 05 章马上追问“反馈里使用的状态从哪里来”；第 05 章形成 `x_hat`，第 06 章才有依据在多个动作之间选择；第 06 章只讲选择结构，第 07 章再把它放回完整软件与硬件系统。

---

## 4. 逐章叙事审查结果

当前不再用“低、中、高”给读者制造警报，而是记录每章的贯穿对象、因果问题与分层边界。

| 章节 | 贯穿对象或场景 | 新概念怎样长出来 | 第一遍边界 |
|---|---|---|---|
| 00 全局 | 人给人形机器人速度命令 | 目标必须经过模型、估计、控制、电机和现实反馈 | 先分清 `x_real、y、x_hat、u、u_real` |
| 01 坐标与姿态 | 同一个障碍物在身体系和世界系中数字不同 | 位置数字离不开参考系，旋转和平移由此出现 | 第一遍抓住“谁相对谁”，姿态表示细节可二读 |
| 02 运动学与雅可比 | 二维二关节机械臂 | 末端位置对关节的瞬时变化自然形成雅可比 | 伪逆、零空间可二读；专题章提供逐行推导 |
| 03 动力学与接触 | 单关节扩展到双脚人形 | 几何不能预测受力后的运动，地面接触又引入外力和摩擦 | 第一遍先抓力平衡、正逆动力学和脚为何会滑 |
| 04 反馈控制 | 一根真实关节 | 开环无法抵抗误差，测量—比较—纠偏逐步长出 P、D、I | 主章保留闭环主线；PD 二阶增益专题提供逐行推导和 RL 真机衔接 |
| 05 状态估计 | 小车只有位置传感器，却需要位置和速度 | 单独信模型或测量都不够，形成“先预测、后纠正” | 第一遍不要求推 KF 矩阵 |
| 06 最优化控制 | 有限力矩的小车/关节必须兼顾到达、平滑和上限 | 候选量、目标、约束先出现，再命名 QP、OCP、MPC | QP 与 MPC 抓结构；求解器和高级变体二读 |
| 07 系统架构 | “去厨房”，途中右脚在 `12.37 s` 滑动 | 一个目标必须跨过导航、规划、估计、控制、驱动与安全 | 先顺着一次真实控制周期读 |
| 08 LIP/ZMP | 人形被向前推 | 同姿态不同速度会有不同未来，支撑区域和下一步由此出现 | 精确双曲函数离散化二读 |
| 09 SLIP/VMC | 一条腿经历落地、压缩、回弹、离地 | 动态跑跳需要看完整一步的储能、释能和落脚 | 先抓一整步时间故事 |
| 10 WBC/TSID | 双脚不动，右手要前伸 `20 cm` | 局部控制互相打架，必须联合选择加速度、力矩和接触力 | 先懂任务与硬约束，HQP 二读 |
| 11 SRBD/凸 MPC | 未来左右脚交替支撑，身体又被侧推 | 为实时看未来，暂时舍弃关节细节，先选接触力 | 线性化与实现细节二读 |
| 12 质心动力学/NMPC | 抱箱行走、移向右侧并转身 | SRBD 看不见内部质量分布变化，因此加入全身动量 | 明确是传统路线进阶章，求解器二读 |
| 13 RL 基础 | 一台机器人从不会站到学会移动 | 手写规则难覆盖复杂接触；MLP 把观测映射为动作，长期后果再推动参数学习 | 第一遍读互动、MLP、Actor/Critic 和部署；PPO/GAE 公式二读 |
| 14 腿足 RL 工程 | 先站住，再跟随 `0.6 m/s` 命令 | 每次实际失败依次引出观测、动作、奖励、重置、课程和频率；停车过渡与 checkpoint 遗忘说明能力不会自动出现或单调增加 | 先完成最小训练题目，指标细节二读 |
| 15 特权学习 | 从普通地面走到低摩擦区域 | 仿真老师看得到真值，Oracle 先定位能力/信息瓶颈，真机学生再只凭普通测量历史行动 | 第一遍抓训练信息、诊断信息与部署信息的边界 |
| 16 感知行走 | 前方 `15 cm` 台阶，后面还有缺口 | 盲走发现得太晚，视觉、坐标、时间、落脚和接触反馈逐层出现 | PIE 与注意力公式放到故事成立以后 |
| 17 重映射与模仿 | 舞者举手、深蹲、左转 90° | 人体数字不能复制给不同骨架，先重映射参考，再闭环模仿 | AMP 和判别器可二读 |
| 18 多动作/BFM | 走路、下蹲、踢腿三个专家合成一个系统 | 单技能切换会失败；统一接口既可是一张纯网络，也可包含专家、门控和有限事件状态 | FB/BFM 明确为第二条进阶故事线 |
| 19 Sim2Real | 仿真直行平稳，真机右膝慢、身体振荡、右脚滑；再对照真实 G1 部署 | “真机不对”被拆成观测、接口、安全后处理、执行器、控制权、时序和接触差异 | 先按诊断顺序排离散接口，再查连续模型 |
| 20 方案选择 | 工厂机器人走到箱子旁并搬运 | 不同任务段的约束、数据和风险不同，组合方案自然出现 | 缩写表只作查询，重点是责任边界 |
| 21 项目路线 | 同一个项目从矩阵测试长到真机行走 | 共同地基后分出 QP-WBC 与从零 RL；模型、部署契约、安全、控制权和恢复共同接受验收 | 先完成共同阶段，再选传统或 RL 分支 |

---

## 5. 三条技术路线怎样保持同一个世界观

### 5.1 传统模型控制

```text
当前估计 x_hat
→ 选择保留哪些物理量的模型
→ 预测候选接触力或动作的未来结果
→ 在任务和约束中选择
→ WBC/低层控制落实当前命令
→ 真实反馈重新估计
```

08～12 的区别不是谁“更高级”，而是预测时保留多少信息、付出多少计算代价。

### 5.2 学习控制

```text
先定义观测、动作、奖励和训练分布
→ 在仿真中反复形成反馈经验
→ 把经验压进策略参数
→ 部署时根据当前观测快速输出动作
→ 真实机器人反馈下一观测
```

13～18 逐步增加的不是神秘网络名词，而是任务难度：从单一行为，到隐藏环境、未来地形、人体动作和许多技能。

### 5.3 真机集成

两条路线最后都必须通过：

```text
真实传感器 → 状态判断 → 当前决定 → 驱动器与电机 → 真实接触 → 新测量
```

所以状态估计、接口、延迟、饱和、安全和日志不是“传统控制附属品”，也不是“RL 训练好以后可以删除的工程杂事”。

---

## 6. 公式审查的当前底线

每个关键公式至少检查：

1. 公式出现前是否已经有物理问题；
2. 左边是要求、估计、预测、损失还是现实量；
3. 右边每个新符号是否原地解释；
4. 命令 `u` 与现实作用 `u_real` 是否区分；
5. 参考与真实结果是否区分；
6. 坐标系和单位是否足以判断式子能否相加；
7. 离散下标表示过去、当前还是未来；
8. 同一字母是否在邻近内容中突然换含义；
9. 高级公式是否明确允许第一遍跳过。

全书统一使用下面的核心关系：

```text
x_real
→ y = h_real(x_real) + noise
→ estimator(y, 旧命令 u, 内部模型 f_model)
→ x_hat
→ controller 或 policy
→ 新命令 u
→ 驱动器、电机和传动
→ 现实作用 u_real
→ 新的 x_real
```

- `x_real`：现实真实状态，通常无法完整直接读取；
- `y`：传感器测量；
- `h_real`：现实测量过程；
- `noise`：测量误差的统称；
- 旧命令 `u`：上一周期程序已经请求的动作，估计器用它预测这段时间本应怎样变化；
- `f_model`：估计器内部的近似状态变化规则；
- `x_hat`：根据模型和测量形成的当前状态判断；
- 新命令 `u`：控制器基于当前 `x_hat` 发出的下一步请求；
- `u_real`：现实执行器实际做到的输入，可能受延迟、饱和、摩擦和故障影响。

同一个字母 `u` 在循环中会先作为“上一周期已经发出的旧命令”进入估计器，再作为“本周期刚算出的新命令”离开控制器。正文必须借助时间顺序说清这一点，不能假设读者自己猜。

---

## 7. 连续阅读时仍允许什么

“叙事完整”不等于“一遍掌握全部数学”。当前正文允许：

- 第 01～03 章先抓物理画面，较密的矩阵性质二读；
- 第 05 章先懂预测与纠正，不推卡尔曼矩阵；
- 第 06 章先懂目标、候选量、约束和滚动重算；
- 第 08～12 章先理解每种模型保留和舍弃什么；
- 第 13～18 章先跟着固定机器人场景，论文结构与高级损失二读；
- 附录 A、B 永远按需查，不要求连续通读。

但不允许把下面这些基础因果关系推给第二遍：

- 一个量从哪里来；
- 为什么需要它；
- 谁使用它；
- 它是事实、估计、参考还是命令；
- 计算以后怎样进入真实闭环。

读者在这些地方看不懂，首先应视为讲义需要检查，而不是读者“数学不好”。

---

## 8. 本轮修改的实际重点

本轮不是给每章机械补一句“举个例子”，而是重构最明显的叙事断层：

- 第 06 章：从一维有限力矩选择长出优化、QP、OCP、MPC 和 LQR；
- 第 07 章：用“去厨房途中滑脚”串起所有系统模块和一次 `1 ms` 周期；
- 第 08～10 章：分别用被推、完整跑步一步、双脚站立同时伸手建立传统控制直觉；
- 第 11～12 章：补上“为什么暂时简化”和“简化何时不够”的连续关系；
- 第 13～14 章：先打开 MLP 策略黑箱，再从一台机器人反复尝试，走到亲手定义站立与 `0.6 m/s` 行走任务；
- 第 15～17 章：用低摩擦、台阶缺口和舞者动作固定三章的具体对象；
- 第 18 章：拆开“已知技能统一”和“无标签行为覆盖”两条原本混在一起的故事；
- 第 19～21 章：用一次真机失败、一次搬箱任务、一个从零 PPO 到 G1 吊架真机的真实项目和同一个逐级路线完成收束；明确安全后处理、控制权、实时线程和发布物也是闭环的一部分。

后续新增或修改内容也应接受同一标尺，不能因为全书已有附录，就退回“列公式—列缩写—列优缺点”的写法。
