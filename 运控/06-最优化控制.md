# 第 06 章：最优化控制——机器人不能“全都要”时，怎样作出选择

> **所属部分**：第一部分 · 物理与闭环基础。
>
> **前置知识**：[第 04 章：反馈控制](04-反馈控制基础.md)和[第 05 章：状态估计](05-状态估计.md)。
>
> **核心问题**：目标彼此冲突、能力又有限时，控制器怎样从许多候选动作中选出一个？
>
> **下一步**：[第 07 章：人形控制系统架构](07-人形控制系统架构.md)。

## 怎么读这一章

这章只沿着一条主线前进：

```text
先选择一个力矩
→ 学会区分目标与约束
→ 推广成 QP（二次规划）
→ 再把一次选择延伸成未来一串选择
→ 理解 MPC（模型预测控制）为什么不断重算
```

建议这样读：

- **第一遍读第 1～9 节**：只理解决策变量、目标函数、约束、QP（二次规划）和 MPC（模型预测控制）；
- **第二遍读第 10～12 节**：再认识 LQR（线性二次调节器）、NMPC（非线性模型预测控制）、软约束和权重；
- **第 13 节是名词地图**：只为以后看论文时不陌生，现在完全可以跳过。

如果读完以后只记住一句话，也应该是：

> **优化器不会替机器人创造能力。它只会在允许的候选答案中，按照人写下的评分规则选择一个。**

---

## 1. 为什么反馈控制之外还需要“选择”

第 04 章中的 PD（Proportional-Derivative，比例—微分）控制已经会根据误差纠偏。目标位置在前方，它就朝前用力；误差越大，通常用力越大。

但真实机器人很快会遇到“不能全都要”的情况：

- 想更快追上目标，就要使用更大力矩；
- 想省电、少发热，又希望力矩小一些；
- 想让手保持不动，同时又想挥动手臂挽救平衡；
- 想让两只脚都牢牢踩住，但下一步又必须抬起一只脚；
- 想立即停下，可电机力量和地面摩擦都有限。

这时，问题不再只是“误差是多少”，而是：

> 几个愿望无法同时完美满足时，应该牺牲哪个、保住哪个？

同时，有些事情不是“尽量做到”，而是绝对不能违反：

- 电机力矩不能超过硬件上限；
- 关节不能穿过机械限位；
- 支撑脚不能要求地面提供根本不存在的摩擦力；
- 候选动作必须符合机器人动力学。

最优化控制就是把这两类信息放在一起：

```text
我更喜欢什么       → 目标函数
我绝对不能违反什么 → 约束
我这次能够选择什么 → 决策变量
```

然后从所有合法候选中，选一个最符合偏好的答案。

---

## 2. 先认识三个最重要的词

先不看任何矩阵。假设控制器需要决定一个电机力矩。

### 2.1 决策变量：这道题让求解器决定什么

用 `τ` 表示候选电机力矩：

```text
τ = 求解器准备选择的力矩
```

此时 `τ` 还不是电机已经产生的真实力矩。它只是数学问题中的一个候选答案。

这种“等待求解器决定的未知量”叫作 **决策变量**。

真实问题可能同时决定许多量，例如：

```text
左腿各关节力矩
右腿各关节力矩
两只脚的候选接触力
全身候选加速度
```

它们都可以成为决策变量。

### 2.2 目标函数：怎样判断一个候选答案好不好

假设希望力矩接近 `5 N·m`。可以用：

```text
Cost(τ)=(τ-5)²
```

给每个候选力矩打分。

- `Cost`：cost，代价或“不满意程度”；
- `τ-5`：候选力矩与期望力矩的误差；
- 平方以后，正误差和负误差都会得到正的惩罚；
- 代价越小，候选答案越好；
- `τ=5 N·m` 时，代价为 0。

这种“给候选答案打分的规则”叫作 **目标函数**。

### 2.3 约束：哪些候选答案禁止使用

假设电机只能输出 `-3 N·m` 到 `3 N·m`：

```text
-3≤τ≤3
```

这叫作 **约束**。不满足约束的候选答案，即使目标函数分数再好，也不允许使用。

现在三个词就齐了：

```text
决策变量：τ
目标函数：(τ-5)²
约束：-3≤τ≤3
```

以后看到任何优化问题，都先问这三件事，不要先研究求解器名字。

---

## 3. 求解器到底在做什么

把几个候选力矩列出来：

| 候选力矩 `τ` | 代价 `(τ-5)²` | 是否满足 `-3≤τ≤3` |
|---:|---:|---|
| `0 N·m` | 25 | 合法 |
| `2 N·m` | 9 | 合法 |
| `3 N·m` | 4 | 合法 |
| `4 N·m` | 1 | 不合法：超过上限 |
| `5 N·m` | 0 | 不合法：超过上限 |

若只看目标函数，`5 N·m` 的分数最好。

但加入电机限制后，`5 N·m` 和 `4 N·m` 都被淘汰。在剩下的合法候选中，`3 N·m` 的代价最小，所以求解结果是：

```text
τ_best=3 N·m
```

- 下标 `best`：表示优化问题选出的最好候选；
- 它不是人的原始愿望 `5 N·m`；
- 它是现实限制下最接近愿望的合法答案。

真正的求解器不会像表格这样一个个枚举所有实数。它会利用目标和约束的数学结构，更聪明地找到最低点。但无论算法多复杂，本质仍然是：

```text
先排除不合法答案
再在合法答案中寻找代价最低者
```

优化不是“保证愿望实现”，而是“在做得到的范围内作出取舍”。

---

## 4. 一个目标不够时，怎样表达取舍

机器人可能既希望力矩接近 `5 N·m`，又希望不要太费力。

可以把两个不满意程度相加：

```text
Cost(τ)
=
w_track(τ-5)²
+w_effort τ²
```

逐项解释：

- `(τ-5)²`：没有达到期望力矩的不满意程度；
- `τ²`：力矩太大的不满意程度；这里是“用力大小”的代理，不等于真实能耗；
- `w_track`：跟踪目标的重要程度；
- `w_effort`：少用力的重要程度；
- 两项相加后得到一个总代价 `Cost(τ)`。

这两个愿望会互相拉扯：

```text
只重视跟踪 → 更愿意选择接近 5 的大力矩
只重视省力 → 更愿意选择接近 0 的小力矩
两者都重视 → 在中间作出折中
```

`w_track` 和 `w_effort` 叫作 **权重**。权重不是某个宇宙真理，而是设计者对任务优先级的表达。

但是权重也不能把硬限制变没。即使把 `w_track` 设得非常大，只要约束仍是 `τ≤3`，合法结果就仍不能超过 `3 N·m`。

这一区别非常重要：

> **权重负责愿望之间的取舍；约束负责划出不可越过的边界。**

---

## 5. QP：把刚才的选择题推广到很多未知数

QP 是 Quadratic Programming，二次规划。

刚才的问题已经是一个最简单的 QP：

```text
在 -3≤τ≤3 中，
寻找让 (τ-5)² 最小的 τ。
```

它之所以叫“二次”，是因为目标函数中出现了决策变量的平方。

### 5.1 为什么平方目标叫“二次”

展开目标函数：

```text
(τ-5)²
=τ²-10τ+25
```

- `τ²`：二次项，因为 `τ` 被平方；
- `-10τ`：一次项；
- `25`：常数项，不随候选 `τ` 改变。

常数 `25` 会让所有候选答案的分数同时增加 25，却不会改变哪个答案最低。因此，寻找最低点时可以暂时省略它：

```text
让 τ²-10τ 最小
```

所谓二次目标，首先就是这种含平方项的评分规则。

### 5.2 为什么突然出现 `z`

真实机器人不会只选择一个力矩。全身控制可能需要同时选择：

```text
候选全身加速度
候选关节力矩
候选接触力
```

把所有等待选择的数装进一个向量：

```text
z=
[
候选全身加速度
候选关节力矩
候选接触力
]
```

这个总盒子叫作 `z`。

`z` 没有固定的物理含义。它只表示“这道优化题允许求解器一起决定的所有数”。另一道题完全可以在 `z` 中放入别的量。

### 5.3 通用二次目标从哪里来

多个未知数的二次目标常写成：

```text
Cost(z)
=
1/2 · zᵀ · H · z
+cᵀ · z
```

第一次阅读只需要这样翻译：

- `z`：所有候选答案组成的向量；
- `Cost(z)`：这整组候选答案的总代价，最终只是一个数；
- `H`：规定哪些候选量要受到平方惩罚，以及惩罚有多重；
- `c`：一次项系数，让最低点偏向某个方向；
- 上标 `T`：转置，使矩阵乘法最终得到一个标量分数；
- `1/2`：方便求导的书写习惯，不改变最低点位置。

它和刚才的一维问题并不是两件事。对：

```text
(τ-5)²=τ²-10τ+25
```

忽略常数 `25`，令：

```text
z=τ
H=2
c=-10
```

就得到：

```text
1/2 · zᵀHz+cᵀz
=1/2 · 2τ²-10τ
=τ²-10τ
```

通用公式只是把“选择一个力矩”推广成“同时选择许多个数”。

### 5.4 等式约束：某个关系必须精确成立

假设机器人静止站立，两只脚的竖直支撑力分别为：

```text
f_left  = 左脚竖直支撑力
f_right = 右脚竖直支撑力
```

假设为了平衡重量，它们的总和必须是 `500 N`：

```text
f_left+f_right=500
```

这是一条等式约束：左脚和右脚可以有不同分配，但总和必须精确达到 500。

若把两个未知力装进：

```text
z=[f_left,f_right]ᵀ
```

同一句话可以写成：

```text
[1,1]z=500
```

通用等式约束写成：

```text
A_eq z=b_eq
```

- `eq`：equality，等式；
- `A_eq`：规定怎样组合 `z` 中的候选量；
- `b_eq`：规定组合结果必须等于多少。

### 5.5 不等式约束：不能越过某条边界

电机上限：

```text
τ≤3
```

是一条不等式约束。

电机下限 `τ≥-3` 可以改写成：

```text
-τ≤3
```

所以完整上下限也能写成矩阵形式：

```text
[
 1
-1
]τ
≤
[
3
3
]
```

它只是在同时表达：

```text
 τ≤3
-τ≤3，也就是 τ≥-3
```

通用不等式约束写成：

```text
A_ineq z≤b_ineq
```

- `ineq`：inequality，不等式；
- `A_ineq`：规定怎样组合候选量；
- `b_ineq`：规定这些组合不能超过的上界。

矩阵只是为了让程序一次表达很多条限制。如果它暂时妨碍理解，就先读上面的两句普通不等式。

### 5.6 现在再读完整 QP

```text
在 z 中寻找一组候选答案，
让
    1/2 · zᵀ · H · z + cᵀ · z
尽可能小

同时满足
    A_eq z = b_eq
    A_ineq z ≤ b_ineq
```

逐行翻译：

```text
z：这次允许求解器选择什么
H、c：怎样给每组候选答案打分
A_eq z=b_eq：哪些关系必须精确成立
A_ineq z≤b_ineq：哪些边界绝对不能越过
```

如果暂时忘掉矩阵，只要还能说出下面这句话，就已经掌握了 QP 主线：

> **QP 在所有满足线性约束的候选答案中，寻找二次目标评分最低的那个。**

### 5.7 “凸”为什么总和 QP 一起出现

把一维平方目标：

```text
Cost(τ)=(τ-5)²
```

画出来，它像一个开口向上的碗：

```text
代价高       ╲         ╱
              ╲       ╱
               ╲_____╱  ← 最低处在 τ=5
                         候选力矩 τ
```

它没有许多个互相分离的山谷。沿着碗壁向低处走，最后会到达同一个最低区域。

这种“中间低、不会藏着许多局部小坑”的形状，可以先叫作 **凸**。对于通用 QP，当二次项矩阵 `H` 是半正定的，而且约束都是线性的，整个问题就是凸的。

“`H` 半正定”第一遍不用计算。它在这里主要保证：目标在任何方向上都不会突然变成开口向下的山峰；可以向上弯，也可以有平坦方向。

凸问题的重要好处是：

> 找到局部最低点时，不必担心旁边还藏着一个更低但没有发现的山谷；这个局部最低点也是全局最低点。

但“凸”不等于“自动成功”：

- 约束互相冲突时，仍然可能根本没有合法答案；
- 碗底存在平坦方向时，可能有多个同样好的答案；
- 数值尺度很差或计算时间不足时，程序仍可能求不好；
- 数学模型即使求得完美，也可能与真实机器人不一致。

所以凸 QP 的意思是“数学结构比较规整”，不是“机器人一定安全”。

---

## 6. 为什么全身控制喜欢使用 QP

想象一个双脚站立的人形机器人，同时面对这些要求：

```text
身体尽量保持直立
质心尽量保持在双脚上方
右手尽量向前伸
两只支撑脚不能穿过地面
关节力矩不能超过电机上限
脚底需要的摩擦力不能超过地面能力
所有动作必须符合全身动力学
```

这里正好存在两类东西。

可以适当妥协的目标：

```text
身体有多直
右手离目标还有多远
动作是否平顺
力矩是否尽量小
```

必须遵守的约束：

```text
全身动力学关系
支撑脚接触关系
电机力矩上下限
地面摩擦限制
```

因此，WBC（Whole-Body Control，全身控制）常把候选全身加速度、关节力矩和接触力一起放进 `z`，再用 QP 协调这些目标与约束。

但要注意：QP 找到的仍是 **候选命令**。

```text
QP 解出的关节力矩 τ
→ 驱动器收到命令 u
→ 电机和传动真正实现 u_real
→ 真实机器人产生运动
```

求解器得到合法数学答案，不等于真实电机一定精确实现，也不等于模型一定正确。第 10 章会具体讲 WBC 怎样构造这道题。

---

## 7. 一次选择为什么还不够

到目前为止，QP 只回答了：

> 当前这一刻，应该选哪个动作？

但控制中的“现在最好”可能导致“下一刻很糟”。

例如汽车接近红灯：

- 如果只看当前速度误差，它可能还想加速；
- 如果看见两秒后必须停车，现在就会提前减速。

机器人走路也一样：

- 当前猛推身体，也许能更快达到目标速度；
- 但推得太猛，下一步可能没有地方落脚；
- 当前主动慢一点，反而能给未来留下恢复余地。

所以控制器常常不是只比较“当前一个动作”，而是同时比较“一串未来候选动作”。

```text
当前状态估计 x_hat_now
→ 候选动作 u[0] → 预测状态 x_model[1]
→ 候选动作 u[1] → 预测状态 x_model[2]
→ 候选动作 u[2] → 预测状态 x_model[3]
```

- `x_hat_now`：估计器对机器人当前状态的判断；
- `u[0]、u[1]、u[2]`：准备比较的未来候选命令；
- `x_model[k]`：内部模型预测的第 `k` 步状态，不是真实未来；
- 方括号中的数字：未来第几步，不是乘法。

这条候选的状态—动作序列叫作 **轨迹**。

---

## 8. OCP：给整段未来轨迹打分

OCP 是 Optimal Control Problem，最优控制问题。

它只是把前面的选择题沿时间展开：

```text
选择未来一串动作
→ 用模型预测这串动作会造成什么状态
→ 给整段预测过程打分
→ 淘汰违反限制的轨迹
→ 留下合法轨迹中代价最低的一条
```

先用人话写：

```text
已知：当前状态估计

需要选择：未来 N 步的候选动作

希望：
    每一步都尽量接近目标
    动作不要过大
    最后一步也不要离目标太远

同时要求：
    每一步状态变化都符合内部模型
    每一步动作和状态都不违反限制
```

再看紧凑写法：

```text
在 x_model[0:N] 和 u[0:N-1] 中寻找：

让
    Σ[k=0→N-1] step_cost(x_model[k],u[k])
    +final_cost(x_model[N])
最小
```

同时满足：

```text
x_model[0]=x_hat_now
```

```text
x_model[k+1]
=f_model(x_model[k],u[k],Δt)
```

```text
每一步都满足状态、输入和接触限制
```

符号解释：

- `N`：向未来预测的离散步数；
- `x_model[0:N]`：从第 0 步到第 `N` 步的所有预测状态；
- `u[0:N-1]`：从第 0 步到第 `N-1` 步的所有候选命令；
- `Σ`：把每一步的代价加起来；
- `step_cost`：每一步的代价，例如速度误差和用力大小；
- `final_cost`：预测窗口最后一步的代价；
- `f_model`：控制器内部用来预测状态变化的模型；
- `Δt`：相邻两个预测时刻之间的时间；
- `x_hat_now`：当前状态估计，是预测的起点。

这一大段公式只在说：

> **不要只评价眼前一个动作，要评价这串动作可能带来的整段后果。**

由于预测使用的是 `x_hat_now` 和 `f_model`，结果不是上帝视角下的真实未来。模型或状态估计错了，预测轨迹也会错。

---

## 9. MPC：规划未来，但只执行眼前一步

MPC 是 Model Predictive Control，模型预测控制。

它反复执行下面的循环：

```text
1. 读取当前状态估计
2. 用模型预测未来 N 步
3. 求出一串候选动作
4. 只下发其中第一步
5. 等待新的传感器测量和状态估计
6. 不再照搬旧计划的剩余部分，结合新状态重新预测和求解
```

工程上可以把旧计划平移后当作新一轮求解的起点，这叫热启动；但它只用于帮助计算，不能因为“上一轮已经算过”就跳过新反馈。

### 9.1 为什么辛苦算出一串动作，却只执行第一步

因为越远的未来越不可信。

假设现在预测机器人两秒后会踩在某个位置。但第一步执行以后，现实可能出现：

- 电机少输出了一点；
- 地面比模型更滑；
- 身体受到外力；
- 状态估计被新测量修正。

原来第二步以后的计划已经建立在旧预测上，不能继续盲目照播。

所以 MPC 的态度是：

> **认真考虑未来，但每走一步都让现实重新发言。**

这种不断把预测窗口向前移动的方式叫作滚动时域。

![MPC 每周期只执行第一步，并根据新状态估计滚动重算](picture/mpc_receding_horizon.svg)

### 9.2 MPC 能看多远

假设：

```text
N=20
Δt=0.02 s
```

预测总时长为：

```text
T=NΔt
=20×0.02
=0.4 s
```

- `T`：预测窗口覆盖的总时间；
- `N` 太小：可能看不到即将发生的失衡；
- `N` 太大：计算更慢，而且远处预测本来就更不可靠。

MPC 需要在“看得足够远”和“算得足够快”之间作出取舍。

---

## 10. LQR：把一种最优选择提前算成反馈规则

这一节第二遍再读也完全可以。

LQR 是 Linear Quadratic Regulator，线性二次调节器。它处理一种结构整齐的问题：

```text
状态变化模型是线性的
评价误差和输入时使用平方代价
```

例如用线性模型描述离散状态变化：

```text
x[k+1]=A x[k]+B u[k]
```

- `x[k]`：第 `k` 步的状态偏差，例如位置误差和速度误差组成的向量；
- `u[k]`：第 `k` 步的控制输入；
- `A`：不加入新控制时，当前状态怎样延续到下一步；
- `B`：控制输入怎样影响下一步状态。

LQR 同时不喜欢状态误差太大和控制输入太大：

```text
Cost
=
Σ[k=0→∞]
(
  x[k]ᵀQx[k]
  +u[k]ᵀRu[k]
)
```

- `Q`：状态误差权重；
- `R`：控制输入权重；
- `Σ`：把未来每一步代价加起来；
- `∞`：理论上的无限未来，用来寻找长期反馈规律。

这里的 `Q、R` 是控制代价权重，不是第 05 章卡尔曼滤波中的噪声协方差。

求解完成后，可以得到一个反馈规则：

```text
u[k]=-Kx[k]
```

- `K`：根据模型 `A、B` 和权重 `Q、R` 计算出的反馈增益矩阵；
- 负号：控制输入朝减小状态偏差的方向作用，前提是状态和执行器正方向定义正确。

因此，LQR 不是部署时每一周期都必须搜索大量候选答案。对于这类固定的线性二次问题，可以先把“怎样作出最优取舍”算成反馈增益 `K`，在线只做一次矩阵乘法。

它和 PD 的共同点是都根据当前偏差反馈；区别是 LQR 明确使用多变量模型和代价权重，能够系统地考虑变量之间的耦合。

---

## 11. 线性 MPC、NMPC 和 QP 到底是什么关系

这些名字不在同一层，混在一起背就会非常痛苦。

| 名称 | 它主要在描述什么 | 先用一句话理解 |
|---|---|---|
| QP | 一道优化题的数学形状 | 二次目标，加线性约束 |
| OCP | 一类控制问题 | 选择未来轨迹，而不只选择眼前一个动作 |
| MPC | 一种在线控制方式 | 反复预测未来，每次只执行第一步 |
| LQR | 一种特殊问题及其反馈解 | 线性模型加二次代价，可以得到 `u=-Kx` |
| NMPC | MPC 使用了非线性模型 | 预测更完整，但求解通常更难 |

线性 MPC 经常能整理成 QP，所以可以使用 QP 求解器。

NMPC 是 Nonlinear Model Predictive Control，非线性模型预测控制。它在预测中保留类似下面的非线性模型：

```text
x_model[k+1]
=f_model(x_model[k],u[k],Δt)
```

这里 `f_model` 不要求能写成固定矩阵 `A、B`。表达能力更强的代价是：问题可能需要反复近似、反复迭代，计算时间和初值敏感性通常更难处理。

所以不能简单说 NMPC 一定“高级且更好”。模型更复杂，如果算不完、参数不准或陷入不好的局部解，真机效果仍可能更差。

---

## 12. 权重、硬约束和软约束

这一节解决优化最常见的三个工程疑问。

### 12.1 权重为什么不能只看数字大小

速度误差的单位可能是 `m/s`，姿态误差可能是 `rad`，力矩可能是 `N·m`。它们的原始数字不能直接公平比较。

例如：

```text
速度误差 = 0.1 m/s
可接受速度误差 = 0.2 m/s
```

可以先归一化：

```text
e_normalized
=e/e_scale
=0.1/0.2
=0.5
```

- `e`：原始误差；
- `e_scale`：认为“已经比较明显”的误差尺度；
- `e_normalized`：归一化后的无量纲误差。

先根据物理容差统一尺度，再用权重表达优先级，比直接乱试 `1000` 或 `0.0001` 更容易解释。

### 12.2 硬约束：违反就不能使用

电机绝对力矩上限通常属于硬约束：

```text
-τ_max≤τ≤τ_max
```

- `τ_max`：硬件允许的力矩绝对值上限；
- 候选 `τ` 超过它，就不是可下发的合法答案。

若两条硬约束互相冲突，数学问题会变成 **不可行**：不存在能够同时满足它们的答案。

### 12.3 软约束：迫不得已时允许付出代价违反

有些要求可以略微放宽，例如“手尽量不要偏离目标区域”。可以引入松弛变量 `s`：

```text
h(z)≤s
s≥0
```

并在目标中惩罚它：

```text
Cost_new=Cost+ρs²
```

- `h(z)≤0`：原本希望满足的限制；
- `s`：允许放宽多少；`s=0` 表示没有违反；
- `ρ`：违反要求的惩罚权重；
- `Cost_new`：加入违约惩罚后的新代价。

这叫作软约束：实在无法两全时，可以用较高代价换取少量违反。

安全边界不能随便软化。相比放宽电机硬件极限或碰撞限制，通常更合理的是放宽手的位置目标、身体姿态目标等任务要求。

---

## 13. 进阶名词地图：第一次可以完全跳过

以后看论文或代码时，还会遇到下面这些词。现在只知道它们在解决什么即可，不需要学习推导。

| 缩写或术语 | 全称 | 它在做什么 |
|---|---|---|
| NLP | Nonlinear Programming，非线性规划 | 处理目标或约束中含非线性关系的问题 |
| SQP | Sequential Quadratic Programming，序列二次规划 | 把非线性问题反复近似成一系列 QP |
| DDP | Differential Dynamic Programming，微分动态规划 | 利用轨迹的时间结构迭代改进控制序列 |
| iLQR | iterative LQR，迭代 LQR | 围绕当前轨迹反复构造局部 LQR 问题 |
| shooting | 打靶法 | 先选动作，再从初始状态向前模拟轨迹 |
| collocation | 配点法 | 同时选择多个时刻的状态和动作，再约束它们彼此连续 |

这些方法的共同任务仍然没有变化：

```text
提出候选轨迹
→ 预测后果
→ 检查约束
→ 比较代价
→ 继续改进
```

不要把“知道缩写”误认为“理解优化控制”。能讲清决策变量、目标、约束、预测和反馈，比背完整条方法谱系重要得多。

---

## 14. 一个贯穿全章的 MPC 小例子

最后把本章概念放进一条完整因果链。

假设一个质量为 `10 kg` 的小车：

```text
当前速度估计：0 m/s
目标速度：1 m/s
最大水平推力：20 N
预测时间步长：0.1 s
预测未来：3 步
```

简化速度模型：

```text
v_model[k+1]
=
v_model[k]
+u[k]/m · Δt
```

- `v_model[k]`：模型预测的第 `k` 步速度；
- `u[k]`：第 `k` 步候选水平力；
- `m=10 kg`：小车质量；
- `Δt=0.1 s`：每个预测时间步；
- 约束为 `-20≤u[k]≤20 N`。

若未来三步都选择最大推力 `20 N`：

```text
每一步速度增加量
=20/10×0.1
=0.2 m/s
```

预测轨迹为：

```text
现在：0.0 m/s
第 1 步：0.2 m/s
第 2 步：0.4 m/s
第 3 步：0.6 m/s
```

它在三步内仍达不到 `1 m/s`，因为推力和时间都有限。优化器不会伪造一条违反 `20 N` 上限的轨迹，只会在合法动作中选择更接近目标的方案。

若只惩罚速度误差，它可能倾向于一直使用最大推力；若同时惩罚用力过大，它可能选择较温和的动作。这取决于目标函数和权重。

MPC 算出三步动作后，只会先下发 `u[0]`。经过 `0.1 s`：

```text
真实电机可能没有精确实现命令
真实地面阻力可能与模型不同
新传感器会产生新测量
状态估计器会给出新的速度判断
```

控制器于是以新的 `x_hat` 为起点，再预测未来三步。

完整闭环是：

```text
当前状态估计 x_hat
→ 构造候选动作序列
→ f_model 预测候选后果
→ 目标函数评分
→ 约束淘汰非法答案
→ 求出最优候选序列
→ 只下发第一条命令 u
→ 真实执行形成 u_real 和真实运动
→ 传感器测量 y
→ 状态估计更新 x_hat
→ 下一周期重新选择
```

这就是最优化控制真正进入反馈闭环后的样子。

---

## 15. 求解器成功，不等于机器人一定成功

求解器报告 `success`，只表示它成功处理了收到的数学问题。下面任何一项出错，真机仍可能失败：

- `x_hat` 不准：优化从错误的当前状态出发；
- `f_model` 不准：候选后果预测错误；
- 单位或坐标系错误：数学上可算，物理意义却错了；
- 漏掉约束：求解器会选择现实做不到的动作；
- 权重不合理：它会忠实优化一个不符合真实意图的目标；
- 求解超时：答案虽然好，却来得太晚；
- `u_real` 偏离命令 `u`：电机没有实现计算结果；
- 接触改变：原本假设踩稳的脚可能已经滑动或离地。

实时系统还会使用一些工程措施：

- **热启动**（warm start）：以上一周期的解作为本周期起点；
- **迭代上限**：到达时间预算就停止继续搜索；
- **监控器**（watchdog）：检查耗时、数值和约束残差；
- **降级方案**（fallback）：求解失败时切换到已知安全的简单控制器或停止策略。

优化控制不是反馈的替代品。它只是让每一周期的选择更有结构；现实是否照做，仍要由下一次测量回答。

---

## 本章小结

1. **决策变量**是求解器能够选择的候选量，**目标函数**负责评分，**约束**负责淘汰不合法答案。
2. 权重表达“更偏爱什么”，硬约束表达“绝对不能做什么”；两者不能混为一谈。
3. QP 是二次目标加线性约束。矩阵公式只是把“选择一个力矩”推广到“同时选择许多个量”。
4. OCP 把一次选择延伸为未来一串选择；MPC 每周期预测未来，却只执行第一步，然后根据新反馈重算。
5. 优化器只对收到的状态估计、模型、目标和约束负责。数学问题求解成功，不保证真实机器人成功。

## 练习

第一遍只做前四题：

1. 希望力矩接近 `5 N·m`，但限制是 `-3≤τ≤3`。为什么结果是 `3 N·m`，而不是 `5 N·m`？
2. 在这道题中，决策变量、目标函数和约束分别是什么？
3. 为什么把某项权重调得无限大，仍然不能合法突破硬件力矩上限？
4. MPC 为什么算出未来一串动作后，只执行第一步？
5. QP、OCP 和 MPC 分别在描述问题的哪个方面？
6. 如果两条硬约束互相冲突，会发生什么？

参考答案：

1. `5 N·m` 违反硬约束；`3 N·m` 是合法范围内最接近目标的答案。
2. 决策变量是 `τ`，目标函数是 `(τ-5)²`，约束是 `-3≤τ≤3`。
3. 权重只改变合法候选之间的偏好，不能取消硬约束。
4. 未来基于当前模型和状态估计，执行一步后现实已经提供新证据，应重新预测。
5. QP 描述优化题的数学形状；OCP 描述跨时间选择轨迹的问题；MPC 描述在线滚动重算、只执行第一步的控制方式。
6. 不存在同时满足它们的候选答案，问题不可行；需要修改任务、约束或谨慎引入松弛。
