# 第 05 章：反馈控制基础——为什么机器人必须不断看结果

## 1. 开环与闭环

开环控制只发预先计算好的命令，不看执行结果。闭环控制持续测量误差并修正：

```text
参考值 r → [控制器] → u → [机器人] → y
             ↑                    │
             └──── 误差 r-y ──────┘
```

- `r`：reference，参考值；
- `y`：传感器提供的系统输出测量；可能带噪声和延迟；
- `e=r-y`：跟踪误差；
- `u`：控制输入。

![反馈控制不断测量实际结果并纠正误差](picture/feedback_control_loop.svg)

真实机器人一定需要反馈，因为负载、摩擦、地面和模型都不确定。

反馈不只是“做错以后补一下”。控制器把本次测量变成下一次输入，从而改变误差在多个时刻之间怎样传播。评价闭环控制时，不能只看某一帧是否接近目标，而要看扰动之后的误差会缩小、持续振荡，还是不断放大。

## 2. P 控制

```text
u = K_p · (r-y) = K_p · e
```

- `K_p`：比例增益（proportional gain）；
- `e`：当前误差；
- `u`：控制输出。

误差越大，纠正越强。`K_p` 太小会软、响应慢；太大会振荡，对噪声和延迟敏感。

## 3. PD 关节控制

PD 是 Proportional-Derivative Control，比例—微分控制：P 项根据位置误差纠偏，D 项根据误差变化速度抑制振荡。

机器人关节最常见：

```text
τ
=
K_p · (q_des-q)
+ K_d · (q_dot,des-q_dot)
```

- `τ`：输出力矩；
- `q_des`：期望关节角；
- `q`：实际关节角；
- `q_dot,des`、`q_dot`：期望和实际关节速度；
- `K_p`：位置误差增益，像弹簧刚度；
- `K_d`：速度误差增益，像阻尼器。

若 `q_des=1.0` rad、`q=0.9` rad、两者期望/实际速度都是 0，`K_p=100` N·m/rad，则比例项输出 10 N·m。

PD 可理解为虚拟弹簧阻尼器。P 把关节拉向目标，D 抑制高速冲过目标。

## 4. PID 中的积分项

PID 是 Proportional-Integral-Derivative Control，比例—积分—微分控制。它在 PD 基础上增加误差累积项，用来消除长期偏差。

```text
u(t) = K_p · e(t) + K_i · ∫[0→t] e(τ) dτ + K_d · e_dot(t)
```

- `K_i`：积分增益；
- 积分项累积长期误差，可消除恒定负载造成的稳态误差；
- `e_dot`：误差变化速度。

积分项容易 windup（积分饱和）：执行器已经达到上限，误差仍在累积，恢复后产生巨大反向输出。需要 anti-windup（抗积分饱和）设计。

腿足机器人的低层常以 PD 或力矩前馈 + PD 为主，不一定直接使用完整 PID。

## 5. 前馈 + 反馈

```text
τ
=
τ_ff
+ K_p · (q_des-q)
+ K_d · (q_dot,des-q_dot)
```

- `τ_ff`：feedforward，前馈力矩，由动力学模型或策略给出；
- 后两项：反馈修正。

软件类比：前馈是根据已知请求量提前配置资源，反馈是根据监控指标自动扩缩容。只有前馈怕模型不准，只有反馈则总要等误差出现才行动。

在机器人中，前馈或预测负责提前准备可能需要的力，反馈负责用真实测量修正预测与现实的差异。两者解决的是不同问题，通常应组合使用。

## 6. 状态空间模型

连续线性系统：

```text
x_dot
=
Ax
+Bu
```

- `x`：状态；
- `x_dot`：状态变化率；
- `u`：控制输入；
- `A`：状态自身如何演化；
- `B`：输入如何影响状态。

输出模型：

```text
y
=
Cx
+Du
```

- `y`：可观测输出；
- `C`：从状态选出或组合观测；
- `D`：输入对输出的直接影响，很多系统中为零。

离散形式：

```text
x_(k+1)
=
A_dx_k
+B_du_k
```

下标 `d` 表示 discrete（离散）。

## 7. 稳定是什么意思

直觉上，系统受到小扰动后能回到目标附近，而不是误差不断增长。

对离散标量系统：

```text
x_(k+1)=a x_k
```

若 `|a|<1`，状态会逐步趋近 0；若 `|a|>1`，状态会发散。多维系统用特征值判断类似性质。初学阶段不必手算，记住稳定性描述的是误差随时间的演化，而不只是某一帧误差小。

稳定也不等于静止。机器人可以一边周期行走，一边保持稳定；更实用的理解是：受到合理扰动后，系统仍能找到并执行至少一种恢复方案，而不是不可逆地进入摔倒。增益越大也不代表越稳定，因为噪声、延迟、饱和和未建模柔性都可能把强纠偏变成振荡。

## 8. 延迟和控制频率

假设低层控制频率为 1 kHz，周期：

```text
Δt=(1)/(1000)=0.001 s=1 ms
```

一次控制循环必须在 1 ms 内完成读取、估计、求解和下发，否则产生 deadline miss（截止时间错过）。

延迟会让控制器根据“过去的状态”施加“现在的动作”。高增益系统尤其容易因此振荡。常见延迟来源：

- EtherCAT（Ethernet for Control Automation Technology，面向控制自动化的实时以太网）或 CAN（Controller Area Network，控制器局域网）通信；
- 传感器滤波；
- 状态估计；
- 优化求解；
- 神经网络推理；
- 电机驱动器内部环路。

控制频率并非越高越好。频率提升必须配合传感器带宽、通信、执行器响应和数值噪声。

## 9. 级联控制

电机内部可能已有电流环、速度环，外部再跑位置/力矩控制：

```text
位置环（较慢） → 速度环 → 电流/力矩环（最快） → 电机
```

外环通常应比内环慢。若不知道驱动器内部做了什么，就容易重复加控制或产生环路冲突。

人形系统也常级联：

```text
高层规划/MPC 30–100 Hz
→ WBC 500–1000 Hz
→ 电机电流环数 kHz
```

这里的 MPC（Model Predictive Control，模型预测控制）负责较低频地预测未来；WBC（Whole-Body Control，全身控制）负责较高频地把全身任务变成关节命令。

## 10. 饱和与限幅

真实电机有：

```text
τ_min≤τ≤τ_max
```

还有位置、速度、电流、温度限制。控制器若长期请求不可实现的力矩，跟踪误差会积累，模型假设随之失效。限幅不是部署前最后加的 `clip()`，而应尽早进入控制设计和训练随机化。

## 11. 调参顺序

以站立关节 PD 为例：

1. 先确认单位、关节方向、零位、力矩符号；
2. 低 `K_p`、足够阻尼的 `K_d` 开始；
3. 缓慢增加 `K_p`，观察跟踪和振荡；
4. 调整 `K_d` 抑制振荡；
5. 检查力矩峰值、温度、延迟和噪声；
6. 每次只改一组关节或一个因素。

如果符号反了，调任何增益都只会更快发散。

## 12. 小练习

1. `K_p=80`，位置误差 0.05 rad 时，比例力矩是多少？
2. 500 Hz 控制循环的周期是多少毫秒？
3. 为什么 D 项会放大编码器速度噪声？
4. 前馈力矩已经准确补偿重力时，为什么仍需要反馈？

答案：1）4 N·m；2）2 ms；3）求导强调高频变化；4）模型、负载和传感器不可能完全准确，外部扰动也不可预知。
