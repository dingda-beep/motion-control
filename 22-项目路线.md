# 第 22 章：从零到站立行走——给软件工程师的项目路线

这不是“读完所有理论再写代码”的路线。每一阶段都有可见产物、验收指标和失败定位。

## 贯穿所有阶段的实验闭环

每完成一个功能，都按同一条证据链工作：

```text
提出假设
→ 写出可复现实现
→ 记录测量与中间量
→ 比较预测和实际
→ 尝试证伪假设
→ 保留或修正模型、代码与参数
```

例如“站不住是 PD（Proportional-Derivative，比例—微分）增益太小”只是一个假设。先记录期望/实际关节状态、力矩饱和、接触力、姿态估计和时间戳；若关节方向写反或脚已经打滑，数据会直接推翻这个假设。

失败时优先归到以下一类，再决定改哪里：

1. 目标或参考不合理；
2. 传感器、坐标、时间戳或状态估计错误；
3. 模型或接触模式与现实不符；
4. 运动学、动力学、摩擦或执行器约束不可行；
5. 求解器、策略或软件接口错误；
6. 驱动器没有实现命令或已经饱和；
7. 反馈频率、延迟或恢复逻辑不足。

“偶尔走通一次”只能证明存在一次成功样本。阶段完成应同时有重复实验、失败分布、中间日志和明确的安全退出条件。

## 0. 环境建议

- Linux；
- Go 用于本教程中的数学练习、接口设计和控制逻辑原型；
- 部分仿真与强化学习平台以 Python 为主要入口，使用它们时只需把 Python 当作工具调用层；
- C++ 用于实时控制与动力学集成；
- Git + 可复现实验配置；
- Pinocchio：运动学/动力学库；
- MuJoCo：轻量、适合控制入门；
- Isaac Lab：使用 GPU（Graphics Processing Unit，图形处理器）并行训练 RL（Reinforcement Learning，强化学习）策略；
- OSQP、qpOASES、Clarabel：几种 QP（Quadratic Programming，二次规划）求解器，任选其一开始即可。

版本和安装方式会变化，应以项目官方文档为准。

## 阶段 1：数学和坐标代码化

### 任务

1. 用 Go 实现二维旋转和平移；
2. 写单元测试验证变换与逆变换；
3. 实现有限差分速度；
4. 对不同 `Δt` 比较欧拉积分误差。

### 验收

```go
// 逆变换乘原变换后，应得到单位矩阵。
requireMatrixClose(t, Mul(inverseT, t), identity, 1e-9)

// 合法旋转矩阵应满足 RᵀR=I，且行列式等于 1。
requireMatrixClose(t, Mul(Transpose(r), r), identity, 1e-9)
if math.Abs(Det(r)-1.0) >= 1e-9 {
	t.Fatal("旋转矩阵的行列式不等于 1")
}
```

### 你会学到

坐标系、单位和离散时间不是理论细节，而是接口契约。

## 阶段 2：二连杆 FK、IK、Jacobian

FK（Forward Kinematics，正运动学）由关节角计算末端位置；IK（Inverse Kinematics，逆运动学）由目标末端位置反求关节角；Jacobian（雅可比矩阵）描述关节速度怎样影响末端速度。

### 任务

1. 手写二连杆 FK；
2. 用有限差分验证解析 Jacobian；
3. 用阻尼伪逆做 IK；
4. 可视化奇异位形附近关节速度。

有限差分验证：

```text
J_(:,i)
≈
(p(q+εe_i)
-p(q))/(ε)
```

- `e_i`：只有第 `i` 项为 1 的基向量；
- `ε`：很小的角度扰动。

### 验收

- FK 与手算示例一致；
- Jacobian 相对误差小；
- 可达点 IK 收敛；
- 不可达点不会产生 NaN；
- 关节限位有效。

## 阶段 3：读懂一个 URDF

URDF 是 Unified Robot Description Format，统一机器人描述格式，用来记录机器人的连杆、关节、质量和碰撞几何。

### 任务

1. 找一个开源人形 URDF；
2. 列出 link、joint、parent、child；
3. 检查关节轴、限位、质量、惯量；
4. 用 Pinocchio 加载并打印 `nq`、`nv`；
5. 计算左右脚和双手的 FK/Jacobian。

### 验收

- 解释为什么浮动基 `nq != nv`；
- 给定默认姿态时脚底高度合理；
- 左右关节方向和命名明确；
- 总质量等于所有 link 质量和。

## 阶段 4：单关节与倒立摆控制

本阶段使用：

- PD（Proportional-Derivative，比例—微分控制）：根据位置和速度误差纠偏；
- LQR（Linear Quadratic Regulator，线性二次调节器）：为线性系统系统化地求反馈增益。

### 任务

1. 仿真单关节 PD；
2. 扫描 `K_p,K_d`，画阶跃响应；
3. 加饱和、噪声和延迟；
4. 实现小车倒立摆 LQR；
5. 比较开环、PD、LQR。

### 指标

- rise time：上升时间；
- overshoot：超调；
- settling time：稳定时间；
- steady-state error：稳态误差；
- 最大控制输入。

## 阶段 5：MuJoCo 双脚站立

这里会第一次实现 WBC（Whole-Body Control，全身控制），把站立和接触要求协调成关节命令。

### 任务

1. 导入 URDF 或 MJCF（MuJoCo XML Model Format，MuJoCo XML 模型格式）；
2. 先用固定基检查关节 PD；
3. 切换浮动基；
4. 生成静态站立姿态；
5. 加重力补偿或最简 WBC；
6. 施加小扰动。

### 验收

- 站立 30 s 不摔；
- 力矩不长期饱和；
- 两脚法向力和约为 `mg`；
- 支撑脚滑移在阈值内；
- 日志可对齐状态、参考、命令和接触力。

失败时先检查：

1. 关节方向与零位；
2. 碰撞体和地面；
3. PD 与控制频率；
4. 初始姿态；
5. 质量和单位；
6. 才检查高级控制。

## 阶段 6：QP-WBC

QP-WBC 表示“用二次规划实现的全身控制器”：QP 负责统一求解任务和约束，WBC 负责把全身目标落实成关节命令。

### 任务

1. 决策变量先只用 `v_dot,τ,λ`；
2. 加全身动力学等式；
3. 加双脚接触加速度约束；
4. 加摩擦与力矩限幅；
5. 先跟踪质心高度；
6. 再加躯干和摆动脚任务。

### 单元测试

- 静态站立解满足力平衡；
- 任务清零时不产生异常大力矩；
- 人工给冲突约束时能检测 infeasible；
- QP 每周期耗时分位数满足 deadline；
- 所有等式残差在容差内。

### 行为验收

站立 → 小幅蹲起 → 重心左右移动 → 单脚减载 → 原地踏步。

不要直接从站立跳到行走。

## 阶段 7A：传统控制行走

SRBD（Single Rigid Body Dynamics，单刚体动力学）把全身近似成一个刚体；Convex MPC（凸模型预测控制）用这个简化模型快速预测未来接触力。

### 最小路线

1. 固定时间步态状态机；
2. Raibert/Capture Point 落脚点；
3. 摆动脚三次或五次多项式轨迹；
4. SRBD Convex MPC 求接触力；
5. WBC 求关节力矩。

### 验收

- 速度命令阶跃响应；
- 左右步态对称性；
- 摩擦锥余量；
- 接触切换力矩连续性；
- 推扰后的捕获步；
- 求解器最坏耗时。

## 阶段 7B：RL 行走

### 最小路线

1. Isaac Lab 中固定参数平地站立；
2. 加水平速度命令；
3. 加随机初始姿态；
4. 加推扰；
5. 加 domain randomization；
6. 加地形 curriculum；
7. 独立测试集评估；
8. 导出 ONNX（Open Neural Network Exchange，开放神经网络交换格式）或 TorchScript，检查训练端和部署端的推理结果是否一致。

### 训练配置必须版本化

```text
robot model hash
physics dt
policy dt / decimation
observation order and scale
action scale
reward weights
randomization ranges
terrain seed
network architecture
checkpoint and code commit
```

### 验收

- 不只看训练平均奖励；
- 在未见 seed、质量、摩擦、延迟上报告成功率；
- 记录最坏分位；
- 关闭随机化重新评估名义性能；
- 观测中无仿真特权泄漏。

## 阶段 8：真机前接口审计

建立一个逐元素表：

| 索引 | 训练观测 | 单位 | 坐标系 | 真机来源 | 范围 |
|---:|---|---|---|---|---|
| 0..2 | command velocity | m/s, rad/s | body | joystick | ... |
| ... | ... | ... | ... | ... | ... |

动作也做相同表格。

### 离线回放

用真机静止日志喂给策略，检查：

- 输出是否接近安全站立；
- 是否有 NaN/Inf；
- 是否频繁饱和；
- 轻微传感噪声是否导致巨大动作变化。

## 阶段 9：真机渐进部署

1. 电机断使能，验证通信与观测；
2. 单关节低力矩；
3. 悬空全关节；
4. 吊架下站立；
5. 重心小范围移动；
6. 原地踏步；
7. 低速直行；
8. 转弯；
9. 扰动；
10. 复杂地形。

每一步定义：

- 最大试验时长；
- 成功标准；
- 自动停止条件；
- 人工急停角色；
- 回放日志检查表。

## 阶段 10：模仿学习

这一阶段会用到：

- RSI（Reference State Initialization，参考状态初始化）：从参考动作的随机时刻开始训练；
- AMP（Adversarial Motion Priors，对抗运动先验）：用判别器学习动作风格；
- PPO（Proximal Policy Optimization，近端策略优化）：用于更新控制策略。

1. 选一段 3–5 秒、低动态、高质量动作；
2. 统一坐标和频率；
3. 做受限 retarget；
4. 检查位置、速度、接触连续；
5. 用 RSI + adaptive sampling 训练 DeepMimic；
6. 再尝试多动作或 AMP。

先学会单动作的完整数据管线，再扩大到 AMASS（Archive of Motion Capture as Surface Shapes，大规模人体动作捕捉集合）或 LAFAN1（Ubisoft La Forge 发布、常用于动作预测与补间的人体动作数据集）规模。

## 推荐仓库阅读方式

不要从训练入口文件一路跳转到所有依赖。按运行时数据流：

1. robot config；
2. observation builder；
3. action application；
4. reward；
5. reset/termination；
6. PPO rollout/update；
7. export；
8. deployment loop。

每读一个张量，写下：

```text
shape / unit / frame / frequency / train-only? / deploy source
```

## 最终作品

建议仓库至少包含：

```text
docs/
configs/
models/
src/control/
src/estimation/
src/simulation/
src/deployment/
tests/
logs/schema.md
experiments/
```

最终报告应对比至少一个传统 baseline 和一个 RL baseline，不只展示视频。
