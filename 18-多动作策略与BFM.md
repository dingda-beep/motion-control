# 第 18 章：多动作策略与 BFM——从一个动作到行为基础模型

> **所属部分**：第三部分 · 学习控制。
>
> **前置知识**：[第 14 章](14-腿足强化学习工程.md)、[第 17 章](17-动作重映射与模仿学习.md)。
>
> **核心问题**：怎样从单技能专家扩展到覆盖许多动作的统一策略，同时避免遗忘和能力稀释？
>
> **下一步**：[第 19 章：Sim2Real 与控制工程](19-Sim2Real与控制工程.md)。

## 怎么读这一章

- **第一遍读第 1～4、8～10 节**：只理解多技能为什么会互相干扰、数据不平衡为什么会遗忘，以及“会生成很多动作”为什么不等于真机稳定。
- **第二遍读第 2～3 节**：再看多 Teacher、蒸馏、残差微调和自适应采样。
- 第 5～7 节的 FB/BFM-Zero 是进阶研究概览。第一次完全可以跳过公式，只读每节开头的房间机器人例子和输入输出。

BFM 是 Behavior Foundation Model，行为基础模型：希望一个模型覆盖许多动作和技能，而不是每个动作单独训练一个控制器。这个术语仍在快速演化；本章把多动作模仿、无监督技能和大规模运动控制放在同一脉络下讨论。

![从多个技能和专家到统一行为策略，再到闭环评估](picture/multi_skill_policy.svg)

## 1. 单策略多动作为什么难

训练一个只会直行的策略时，所有数据都在推动同一个目标。把跑步、踢腿、下蹲和单腿站立塞进同一网络后，不同样本可能要求同一组参数朝相反方向变化。例如某个更新让跑步时抬腿更高，却破坏了单腿站立所需的稳定姿态。

“梯度冲突”只是这一直觉的数学表达。梯度可以先理解为“为了让某个任务损失下降，参数应该朝哪个方向修改”：

行走、跑步、踢腿、单腿站立对策略参数的梯度可能冲突：

```text
gradient(L_i) · gradient(L_j) < 0
```

- `L_i,L_j`：动作 `i,j` 的训练损失；
- `gradient(L_i)`：为了让第 `i` 个任务损失变化，参数最敏感的方向；
- 中间的 `·`：两个梯度向量的点积；
- 点积小于 0 表示两个方向夹角大于 90°，沿一个任务的下降方向更新时可能损害另一个任务。

还存在：

- 数据量极不平衡；
- 高动态动作探索更难；
- 同一观测下动作可能多峰；
- 网络容量与并行环境不足；
- 机器人本体无法完成部分数据。

## 2. 多 Teacher → Student → RL 微调

这里的 RL 是 Reinforcement Learning，强化学习；它通过环境奖励继续修正蒸馏后的策略。

### 阶段一：多个 Teacher

按动作类别、动态程度或 hard mining（困难样本挖掘）拆分数据，分别训练专家。

### 阶段二：Student 蒸馏

一个 Student 模仿多个 Teacher：

```text
L_distill
=
𝔼_i,o
‖
π_S(o,i)
-π_T_i(o)
‖²
```

- `i`：技能/动作标识；
- `π_T_i`：第 `i` 个 Teacher；
- `π_S`：统一 Student。
- `o`：Student 和对应 Teacher 用来生成标签的观测；
- `𝔼_i,o`：对采样到的技能 `i` 和观测 `o` 取平均；
- `L_distill`：Student 与对应 Teacher 动作之间的平均平方误差。

Student 与 Teacher 的动作必须具有相同顺序、单位和缩放。如果专家是随机策略，只比较单次采样动作会引入额外噪声，通常要明确比较均值还是完整动作分布。

环境分配应考虑每个动作数量和难度，避免简单动作淹没难动作。

### 阶段三：残差 RL 微调

冻结 Student，训练小残差网络：

```text
a
=
a_student
+αa_res
```

- `a_student`：冻结的 Student 基础动作；
- `a_res`：残差网络学到的修正动作；
- `α`：限制残差影响大小的缩放；
- 三者必须使用相同动作空间，最终动作仍要经过安全裁剪和真实执行验证。

并用 KL（Kullback–Leibler，库尔贝克—莱布勒）散度正则限制偏离。KL 散度衡量新策略和原策略的概率分布差异：

```text
L
=
L_PPO
+β
D_KL
(π_new‖π_student)
```

- `D_KL`：KL 散度；
- `L_PPO`：残差策略的 PPO 训练损失；
- `β`：不希望新策略偏离原 Student 的正则权重；
- `π_new‖π_student`：比较新策略分布与原 Student 分布，双竖线在这里是 KL 的参数分隔，不是普通范数。

微调让策略重新理解“摔倒代价”，但过强会破坏已蒸馏动作。

## 3. SONIC 的规模化思路

SONIC 一类工作探索：数据帧、并行环境、网络容量一起扩大时，单策略能否跟踪大规模动作库。

这里把 SONIC 当作具体方法名称使用；它不像 MPC（Model Predictive Control，模型预测控制）那样是一个可以仅凭字母展开就理解职责的通用控制缩写。阅读对应论文或代码时，应以作者对数据、采样和策略结构的定义为准。

一种复现经验给出的优先级是：

```text
自适应采样 > 并行环境数 > 网络容量 > 未来参考帧
```

这不是普适定律，但说明数据规模扩大后，采样分配和覆盖率比单纯换网络模块更关键。

### 自适应采样

为动作片段 `i` 维护失败分数 `e_i`：

```text
p_i
=
((e_i+ε)^α)/(Σ_j(e_j+ε)^α)
```

- `p_i`：采样片段 `i` 的概率；
- `e_i`：片段 `i` 的非负失败分数或难度分数；
- `ε`：避免零概率；
- `α`：聚焦难例的强度；`α=0` 时这一项退化成均匀概率，`α` 越大越偏向高失败分数片段；
- 分母 `Σ_j`：对所有片段分数求和，使全部 `p_i` 的和等于 1。

还要保留一定均匀采样，防止旧技能被遗忘。

## 4. Scale 不是免费午餐

扩大规模同时增加：

- GPU（Graphics Processing Unit，图形处理器）显存与训练天数；
- 数据清洗和重映射成本；
- 低质量动作污染；
- 评估难度；
- OOD（Out of Distribution，分布外）交互时的剧烈恢复，即遇到训练中没覆盖的状态时动作可能失控；
- 训练可复现性问题。

“数据越多越鲁棒”只有在数据质量、采样和模型容量匹配时成立。

## 5. FB：无监督行为表征

这一节比前面的单动作模仿更抽象，可以先用一个网格机器人理解目标。假设机器人能在房间里上下左右移动，但训练时没有人逐个写“去门口”“去桌边”的奖励。我们希望它先通过探索学会房间中哪些位置能互相到达；使用时再给一个目标位置，让它调用合适的移动行为。

FB 是 Forward-Backward Representation，前向—后向表征。它把上面的想法推广到高维机器人状态：先学习“从当前状态采取某种行为后，容易到达哪些未来状态”，再用一个潜变量 `z` 选择行为方向。

这里的 `z` 不是“跑步=1、跳跃=2”这样的人工标签。它是一组由训练形成含义的数字；改变 `z`，策略可能产生不同的可达行为。只有通过执行结果或目标状态，才能解释某个 `z` 实际代表什么。

各模块先按职责理解：

- Backward 网络 `B(s)` 把状态编码到潜空间；
- Forward 网络 `F(s,a,z)` 评估在技能 `z` 下从状态动作访问未来状态的关系；
- Actor 根据当前状态和潜变量 `z` 产生动作；
- 训练时广泛采样 `z`，不为每个技能手写奖励。

仍用房间例子：`B(s_goal)` 把“门口这个目标状态”变成一个潜向量；Actor 读取当前位置和该向量，产生朝门口移动的动作。对人形机器人，`s_goal` 不只包含位置，还可能包含身体姿态、关节状态和速度，所以“到达目标”可能表现为走到某处，也可能表现为摆成某个动作。

下面的内积公式是这种关系的一种紧凑写法，不需要第一次阅读就推导它。先记住输入输出：`F` 评价当前状态和动作，`z` 指定希望到达的行为方向，二者匹配得越好，`Q` 越大。

一个抽象的隐空间价值：

```text
Q(s,a,z)
≈
F(s,a,z)ᵀz
```

- `z`：技能/目标潜变量；
- `s`：当前状态；`a`：候选动作；
- `F(s,a,z)`：与 `z` 维度匹配的前向表示向量；
- `F(s,a,z)ᵀz`：两个潜向量的点积；
- `Q(s,a,z)`：在技能方向 `z` 下评价状态—动作的标量价值；
- Actor 最大化对应 `Q`。

训练后目标状态可编码为：

```text
z_goal
=
(B(s_goal))/(‖B(s_goal)‖)
```

再输入 Actor，尝试到达该状态。

- `B(s_goal)`：Backward 网络对目标状态的编码；
- `‖B(s_goal)‖`：编码向量的长度；
- 相除把它归一化成单位方向，防止向量大小而不是方向主导行为；若编码接近零，实际实现必须加小量或采用其他保护，避免除零。

## 6. BFM-Zero 的三种使用方式

### Goal reaching

给定目标状态 `s_goal`，用 `B` 得到技能向量。

### Motion tracking

对目标序列 `s_goal,t` 每时刻编码 `z_t`，策略跟随。

### Reward guidance

根据候选状态的奖励加权聚合 `B(s)`，构造偏好对应的 `z`。

若负载等导致 OOD，可不改网络参数，只优化潜变量：

```text
z_best
=
arg max_(z∈𝒵) Score(z)
```

- `𝒵`：允许搜索的潜变量范围；
- `Score(z)`：使用候选潜变量 `z` 后得到的任务表现估计；
- `arg max`：返回让分数最大的那个候选 `z`，不是返回最大分数本身；
- `z_best`：本次搜索选出的潜变量。

## 7. 为什么无监督动作不一定像人

动力学可行和连续不等于拟人。策略可能发现人类不会使用但机器人能完成的动作。可加入：

- GAN（Generative Adversarial Network，生成对抗网络）或 AMP（Adversarial Motion Priors，对抗运动先验）风格损失，用判别器约束动作看起来像示例数据；
- 关节限位、碰撞、能耗惩罚；
- 少量人体数据正则；
- 任务条件。

原始资料使用 **FBCPR** 作为该组合方案的标签，并概括为 FB + GAN 风格正则 + 物理惩罚。它不像 MPC（Model Predictive Control，模型预测控制）或 PPO（Proximal Policy Optimization，近端策略优化）那样已有较统一的含义；阅读具体论文或代码时，应以作者给出的全名和实现为准，不能只凭这几个字母推断模块。

## 8. DeepMimic 与 FB 的差别

DeepMimic 从数据定义要会什么；FB 先探索可达行为空间，再在推理时用目标状态或奖励选择行为。

DeepMimic：

- 数据利用直接；
- 精确跟踪强；
- 数据外状态恢复可能剧烈。

FB：

- 潜在覆盖更广；
- 行为连续性有吸引力；
- 训练和表征更难；
- 无监督覆盖不保证真正覆盖所有有意义技能；
- 拟人性需额外约束。

## 9. 评估一个 BFM

不能只展示精选视频。至少评估：

- 动作覆盖率；
- 跟踪误差分布；
- 扰动恢复；
- 技能切换平滑度；
- OOD 目标行为；
- 真机成功率；
- 数据和算力规模；
- 不同本体迁移；
- 安全边界；
- 与单技能专家的性能差距。

这些指标要在完整闭环轨迹上统计，而不是只比较生成的一帧动作是否自然。尤其要分别报告训练分布内跟踪、分布外扰动后的恢复、技能切换和真机执行；模型能生成许多动作，不等于它在真实本体上拥有同样大的稳定可达域。

## 10. 术语保持谨慎

“Foundation Model”容易让人联想到语言模型的开放式泛化，但行为模型受本体结构、接触和物理可达域强约束。大模型、更多数据和更多参数不能越过电机力矩、关节范围和地面摩擦。

规模扩大改变的是策略在训练数据和探索分布中学到的覆盖范围，不会取消状态估计误差、动作接口、延迟和安全层。BFM（Behavior Foundation Model，行为基础模型）仍是闭环中的一个决策模块，而不是完整机器人系统。

## 本章小结

- 多动作策略面对数据不平衡、梯度冲突、遗忘和多峰行为，扩大数据与网络并不会自动解决这些问题。
- 多 Teacher、蒸馏、残差微调和自适应采样把复杂度放在不同训练阶段；FB 则尝试先学习可达行为表示。
- 行为覆盖不等于拟人、稳定或真机可执行，必须分别评估跟踪、扰动恢复、技能切换、分布外目标和安全边界。

## 11. 练习

1. 多 Teacher 蒸馏为什么能缓解梯度冲突？
2. 为什么自适应采样不能完全取消均匀采样？
3. FB 中 `z` 是人工动作标签吗？
4. 无监督学出的动作为什么还要风格正则？

答案：1）冲突技能先在独立参数空间学习，Student 再做监督融合；2）否则已学会片段可能不再采样而遗忘；3）通常不是，训练初期是采样的潜变量，其语义在表征学习中形成；4）物理可行行为不一定符合人的运动分布。
