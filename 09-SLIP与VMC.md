# 第 09 章：SLIP 与 VMC——用虚拟弹簧理解奔跑和落脚

SLIP 是 Spring-Loaded Inverted Pendulum，弹簧负载倒立摆，用弹簧腿描述跑跳时的压缩和回弹。VMC 是 Virtual Model Control，虚拟模型控制：它在软件中构造虚拟弹簧和阻尼，再换算成关节力矩。

![SLIP 的弹簧腿直觉与 VMC 的虚拟力控制流程](picture/slip_vmc_principle.svg)

## 1. 从硬腿到弹簧腿

LIP（Linear Inverted Pendulum，线性倒立摆）假设质心高度固定，适合较平缓的行走。跑步时腿会压缩、回弹，质心明显上下运动。SLIP 把支撑腿视为一根无质量弹簧：

```text
F_spring
=
k · (l_0-l) · e
```

- `F_spring`：沿腿方向的弹簧力；
- `k`：虚拟弹簧刚度，单位 N/m；
- `l_0`：弹簧自然长度；
- `l`：当前质心到足端的距离；
- `e`：从脚指向质心的单位向量。

当 `l<l_0`，弹簧被压缩，产生把身体推开的力。

加入阻尼：

```text
F_leg
=
[k · (l_0-l)-d · l_dot] · e
```

- `d`：阻尼系数，单位 N·s/m；
- `l_dot`：腿长变化速度；
- 阻尼消耗振荡能量。

## 2. 支撑相与摆动相

- stance phase：支撑相，脚接地，调节地面反力；
- swing phase：摆动相，脚离地，控制它到下一落脚点；
- gait：步态，描述各腿接触相位随时间的组织方式。

控制器必须知道或估计当前接触相位。相位判断晚几毫秒可能让摆动控制和地面硬碰撞，或让支撑力突然消失。

## 3. VMC 的思想

VMC 不要求真实机器人有物理弹簧。它在任务空间构造虚拟弹簧、阻尼器和姿态力矩，再通过雅可比转置映射到关节：

```text
τ
=
JᵀF_virtual
```

例如质心高度控制：

```text
F_z
=
mg + K_p · (z_des-z) + K_d · (z_dot,des-z_dot)
```

- `F_z`：期望竖直支撑合力；
- `mg`：抵消重力的前馈；
- `z_des,z`：期望和实际质心高度；
- `z_dot,des`、`z_dot`：期望和实际竖直速度；
- `K_p,K_d`：高度方向虚拟刚度和阻尼。

若没有 `mg` 前馈，控制器必须先产生高度误差，P 项才会提供抗重力的力。

## 4. Raibert 落脚点

经典启发式：

```text
x_foot
=
x_hip
+(T_s)/(2)v
+K_v(v-v_des)
```

- `x_foot`：下一落脚点；
- `x_hip`：髋或质心当前水平位置；
- `T_s`：预计支撑相时长；
- `v`：当前水平速度；
- `v_des`：期望速度；
- `K_v`：速度反馈增益，单位 s。

第一部分 `(T_s)/(2)v` 让脚落在自然对称位置；第二部分用速度误差修正：

- 跑得比目标快，脚向前多落一点以“刹车”；
- 跑得太慢，脚相对向后落以“加速”。

具体符号方向取决于坐标定义和实现。

### 数值例子

`x_hip=0`、`T_s=0.3` s、`v=1.0` m/s、`v_des=0.8` m/s、`K_v=0.1` s：

```text
x_foot=0+0.15+0.02=0.17 m
```

## 5. 姿态控制

小角度下可直观写成：

```text
τ_body
=
-K_p · e_R
- K_d · ω
```

- `τ_body`：期望身体力矩；
- `e_R`：当前姿态相对期望姿态的误差向量；
- `ω`：身体角速度；
- 负号表示把误差拉回零。

大角度时不能直接相减欧拉角，应使用旋转矩阵或四元数定义姿态误差。

## 6. 一个清楚的控制循环

```go
var jointTorque Vector

if contact == Stance {
	// 支撑相：先计算希望地面提供的力，再映射成关节力矩。
	desiredWrench := virtualSpringDamper(state, reference)
	jointTorque = MatVecMul(Transpose(contactJacobian), desiredWrench)
} else {
	// 摆动相：计算下一落脚点，并用足端比例—微分控制跟踪。
	footTarget := raibertFootPlacement(state, command)
	jointTorque = swingFootPD(footTarget, state)
}
```

这段循环的输入 `state` 应理解为状态估计 `x_hat`，`reference` 是期望运动；`desiredWrench` 只是希望通过接触产生的外力，并不是已经发生的真实外力。命令下发后，只有真实接触、摩擦和执行器共同决定机器人怎样响应，传感器再把结果送回下一周期。

真实多腿/双足系统还需：

- 多接触力分配；
- 摩擦和法向力限制；
- 关节力矩限幅；
- 平滑相位切换；
- 摆动脚抬高以避免绊倒；
- 落地速度和冲击管理。

## 7. 接触检测

### 直接检测

足底力传感器测得 `F_z` 超过阈值，并持续一定时间，判断接触。需要迟滞（hysteresis）避免阈值附近反复跳变：

- `F_z>F_on`：进入接触；
- `F_z<F_off`：离开接触；
- `F_on>F_off`。

### 力矩估计

由：

```text
τ≈Jᵀf
```

根据关节力矩估计足端力。雅可比奇异、摩擦和电机力矩估计误差都会影响结果。

## 8. VMC 与 WBC 的差别

VMC 通常是启发式地构造任务空间力并映射到关节；WBC（Whole-Body Control，全身控制）把多个任务、完整动力学和物理约束一起放进 QP（Quadratic Programming，二次规划）统一求解。

VMC：

- 直觉强、实现快；
- 很适合建立弹簧腿和落脚直觉；
- 多任务和约束冲突处理较弱。

WBC：

- 构建复杂；
- 能统一处理动力学、接触、摩擦、力矩和多任务；
- 依赖较准确的模型和实时求解。

## 9. 失败模式

- 刚度太大 + 延迟：高频振荡；
- 阻尼太小：弹跳不止；
- 阻尼太大：动作迟钝、能量损失大；
- 接触误判：错误控制相位；
- 落脚点超出腿的工作空间；
- 没有限制摩擦：请求无法实现的水平力；
- 仅用雅可比转置：忽略全身耦合和约束。

其中接触相位错误尤其关键：它不只是一个分类标签错了，而是让控制器误判“当前有哪些外力可以使用”。因此应同时检查计划相位、估计相位和足底真实响应，而不是只调整虚拟弹簧参数。

## 10. 练习

1. `l_0=0.9` m、`l=0.85` m、`k=2000` N/m，忽略阻尼时弹簧力大小是多少？
2. 为什么向前跑太快时，应把脚落得更靠前？
3. 为什么接触检测需要迟滞？
4. VMC 中的“虚拟”意味着什么？

答案：1）100 N；2）产生减速所需的地面反力与力矩；3）避免噪声造成状态机抖动；4）软件构造弹簧/阻尼的力学行为，不要求硬件真的装有对应元件。
